# β07-03章

> **編**: β編

## 計算式

- `MTTF + MTTR`
- `Q = 2C₀R`
- `TEC = C₀R/Q + C_iQ/2`
- `TEC = C_S·r/Q + C_i(m - r)Q/m·1/2`
- `Q = 2mC_Sr/C_i(m - r)`
- `E(L) = ₀`
- `{c(s - r)}f(r)dr + ₆`
- `{d(r - s)}f(r)dr`
- `F(s) = ₀`
- `f(r)dr = c + d`
- `L(y) = p²`
- `(r - y)f(r)dr + h₀`
- `(y - r)f(r)dr`
- `(y＞aのとき)C₀ + c(y - a) + L(y)`
- `C₀ + cS + L(S)＜ca + L(a)`
- `C₀ + c(S - a) + L(S)＜L(a)`
- `C₀ + cy + L(y)≧ca + L(a)`
- `C₀ + c(y - a) + L(y)≧L(a)`
- `C₀ + cy + L(y)＞ca + L(a)`
- `C₀ + c(y - a) + L(y)＞L(a)`
- `K = μ·T + u(α)Tσ`
- `s = μ×(R + T) + u(α)R + Tσ`
- `ᵢ(t), 標準`
- `Fᵢ(t) = x`
- `ᵢ(t) + kᵢσᵢ(t)`
- `Bᵢ = Fᵢ(Lᵢ) = x`
- `ᵢ(Lᵢ) + kᵢσᵢ(Lᵢ)`
- `Ūᵢ = Fᵢ(t) - x`
- `ᵢ(t) = kᵢσᵢ(t) = kᵢσᵢ(1)t = kᵢσᵢ(1)Lᵢ - Sᵢ`
- `C = ∑Cᵢ = ∑pᵢkᵢσᵢ(1)Lᵢ - Sᵢ = ∑pᵢkᵢσᵢ(1)Sᵢ₄₁ - Sᵢ + Tᵢ`
- `Bᵢ = x`
- `ᵢ(1)(Sᵢ₄₁ - Sᵢ + Tᵢ) + kᵢσᵢ(1)Sᵢ₄₁ - Sᵢ + Tᵢ`
- `Bᵢ = x`
- `ᵢ(1)(Sᵢ₄₁ - Sᵢ + Tᵢ) + kᵢσᵢ(1)Sᵢ₄₁ - Sᵢ + Tᵢ + Qᵢ`
- `V₅ = ∑`
- `p₅R₅`
- `wᵢ = Sᵢ/S_T∑`
- `V₅/∑`
- `hᵢ = i/n`

## 表

| 作られ，組立作業に必要な部品はそのスケジュールに従って調達さ 表3･1 二つの生産管理システムの比較 れる．部品の調達が計画どおり行われるならば，生産は間違いな | | |
|---|---|---|
| | 資材手配中心の生産管理シ ステム | 生産スケジューリング中心 の生産管理システム |
| | 資材手配が生産スケジュー リングに先行して行われ る． | 生産スケジューリングが資 材手配に先行して，あるい は両者が同時に行われる． |
| | 多くの場合，負荷計画に基 づいて行われるため，精度 が低い． | 詳細な生産スケジュールに 基づいて行われるため，精 度が高い． |
| | タイムバケットと加工時間 の差が累積され，見積もら れた生産リードタイムが長 くなり，実際の時間もそれ に合わせたものになる． | スケジューリングの段階で リソースの干渉のない計画 が作成され，資材がスケジ ュールに合わせて供給され るため，必然的に短くなる． |
| | 生産リードタイムが長くな ることは仕掛在庫の増大を 意味する．共通部品は一般 に先行して作られるため， それが多量になると仕掛在 庫は高水準になる． | 生産リードタイムが短くな ることは仕掛在庫の減少を 意味する．共通部品の紐付 き管理を原則とするので， 仕掛在庫の増加が抑制され る． |
| | 仕掛在庫の増大や変動によ り，単位期間当たりに発生 する材料費，諸経費の算定 が難しくなる． | 仕掛在庫が低水準に抑えら れる場合は，単位期間当た りに発生する材料費，諸経 費の算定が容易になる．出 荷高が売上高に一致する経 営を目指しやすくなり，単 位期 |
| | MRPあるいはMRPⅡ | スケジューリングソフトと MRP，あるいはAPS |
| | BOM | M-BOM，加工時間 |
| 月間生産計画量 | 1日の生産台数 |
|---|---|
| 6000台 | 300台 |
| 4000台 | 200台 |
| 2000台 | 100台 |
| では間に合わない．調達期間Tの払出量は，分散の加法性から平 のときにS－aだけ発注するのが最適政策であることが示されてい 均μ·T，標準偏差√√Tσの正規分布に | | | | | |
|---|---|---|---|---|---|
| 品切れ確率α % | 20 | 10 | 5 | 2.5 | 1.0 |
| 安全係数u(α) | 0.84 | 1.28 | 1.65 | 1.96 | 2.33 |
| 次元化したパラメータを用いる方法か，運転条件に合わせて基準値 に対して，受動機械は静止構造物のように外部から刺激を与えない を変化させる方法がとられる．後者につ | | |
|---|---|---|
| | 検出技術名（例) | 応用診断技術例 |
| | (１)機械インピーダン ス法 (２)音響インピーダン ス法 (３)ハンマリング法 | (１)機械インピーダンス法による欠 陥診断 (２)音響インピーダンス法による材 質診断 (３)ハンマリング法による欠陥診断 |
| | (１)管内検査法 (２)ホログラフィ法 | (１)ファイバスコープによる管内検査 (２)ホログラフィによる表面欠陥診断 |
| | (１)パルス反射法 (２)パルス浸透法 (３)共振法 (４)超音波ホログラフ ィ法 (５)超音波インピーダ ンス法 | (１)パルス法による内部欠陥診断 (２)超音波ホログラフィによる内部 欠陥診断 (３)肉厚診断 (４)超音波によるベルト接合部の診断 (５)超音波インピーダンス |
| | (１)ストレインゲージ系 (２)ストレスコート法 | (１)応力分布による欠陥の診断 (２)応力分布による異常組立の診断 |
| | (１)応力検査法 (２)張力検査法 | (１)磁気ひずみによる応力分布診断 (２)磁気ひずみによる材質疲労の診断 |
| | (１)水圧検査法 (２)真空検査法 | (１)水圧検査によるリーク検出 (２)真空検査によるリーク検出 |
| | (１)電気応答法 (２)電気パラメータ法 ρ法 tanδ法 c法 ε法 | (１)電気応答法による制御系の診断 (２)電気抵抗法による肉厚診断 (３)電気抵抗法によるクラックの診断 (４)tanδ法による絶縁劣化の診断 (５)ε法による |
| | (１)渦流検査法 (２)磁粉検査法 (３)録磁検査法 (４)コイル検査法 | (１)渦流法による内部欠陥診断 (２)渦流法による材質の診断 (３)渦流法によるロープ劣化の診断 (４)渦流法によるチェーン劣化の診断 |
| | (１)透過法 (２)RIトレース法 (３)放射化分析 | (１)透過法による内部欠陥診断 (２)RIトレース法による摩耗診断 (３)放射分析による微量漏えいの診 断 |
| | (１)石けん膜法 (２)着色浸透法 (３)蛍光浸透法 | (１)石けん膜法による漏えい診断 (２)着色浸透法による表面欠陥の診 断 (３)蛍光浸透法による表面欠陥の診 断 |
| 検出系 | 検出技術名（例) |
|---|---|
| 振動音響 信号系 | (１)振動法 (２)振動モード法 (３)音響法 |
| 超音波信 号系 | (１)超音波法 (２)気中超音波法 (３)応力超音波(AE)法 |
| 圧力信号 系 | (１)圧力脈動法 (２)圧力損失法 (３)衝撃圧力法 |
| 熱的信号 系 | (１)諸温度計法 (２)サーモグラフィ法 (３)熱流計法 (４)示温素子，ほか |
| 電気的信 号系 | (１)波形法 電圧波 電流波 電力波 (２)放電（コロナ）法 (３)直流分法 (４)漏えい電流法 |
| 磁気信号 系 | (１)漏えい磁束法 (２)磁束変動法 |
| 化学的信 号系 | (１)固体分析法 (２)液体分析法 (３)気体分析法 |
| 機械信号 系 | (１)回転速度法 (２)変位法 (３)流量法 |
