/* Per-page explanations, injected under each page title by core.renderCurrent.
 * Keyed by view.module. Plain author-controlled HTML (<b> only).
 * Scope: only Ask and the Labs that visualize a real step of Ask's processing. */
(function (NSCode) {
  'use strict';
  NSCode.pageNotes = {
    ask:
      '本物の LLM（Claude）と同じ<b>「検索＋重み」</b>のハイブリッドで回答するページ。質問のたびに、まず知識ベースから<b>関連箇所を検索</b>し、その文脈を<b>極小ニューラルネット（埋め込み→隠れ層→softmax）が学習</b>。学習した<b>重みで根拠文を選び直し</b>、約100字の<b>自然な回答</b>にまとめます（実文ベースなので文法が崩れません）。関連部分だけを学習するので、大きな PDF/資料でもスケールします。回答の下に検索した根拠（参照箇所）も表示。<b>赤ちゃん級</b>のため回答は不完全なことがあります（重みは Neural Lab、PDF取り込みは PDF抽出 で）。既定の知識は機械工学（機械要素・材料力学・機械設計）の日本語ナレッジベースです。',
    rag:
      'Ask の <b>検索（RAG）</b>段を1ページで体験。入力文書 → チャンク分割 → BM25/TF-IDF 検索 → MMR 再ランク → コンテキスト構築 → ハルシネーション検出。パラメータを変えると全段が連動します。',
    embedding:
      'Ask が使う<b>埋め込み</b>を体験するページ。トークン分解 → ベクトル化 → 類似度（cos / euclid / dot）→ PCA で 2D 可視化。埋め込みは語彙ベースの hashing trick（学習済みニューラルではありません）。',
    neural:
      'Ask の回答生成に使う<b>本物の極小ニューラルネット</b>（埋め込み → tanh 隠れ層 → softmax）を、ブラウザ内で<b>勾配降下学習</b>させて観察するページ。学習データの追加（＝再学習）やハイパーパラメータ調整に加え、最下部に<b>実物のモデルの中身</b>（loss・重み数・次トークン確率・埋め込みの実値・生成）を表示します。Ask と同じインスタンスを共有しています。',
    memory:
      'Ask が回答時に作る<b>要約メモリ</b>の仕組みを体験。記憶（短期 / 長期 / 意味 / エピソード）の閲覧 → 圧縮 → 要約 → 想起（コサイン類似度）。要約・想起はオフラインのヒューリスティックです。',
    grammar:
      'Ask の抽出回答を<b>読める日本語へ正規化</b>する文法エージェント。文を意味単位（SML）へ分解し、自然文へ再構成します（複文は原文保持）。Ask の回答整形そのものを観察できます。',
    agent:
      'Ask を「検索＝調べる行動」とみなした<b>エージェントのループ</b>（観察 → 思考 → 行動）を体験。ゴール入力 → 計画 → ループ実行 → 振り返り → リトライ。LLM 非使用の決定論的シミュレーションです。',
    pdf:
      'PDF から本文テキストを<b>端末内で抽出・クレンジング</b>するページ。全角半角統一（NFKC）・ページ番号やヘッダ/フッタの除去を自動適用し、コピー／.txt 保存／<b>Ask に学習させる</b>へ渡せます。外部送信なし。大きい PDF は重要章に絞ると Ask の精度が上がります。',
    research:
      'PDF を投入して解析するページ。テキスト抽出・ページ図解・TextRank 要約・TF-IDF キーワード・<b>抽出型 QA（RAG連携）</b>・文書内検索。すべて端末内処理で外部送信はありません。',
    evaluation:
      'Ask の<b>検索品質を実測評価</b>（Precision@k / Recall@k）するページ。ラベル付きデータセットで測ります。'
  };
})(window.NSCode);
