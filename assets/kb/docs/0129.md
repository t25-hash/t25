# 1・3・2 プログラミング言語

> **章**: 第1章 情報システム
> **節**: 1・3 計算機ソフトウェア

プログラミング言語とは，問題を解いて解答を得るまでの手順をタプリタである．
インタプリタでは，あらかじめの下準備などがでタの形で保持して編集しながら実行するなど，コンパイラには困難なお，コンパイラとインタプリタを総称して言語処理系（lan-り，プログラムの実行に従って新しく生成されたり破壊されたりすあるという．
複数の変数が同じ名前で参照される場合には，可視で変数のスコープは，プログラムの構造から決定できる場合と，実行時に初めて決定できる場合とがあり，前者を静的なスコープ，後者を動的なスコープと呼ぶ．
CやPASCALなど手続き型の言語の多くは静的なスコープをもち，一方LISPの方言のうちの多くは動的なスコープをもつ．
c .関数引数とFUNARG問題 関数を引数として関数に渡すことのできるプログラミング言語においては，引数として渡す関数の内部の変数の参照をいつの時点の束縛に基づいて行うかが問題となる場合がある．
すなわち，引数とされた関数の内部にある変数の値は，その関数が引数として引き渡された時点の値とするか，その引数が引き渡された先の関数の中で利用された時点の値とするかの選択が可能である．
この二つの選択肢はある束縛の異なった時点での値のこともあり得るし，あるいは全く異なった変数がたまたま同じ名前で参照されているにすぎない場合もある．
この問題を解決するために，関数引数を渡す際にはその関数を評価する環境を関数と組にして渡すことが行われる．
これをクロージャと呼び，この中に関数そのものと関数の中から参照される変数の値を決定するための束縛に関する情報とをセットにして埋め込む．
d.手続型言語 問題を解く過程をステップを追って記述する型のプログラミング言語であり，よく知られるプログラミング言語の多くがこの型に属する．
ここでいう手続き（procedure）とは，解答を得るための手順を順を追って書き下していくことを意味し，原則として書き下された手順を上から順に実行していくが，制御構造と呼ばれる問題を解くための操作そのものではなく，問題を解く手順を制御する構文によって，書き下された手順の中の任意の場所から下へ順に操作するように指示したり，ある条件が成り立ったときだけ行う操作や，何回も繰返して行う操作などを指定できる．
また，同じような手順が毎回少しだけ異なるデータに対して何度も繰返して行われるときには，これを別のプログラムの単位として独立させて扱う．
この別単位のことを手続きもしくはサブルーチンプログラミング言語であり，その歴史は1956年にさかのぼる．
そは1967年にJISとして規格化された後にもFORTRAN77やれた種々の機能を取込んだ．
ここでは現時点で最も広範に用いられンなどから構成されており，大局変数は専用の宣言文によって宣言ータ型や，それらに関する数値演算関数などが豊富に含まれていニズムやポインタ型などは実装されていない．
ラになって他の言語からの影響を受けて構造をもつ文などが書けるリはFORTRAN向けになっているものが多い．
様適合性試験を受ける必要がある．
成する関数ととらえるもので，原理的には変数という概念をもたなの制御の流れという概念もなく，条件分岐や繰返しといった制御構プログラムの全体構造はMAINプログラムと関数やサブルーチうことを目的としたプログラミング言語である．
ALGOL アルゴリズムの記述を目的として設計され.PROLOG 1階述語論理に基づく推論を行うことを目アルゴリズムの教科書などで，特定のプログラミング言語にとられる操作がある．
知識を組合わせて新しい知識を作り上げるルール値を割当てる操作が必要となる．
この，述語論理での値の割当てにに他の変数を割当てるような操作も行う．
例えば，， を変数，に と とがユニファイされたとすると，自動的に も というまた，推論を行ううえでは，数多くの述語表現を組合わせて利用に的確な述語を選択できるとは限らず試行錯誤的な動作をする必要と呼ばれ，処理の過程でその少し前の時点にまで後戻りし，その間を開始できる状態にする必要がある．
PROLOGではバックトラッはバックトラックにかかわる記述をする必要がない．
報世界に存在するあらゆる事柄を表現するモデルであり，内部のデされる概念である．
オブジェクト自身は他のオブジェクトの内部デやり取りすることによって必要な情報を入手しなければならない．
一般にオブジェクトの内部データ構造と適用可能な造内部にいろいろな値をもったオブジェクトをインスタンスと呼.Smalltalk-80 オブジェクト指向型言語として初めてクトどうしのメッセージのやり取りでモデル化したのみならず，すめに，Smalltalk-80ではブロックと呼ばれるプログラムの断片を実行を要求するメッセージを繰返し送信することで，条件実行はあジを送ることで実現する．
に対応した機械語もプログラミング言語の一種であるが，ここではプログラミング言語の実行形態による分類であり，実行前に可能なきないため実行効率は上がらないものの，プログラムの断片をデーな処理も実現可能である．
b.変数のスコープと可視性 プログラミング言語の多くは情るが，この生成から破壊までをその変数のスコープ（scope）と呼ある変数はつねにそれらのうちの一つに限られる．
変数のスコープはALGOLの流れを汲むものである．
特にALGOL68では自動的な参照はがしの機能があり，代入文の左右などでデータの型がポインタ型になっているときには，代入がうまく行われるようにポインタの指しているものの内容が自動的に選択される．
PASCAL プログラミング教育を目的に設計されたプログラミング言語であり，現在でも多くのプログラミングやアルゴリズムの教科書がPASCALで記述されている．
またPASCALのコンパイラをPASCALで書いたものが，初めはPASCALの移植性を高めることを目的として作成されたようであるが，コンパイラの初学者の間でよく参照される．
プログラムの全体構成は，関数などの定義が何重にも入れ子構造になったもので，定義される関数や変数の名前の可視性はこの関数などの入れ子構造によって静的に決定される．
また単純だが強力なデータ型の定義機構をもち，実行時のチェックの厳しさもあって，プログラミングの演習程度の問題であればコンパイルエラーが取れればまず正しい答えが得られるともいわれた．
最初は教育支援に徹して分割コンパイルなどは言語仕様に含まれていなかったが，次第に広く使われるようになっていろいろな拡張を含む方言ができ，最終的にはOSの記述に使われた例もある．
C UNIXオペレーティングシステムの記述用言語として開発されたプログラミング言語である．
どちらかというと専門家に近い人達が今まで機械語で書いていたようなプログラムを，移植性良く記述できるようにするのを目的として設計されているようで，ビット操作などのハードウェアを幾分か意識した演算子をもっている．
ADA アメリカ国防総省が軍事用ソフトウェアの記述用に開発したプログラミング言語である．
大筋ではALGOLやPASCALと同じ流れに属する言語であるが，いろいろなソフトウと呼び，何らかの値を返すときには関数とも呼ぶ．
の後も絶えず拡張が行われ，ISOで国際規格となり，また日本でFORTRAN90などが発表され，後に開発された他の言語に備えらていると思われるFORTRAN77を中心に述べる．
数値計算を目的としているため，いろいろな精度の数値デる．
しかし，あまり非数値演算向きではなく，データ型の定義メカ行うIF文，繰返しを行うDO文などが主であり，最近のコンパイようになった．
きのコンパイラにはFORTRANがよく使われている．
言語で，ALGOL60やALGOL68などが著名である．
述をするのが通例となっている．
すなわち，制御構造を表現するたendで複数の文をくくって文法上では一つの文として扱えるようにユニフィケーションでは変数に定数を割当てるだけでなく，変数ケーションなどのための機能が強化されている．
また，ADAコンパイラを名乗るためにはアメリカ国防総省の仕e .関数型言語 プログラムを入力データから出力データを生い．
また，関数の定義を他の関数の呼出しで行うため，プログラム造もない．
LISP 二分木構造を基本とするリストデータ構造を扱は，プログラム自身や変数の束縛に関する情報などもデータと全くを記述することができることである．
制御構造は任意の点に制御を移すGO TO文や，条件的な実行を工知能の分野などで古くから利用されており，研究の内容や進展にの方言が存在するに至っている．
f .論理型言語 述語論理（predicate logic）に基づく処理をの間に存在する関連を記述する表現である述語を中心として記述される．
的として設計されたプログラミング言語である．
PROLOGによる推論に特徴的なものとして，ユニファイと呼ばには，当然変数を用いることになるが，推論の進行に応じて変数に相当する作業をユニフィケーションと呼ぶ．
を定数としたとき，まず最初に と とがユニファイされ，後値をもつようになる．
し，それらの引数のユニフィケーションを行う必要があるが，つねがある．
オブジェクト指向型言語はこのような計算モデルに忠実に作られ演算操作集合とを定義したオブジェクトをクラスと呼び，データ構ぶ．
世に出たプログラミング言語である．
すべての演算操作をオブジェべての制御構造をもメッセージのやり取りで定義している．
