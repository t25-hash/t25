# 22・6・1 固有値解析法

> **章**: 第22章 振動・音響の計算法
> **節**: 22・6 数値計算の基礎

固有値解析法とは固有値問題を解く方法である。固有値問題(eigenvalue problem)は次の正方行列に対して(標準形)固有値方程式=λ(22・102)を満たすλとの固有ペアλ,を求める問題である。ここでλ2―220機械力学繰返し法逆反復法繰返し法標準、一般実対称中〇最小固有値サブスペース法レイリー・リッツ法に基づく繰返し法標準、一般実対称大〇複数個の(≪)の固有値相似変換に基づく方法ヤコビ法(一般化ヤコビ法)相似変換(対角)標準(一般)実対称小〇個ハウスホルダ-QR-逆反復法相似変換(三重対角)標準対称中×個代数的方法行列式法行列式零点標準、一般任意小〇1個スツルム列法区間内の固有値の数標準、一般実対称中〇1個文献(17)を引用した。演算中の行列のスパース性が保持される場合は〇、そうでない場合は×.いずれも固有値のみ計算、スツルム列法は二分法と組合わせて使用される。を固有値、これに対するを固有ベクトルと呼ぶ。次の正方行列に対して固有ペアは組ある。数値的にこの固有ペアを計算するのが固有値解析であり、その解法にはいろいろある。現在使用実績の多い解法を表22・1に示した。解法には大別して(a)繰返し法、(b)相似変換に基づく方法、(c)代数的方法、などがある。ベクトル繰返し法であるべき乗法、逆反復法、サブスペース法などは大規模な方程式の高次または低次のいくつかの固有値を求める方法であり大規模構造解析に多く用いられる。変換法にはヤコビ法(Jacobi method)、ハウスホルダ-QR-逆反復法などがある。これらは全部の固有値を求めるために使用されるが解析対象とする系のサイズはそれほど大きくない。代数的方法には行列式法、スツルム列法などがある。これらも大きな問題には不向きである。a. べき乗法、逆反復法固有値問題=λを、出発ベクトルを適当にとり、요=요=0,1,2,...(22・103)の形に反復計算していくとベクトル列;,,,...ができる。このベクトルはの絶対値が最大の固有値に対する固有ベクトルに収束していく。この性質を利用して固有ベクトルを計算し、この求められた固有ベクトルをもとにレイリー商=(22・104)などにより固有値を求めるのがべき乗法(power method)である。この方法は収束がそれほど速くない。これを改良したのが逆反復法(inverse iteration method、逆べき乗法)である。以下この方法について説明する。固有値方程式(22・102)においての固有値をλ,λ,...,λ욈とすれば、の固有値は1/λ,1/λ,...,1/λ욈と、-αの固有値はλ-α,λ-α,...,λ욈-αとなる。この性質を利用して、行列=-αにべき乗法を適用して、の固有値の中でαに最も近い固有値を求める。の固有値μ욡=1,2,...,はμ욡=1/λ욡-αとなるから、この中で絶対値が最大になるものはλ욡-αが最小のものであり、これをべき乗法で求めてλ욡=α+1/μ욡としてλ욡を求める。なお固有ベクトルは,に対して、ともに同じになる。[計算手順](1) 出発ベクトルを作る。(2) -αを分解(対称ならコレスキー分解)する。(3) 長さの調整新=/(/を計算して新たにこれをとおくという意味である。以下も同様)。(4) 乗算-α=すなわち=となるを計算する。(5) 収束判定の成分,,...,욈,の成分,,...,욈の比=/, =/,..., 욈/욈のうちの最大値と最小値が十分に接近したら収束したとしてレイリー商=/を=-αの固有値μとする。収束したら(7)へとぶ。(6) 原点移動量の変更αを決定するにはレイリー商などに表22・1代表的な固有値解析法とその特徴分類解法名基礎とする理論問題の形式適用可能な行列問題の規模スパース性の保持得られる固有値と固有ベクトルべき乗数繰返し法標準、一般実対称中〇最大固有値よって現在の-αの固有値の近似値μを求めて新α=α+1/μとする。新=とし(2)に戻る。なおαを変更しないときは(3)に戻る。(7) の固有値の導出=-αの固有値μから、λ=α+1/μによりの固有値を求める。(8) デフレーション固有値が一つ求まったら次の固有値を求めるにはデフレーションを行い行列を作り直す。すなわち新=-μとする。ここでμはいま得られた固有値、はμに対するの固有ベクトル(右固有ベクトル)、はμに対するの固有ベクトル(左固有ベクトル)である。が対称の場合にはとは一致する。なお、これらは==1に正規化してあるものとする。b. サブスペース法構造解析に現れる大規模な次元の(型)固有値方程式-λ=0(22・105)において、より小さな数の固有値と固有ベクトルを計算するのによく用いられる。これは大次元の固有値問題を小さな次元の部分空間の固有値問題に帰着させ、逆反復法と組合わせて解く方法である。最小の固有値から個の固有値を求めるためのサブスペース法(subspace iteration method)は次の手順で行われる。[計算手順](1) より少し大きな数を=min+,2により選び、個の互いに独立な出発ベクトル,,...,욇を仮定し、×の行列=,,...,욇を作る[文献(18)では=8を使用している].(2) 乗算욈욫욇=を計算する。連立一次方程式=を解いて욈욫욇を求める。(3) 部分空間の射影==,=の計算。,は小さなサイズ×の対称行列である。(4) 部分空間の次元の小さな固有値問題=λを(一般化ヤコビ法などにより)解き、その固有ベクトルより×の大きさの行列=... 욇を作る。(5) 元の空間への変換新=を計算する。(6) 収束判定逆反復法の計算手順(5)で示したのと同様の収束判定を行い、対象とする個のベクトルについて収束が完了するまで上記(2)~(6)の間を繰返す。(7) 収束したら、上記(4)の個の固有値のうち個を、(5)の個の固有ベクトルのうち個を採用する。c. ハウスホルダ-QR-逆反復法式(22・102)の形の固有値問題を解くための方法で(1)ハウスホルダ法(Housholdermethod)により実対称行列を相似変換により同じ固有値をもつ三重対角行列に変換する、(2)三重対角行列の固有値をQR 法(QR method)により解く、(3)逆反復法により固有ベクトルを求める、という手順をとる。
