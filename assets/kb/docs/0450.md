# 22・6・1 固有値解析法

> **章**: 第22章 振動・音響の計算法
> **節**: 22・6 数値計算の基礎

固有値解析法とは固有値問題を解く方法である。ここでλ2―220機械力学繰返し法逆反復法繰返し法標準、一般実対称中〇最小固有値サブスペース法レイリー・リッツ法に基づく繰返し法標準、一般実対称大〇複数個の(≪)の固有値相似変換に基づく方法ヤコビ法(一般化ヤコビ法)相似変換(対角)標準(一般)実対称小〇個ハウスホルダ-QR-逆反復法相似変換(三重対角)標準対称中×個代数的方法行列式法行列式零点標準、一般任意小〇1個スツルム列法区間内の固有値の数標準、一般実対称中〇1個文献(17)を引用した。演算中の行列のスパース性が保持される場合は〇、そうでない場合は×.いずれも固有値のみ計算、スツルム列法は二分法と組合わせて使用される。を固有値、これに対するを固有ベクトルと呼ぶ。数値的にこの固有ペアを計算するのが固有値解析であり、その解法にはいろいろある。現在使用実績の多い解法を表22・1に示した。解法には大別して(a)繰返し法、(b)相似変換に基づく方法、(c)代数的方法、などがある。ベクトル繰返し法であるべき乗法、逆反復法、サブスペース法などは大規模な方程式の高次または低次のいくつかの固有値を求める方法であり大規模構造解析に多く用いられる。変換法にはヤコビ法(Jacobi method)、ハウスホルダ-QR-逆反復法などがある。これらは全部の固有値を求めるために使用されるが解析対象とする系のサイズはそれほど大きくない。代数的方法には行列式法、スツルム列法などがある。これらも大きな問題には不向きである。このベクトルはの絶対値が最大の固有値に対する固有ベクトルに収束していく。この方法は収束がそれほど速くない。これを改良したのが逆反復法(inverse iteration method、逆べき乗法)である。以下この方法について説明する。[計算手順](1) 出発ベクトルを作る。以下も同様)。収束したら(7)へとぶ。(6) 原点移動量の変更αを決定するにはレイリー商などに表22・1代表的な固有値解析法とその特徴分類解法名基礎とする理論問題の形式適用可能な行列問題の規模スパース性の保持得られる固有値と固有ベクトルべき乗数繰返し法標準、一般実対称中〇最大固有値よって現在の-αの固有値の近似値μを求めて新α=α+1/μとする。なおαを変更しないときは(3)に戻る。(8) デフレーション固有値が一つ求まったら次の固有値を求めるにはデフレーションを行い行列を作り直す。これは大次元の固有値問題を小さな次元の部分空間の固有値問題に帰着させ、逆反復法と組合わせて解く方法である。最小の固有値から個の固有値を求めるためのサブスペース法(subspace iteration method)は次の手順で行われる。(6) 収束判定逆反復法の計算手順(5)で示したのと同様の収束判定を行い、対象とする個のベクトルについて収束が完了するまで上記(2)~(6)の間を繰返す。(7) 収束したら、上記(4)の個の固有値のうち個を、(5)の個の固有ベクトルのうち個を採用する。c. ハウスホルダ-QR-逆反復法式(22・102)の形の固有値問題を解くための方法で(1)ハウスホルダ法(Housholdermethod)により実対称行列を相似変換により同じ固有値をもつ三重対角行列に変換する、(2)三重対角行列の固有値をQR 法(QR method)により解く、(3)逆反復法により固有ベクトルを求める、という手順をとる。
