# 3・2・3 クラスタ解析

# 3・2・3 クラスタ解析

> **章**: 第3章 ロボティクス/知能システム
> **節**: 3・2 パターン処理

イブラリは広く普及しており，それらを利用することにより，容易
標本が得られているが，それらにラベルが付いていない，いわゆ
に開発することができる．
る教師なし（unsupervised）状態のとき，それらを分類する手法
その他，一般的な記号的パターン処理を行う際には，プログラミ
としてクラスタ解析あるいはクラスタ分析（cluster analysis ）と
ング言語自身にあらかじめ単一化（unification）やバックトラック
呼ばれる手法が適用できる．以下に挙げる手法はどれも非パラメト
機能が備わっているような言語（例えば論理型言語Prolog）を利
リックな手法である．以下の手法のほかにも，ニューラルネットワ
用することにより，より早く開発できる場合もある．
ークの応用であるコホネン（Kohonen）の自己組織化マップ
d.隠れマルコフモデル 隠れマルコフモデル（HMM:hid-
（SOM:self -organizing map）を利用する手法などが挙げられる．
den Markov model ）は時系列パターンの処理方法であり，特に音
a. 平均クラスタリング 平均クラスタリング（ - means
声認識によく用いられる．教師あり学習であり，トレーニングセッ
clustering）は，非階層的クラスタリング手法の一種である．クラ
トから学習してクラスごとにモデルを決定する．観測されたデータ
スタ数 は既知とする．反復的最適化（iterative optimization）
を各モデルに適用し，それらの中からもっともらしいクラスを選択
の一種であり，山登り手法と等価である．パターン数を とし，
することになる．
個の平均ベクトル,,…,を求める． 個のサンプルをそれ
まず，時系列に沿ったデータを考える．時刻 での状態を
ぞれ最近傍の のクラスタとして識別することにして，改めて
とする．長さ の状態の系列を＝1,2,…,で表
を再計算するという手順で処理を行う．すなわち，
す．状態系列を生成するモデルθは，遷移確率＋1
(１) 個の初期クラスタ中心 を適当に決める
＝の集合で表される．例えば，状態系列＝,,,
(２)すべてのデータ を，それぞれが最も近いクラスタ中心
の生成確率は，θ＝となる．音声認識問題で を
のクラスタ に分類する
音素とすると，システムで観測できるのは実は音についての何らか
(３)新たなクラスタの重心をそれぞれ求め，クラスタ中心 と
する
(４)新たなクラスタ中心がすべて以前と同じであれば終了し，そ
うでなければ(２)に戻る
となる． 平均アルゴリズムは単純かつ実用的にうまく機能するた
め，主要なクラスタリング法となっている．また，この 平均ア
ルゴリズムの拡張として，どの標本もすべてのクラスタのメンバシ
ップ度をもつよう拡張されたファジィ 平均（fuzzy - means ）
法も提案されている．
b.階層的クラスタリング 生物における分類学では，目，
属，科，種などと階層的に分けられるように，クラスタ分けを階層
的に行うことは人手によっても一般に行われている．階層的クラス
タリング（hierarchical clustering) の結果は，樹系図（dendro-
gram）で表現される．階層的クラスタリングには，分割的手法
（divisive method）と凝集的手法（agglomerative method）があ
る．分割的手法は，すべての標本を含む一つのクラスタから開始し
て，連続的にクラスタを分割していくトップダウンアプローチであ
る．一方，凝集的手法は， 個の1要素クラスタから開始し，連続
的にクラスタを融合して各レベルでのクラスタを形成していくボト
ムアップアプローチである．ここでは凝集的手法に着目してみる．
凝集的手法の主要手順は， をクラスタ（集合），目標クラスタ数
を とすると，
(１)(初期化） ， ，＝1,…,
(２) －1
(３)クラスタ間距離,が最小となる ， を探す
(４) と を融合
(５) ≠なら(２)へ
で表される．融合候補となるクラスタを決定するのに重要な役割を
果たすクラスタ間距離,の定義に，さまざまな式が提案さ
れている．例えば，最近傍アルゴリズム（nearest neighbor algo-
rithm）は，二つの融合候補のクラスタ ， があるとき，それ
ぞれの標本∈ ，∈の距離－のうち，最も小さい
もの（最も近い標本距離）をクラスタ間の距離とする方法である．
すなわち，クラスタ間距離としては，
,＝min－
が使われることになる．最近傍クラスタ間距離が，与えられた任意
のしきい値を超えた場合に処理を停止するものは，単連結アルゴリ
ズム（single-linkage algorithm）と呼ばれる．ノイズまたはデー
タ点のわずかな位置変化に対して非常に敏感で，予期しない細長い
クラスタを形成することがある．この現象はチェーン効果と呼ばれ
る．
また，最遠近傍アルゴリズム（furthest neighbor algorithm）
は，－のうち，最も大きいもの（最も遠い標本距離）をク
ラスタ間の距離とする方法である．すなわち，クラスタ間距離とし
ては，
,＝max－
が使われることになる．最近傍クラスタ間距離が，与えらえた任意
のしきい値を超えた場合に処理を停止するものは，完全結合アルゴ
リズム（complete linkage algorithm）と呼ばれる．本来のクラス
タが密集していてその大きさもほぼ同等である場合に有効である．
しかし，細長いクラスタの場合はうまくいかない．
ウォード法（Ward method）は，融合後のクラスタにおける分
散，つまり
＝∑∑－
を最小とするようにクラスタを融合させていく方法である．これは
すなわち，距離として
,＝ ＋－
ただし，
＝
1∑
∈
を考え，これが最小となるクラスタを融合させていく方法である．
比較的効率的とされているが，大きさが小さいクラスタを作りやす
いといわれている．
どの方法も一長一短あり，一般的なデータセットに対する優劣を
つけることはできない．これらのほかにも，重心法（centroid
method，＝－や群平均法（group average
method），
＝ 1 ∑ ∑－′
やメジアン法などさまざまな方法が提案されている．また，これら
の距離を，四つのパラメータで統一的に表す式がランス（Lance ）
とウィリアムス（Williams ）によって提案されている．これは距
離を再帰的に求めることができる式であり，定義式を直接計算する
よりも効率的に計算できる．この，ランス・ウィリアムスの式
（Lance - Williams updating formula）では，クラスタ とクラス
タ が融合してできたクラスタ と，クラスタ との間の距離を，
＝α＋α＋β＋γ－
で表す．ただし，各手法でのα ，α ，β，γの値は表3･1に示す．
