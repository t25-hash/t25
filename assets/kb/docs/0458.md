# 23・2・1 連続および離散フーリエ変換

> **章**: 第23章 計測と信号処理
> **節**: 23・2 データ処理

連続フーリエ変換(continuous Fourier transform)は、無限大の時間軸の関数を無限大の周波数軸の関数に、またはその逆に変換する方法である。現実の世界の信号を対象とする場合は、積分は数値的(離散的)に行われ、時間軸および周波数軸も有限でなければならない。連続フーリエ変換に対応する離散的な変換は離散フーリエ変換(DFT : discrete Fourier transform)と呼ばれる。離散フーリエ変換をより少ない演算量で実行するアルゴリズムが1965年にクーリーとチューキーにより高速フーリエ変換(FFT : fastFourier transform)として考案された。ここでは、FFT のアルゴリズムの詳細の説明は省略し、それを正しく有効に利用するために知っておくべき、離散フーリエ変換の基本的な性質について述べる。a. フーリエ変換の原理連続フーリエ変換対は次式で表される。例えば周波数、初期位相0の信号は次のように表すことができる。これが式(23・10)の解釈である。一方、離散フーリエ変換対は次式で表される。離散フーリエ変換および逆変換は数列の各要素に複素数の重みを掛けて線形加算し、同数の要素からなる別の数列に変換する操作である。離散フーリエ変換と連続フーリエ変換との対応を調べるため、=8の場合について、インパルスと1波の余弦波および正弦波をDFT した結果を図23・14に示す。余弦波の場合には=1および7のところで同じ大きさの実数のみのスペクトルをもち、それ以外のに対しては零となる。正弦波の場合は虚数成分のみとなる。これらは、式(23・10)および式(23・11)の連続フーリエ変換に対応する結果を与えることがわかる。=8の場合について、どのようにして図23・14の結果が得られ(a) インパルス(b) 余弦波(c) 正弦波るかを説明する。図23・15は振幅1の余弦波1波を、=8で離散化した場合に、式(23・14)の右辺の各項を複素平面でベクトルで表したものである。実際の信号は正弦波と余弦波の組合せであるので、は純実数または純虚数とはならず、複素数の値をもつ。>2の場合は○印が反時計回りとなり、負の周波数成分を抽出することになる。実用的には=2に対応する高周波数成分は、サンプリング定理を満たすように低域通過フィルタで除去するので問題にならない。連続、離散フーリエ変換ともに、以下の基本的な性質をもっている。ここで、記号は連続関数のものを使用している。離散化しただけでが周期関数になるわけではないが、フーリエ変換対をあてはめる場合には、そのように解釈することが必要となる。図23・16の左上は200Hz の信号を=12.8kHz,=1024でサンプリングした時間波形の例である。時間窓=80msの中に16個(整数個)の波があるために、波形が連続している。ところが206.25Hz の波形では、図23・16の右上に示すように、時間窓内の波の数が16.5個となり離散信号の最初と最後で不連続になる。この場合にはパワースペクトルは図23・16の右下のように広がりをもつ。そのような目的のためにハニング窓(Han-ning window)と呼ばれる関数が最もよく用いられる。この窓関数を200Hz および206.25Hzの信号に適用した結果を図23・17に示す。206.25Hz の場合でも多少のスペクトルの広がりが見られるが、図23・16の場合よりはるかに改善される。このように、ノイズの分析などの場合のように、サンプリングされた波形の最初と最後の部分が連続的につながる補償がないときはハニング窓を用いることが多い。多くの窓関数が提案されており、目的に応じて使い分けることが必要である。c. 折返しひずみ(エリアジング)サンプリングする際に考慮すべき重要な定理がシャノンの標本化(サンプリング)定理(sampling theorem)である。サンプリングにより信号を正しく(ゆがませずに)取込むには、連続信号に含まれている最大周波数の2倍以上のサンプリング周波数を用いることが必要である。この条件を満たさない場合には、サンプリングの半分の周波数を超える周波数成分がそれ以下の帯域に折返すように含まれるエリアジング(aliasing)と呼ばれる現象が発生し、正しい分析が行えなくなる。エリアジングによって発生する余分な成分は折返しひずみと呼ばれる。そのため、アナログ信号をサンプリングする際には必ず標本化定理を満たすようにすることが必要である。図23・18は多くの高調波を含む矩形波をサンプリングした例である。エリアジングを防止する低域通過フィルタを用いることによって、矩形波の正しい周波図23・16 窓長に整数個(左)と非整数個(右)の正弦波が入るときの時間波形とパワースペクトル2―229数分析が行えるようになる。d. 高速フーリエ変換式(23・14)および式(23・15)の計算は任意のサンプリング数について可能である。それに要する複素乗算の回数はである。=2(:正の整数)の場合には式(23・14)、(23・15)の演算を高速に行う高速フーリエ変換(FFT)と呼ばれる算法が存在する。サンプリング点数が増すに従って計算時間が問題となるので、FFT の効果はさらに増すことになる。関連するパラメータ間の関係は以下のようになる。
