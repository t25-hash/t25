# 10・2・1 座標（coordinates )

> **章**: 第10章 幾何
> **節**: 10・2 平面幾何（plane geometry)

それぞれの直線に平行に測ったPまでの距離 および を与えれ本の直線を座標軸（ 軸および 軸)，交点を座標原点という．
普b.極座標（polar coordinates) 直角座標の代わりに極座標までの距離（動径)，θはOPがOを通る定直線（これを原線といる．
を結ぶ線分の長さを，PPが 軸となす角をαとすればが動径 となす角をψとすれば＝tanαをPPの方向係数という．
位置を定めるには，平面上に2本の相交わる直線を引き，交点からばよい．
をPの座標と呼び，これをP,と記す．
また2通に用いるのは座標軸が直交する場合で，これを直角座標という．
θを用いることもある．
は定点O（これを極という）からPう）となす角（偏角）である．
新座標軸の原点の旧座標軸に関する座標を,ば次の関係がある．
標,θを使うと＝θと表される．
