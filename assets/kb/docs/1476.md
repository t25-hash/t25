# 6・3・2 メトロポリス法の原理

> **章**: 第6章 固体の離散系シミュレーション
> **節**: 6・3 メトロポリス法

現在の状態から、乱数によって次の状態をある確率で作る。その確率をうまく選ぶことで、平衡状態を作ろうというのがメトロポリス法である。最も単純な例として、状態1と状態2をある確率で行き来する状況を考える。ステップめにそれぞれの状態をとる確率を,とすると、+1ステップめの状態は、次のように表すことができる。+1=+1(6・40)ここで、右辺の2×2正方行列の各要素욡욢は、1ステップで状態から状態に遷移する確率である。当然、+=1,+=1でなければならない。この確率変化を、ベクトル表現で書くと+1=(6・41)となる。は遷移行列(transition matrix)と呼ばれる。このように、次の状態(+1ステップめ)が現在の状態(ステップめ)だけに依存し、過去にはよらないような確率過程を、マルコフ過程6―77(Markov process)という。式(6・41)を繰返し使うことにより=-1=-2=...=욗0(6・42)のように、ステップめの状態をと初期状態0で表すことができる。ステップ数が大きいときには、どんな初期状態から出発しても定常状態(平衡状態)に近づくことが、経験的に期待できる。これは、についていくつかの仮定を設けることで数学的に証明することもできる。この平衡状態を∞とすると、これ以上の変化が起こらないことから∞=∞(6・43)が成り立つ。すなわち、∞はの固有値1に対応する固有ベクトルである。平衡状態を具体的に求めてみる。=1-1-(6・44)として固有値1の固有ベクトルを求めると、∞∞=(6・45)であることがわかる。逆に、平衡状態∞がわかっているとき、式(6・45)となるようにを選べば、任意の初期状態から出発して、目的の平衡状態を得ることができる。ここで、各状態のエネルギーを,とし、<と仮定する。絶対温度の与えられたカノニカルアンサンブルの平衡状態は、∞∞=exp --(6・46)であるから、式(6・45)と比較することにより=exp --≡exp -=1(6・47)のように遷移確率を選ぶと、ボルツマン分布の平衡状態が得られることになる。式(6・45)からわかるように、との比だけが重要なので、,の選び方は式(6・47)以外にも可能であるが、式(6・47)のように選ぶ方法をメトロポリス法という。以上の原理は、ただちに個の状態がある場合に拡張できる。すなわち、次元の状態ベクトルと遷移行列に対して式(6・41)が成り立つとき、適当な条件(エルゴード条件、ergodiccondition)のもとでは、∞で唯一の平衡状態∞に収束することが知られている。各状態のエネルギーを욡とするとき、욡<욢である状態,について、遷移行列の各要素(つまり遷移確率)を次のように選ぶと、この平衡状態がボルツマン分布に従うことが証明できる。욡욢=욢욡=×exp -욢-욡(6・48)ここで、は適当な正定数である。カノニカルアンサンブルにおけるメトロポリス法の実際の手順は、次のようなものである。(1) 初期状態としては何を選んでもよい。ただし、平衡状態になるべく近いところから出発するほうが収束は早い。(2) いまの状態から、一様乱数(uniform random number)によって次の状態の候補を作る。(3) いまの状態とのエネルギー差욢-욡≡を計算する。(4) 式(6・48)に従って遷移させる。具体的には、・もし、≦0ならを次の状態として採用する。・>0なら確率exp-でを採用する。が採用されなかった場合は、元の状態のままとする。(5) 十分な回数だけ、手順(2)~(4)を繰返す。このようにして、十分な状態遷移の後に、温度の熱平衡状態が実現されることになる。エネルギーや原子の配置などをモニタし6―78計算力学て、十分に平衡状態(定常状態)に近づいたと判断した後は、それ以降に生成された状態を用いて、式(6・38)に従い集団平均をとることができる。なお、手順(4)において、確率exp-でを次の状態に採用するためには、0≦≦1の一様乱数を計算機で生成し、≦exp-の場合のみを採用すればよい。原子・分子系では、粒子の質量中心や分子配向を連続変数として扱うのがふつうである。このように状態が離散的でない場合も、上述の原理や手順は全く同じである。
