# 13・2・3 アプリケーションレベルにおける並列処理

> **章**: 第13章 シミュレーション支援法
> **節**: 13・2 ベクトル・パラレル

a.並列化の対象 プログラムを作成するときに，計算処理の並列処理を司る記号は見出せない．
較，S：指数部合わせ，A：加算，N：正規化，W：データ出(ｃ) 共有・分散メモリ型している．
したがって，開発されるアプリケーションプログラムは，最初は逐次処理のプログラムであるといえる．
プログラムが実際に計算機上で稼働するには，コンパイラによって，それぞれのハードウェア環境に適応したロードモジュールに変換されなければならない．
しかしながら，現在，並列処理に関していえば，コンパイラが並列ハードウェア環境の違いや並列処理をFortranやCなどの高級言語から完全に隠ぺいし，1プロセッサ上で開発された逐次処理プログラムから，並列計算機環境でも効率的に実行できるようなロードモジュールを生成するということはない．
すなわち，アプリケーションプログラムのレベルにおいて，何らかの並列化を意識した手続きを加えなければならない．
並列化を意識しなければならない対象には，ワーク（あるいはタスク）とデータがある．
並列プログラミングとは，ワークやデータをどのように分散し，どのように同時実行するかをプログラムの中で指示することにほかならない．
これらはそれぞれ，ワークシェアリング，データシェアリングとも呼ばれる．
ワークを分散する形態には，複数のプロセッサでDOループを分担して処理する，複数のプロセッサで異なるプログラムを同時に実行する，などがある．
また，分散配置されたノードのメモリ量に応じてデータを分散し，ノードごとに異なるデータに対して同じ演算を施す，などがある．
実際にこうしたワークシェアリングやデータシェアリングをプログラムで指示するには，後述のように，メモリ分散方式の違いやプロセッサタイプの違いなどのハードウェア環境に応じて，いくつかの方式がある．
例えば，並列化を支援する通信ライブラリ（MPIやPVM などのメッセージパッシングライブラリ）を選択してそれらをアプリケーションプログラムとリンクさせる，アプリケーションプログラムの中に並列化の指示文（OpenMP など）やベクトル化の指示文を挿入する，並列言語（HPF など）を用いる，などである．
ッサタイプの違いなどのハードウェア環境に応じて，並列プログラ配列要素に対する演算を複数のプロセッサでワークシェアリングす機での並列処理スタイルを指すことが多い．
共有メモリではプロセは，プロセッサごとの局所変数とプロセッサ間で共通な大域変数と演算など）においては，この点に注意する必要がある．
なデータについてはメモリの物理的な分散に応じて演算対象のデーサ（あるいはノード）でそれらを分担して計算実行する．
どのプロ算対象のデータはそれぞれに異なるというプログラミングスタイル多い．
なるプログラムが実行される．
例えば，複数の物理事象の連成解析の解析が異なるプロセッサ（あるいはノード）上で実行される．
こうした並列プログラムは，以下のような方法で作成されDOループが分割され複数のスレッドによって同時実行される)Fortran) がある．
OpenMPが共有メモリ向け並列化インタフとしている．
MPI COMM SIZE コミュニケータの立上げ．
MPI FINALIZE MPIの終了．
MPI BARRIER 各プロセスの同期．
MPI BCAST 一つの送信元から全プロセスにメッセーMPI ALLREDUCE すべてのプロセスからメッセージを受信に送信する．
MPI ISEND,MPI IRECV 1対1非ブロッキング通信（送信，受信)．
メッセージパッシングライブラリ（message passinglibrary) メッセージパッシングライブラリは，分散されたメモd.計算機システムに対応した並列化戦略 複数のプロセッサメモリ型，分散メモリ型，共有・分散メモリ型の三つの方式があトルプロセッサの違いがある．
以下に，計算機環境に対応して，アる．
なお，こうした並列処理やベクトル処理を効率的に行うには，判断し，必要に応じてデータを分割したり，演算順序を並べ替える.共有メモリ型 個々の計算機システムのコンパイラがサプログラムの中に挿入し，おもにDOループに対する並列化を行いて並列化を行う．
ブラリを，逐次プログラムの中からコールすることで並列化を行.共有・分散メモリ型 メモリが物理的に分散しているノ並列化についてはⅰ項の共有メモリ型の戦略を組合わせる．
もしくッシングライブラリを用いる．
ッサの場合，コンパイル時にベクトル化のオプションを指定する．
性がある場合にはベクトル処理してしまうと誤った結果となるた順序を並べ替えるなどの工夫によりループ依存性をなくし，ベクトような場合には，指示文を挿入して強制的にベクトル化する．
リ間でネットワークを介してデータを送受信したり，プロセスの起動や同期などの制御を行う機能をサポートしたライブラリ群である．
FortranやCなどのライブラリとして，逐次プログラムの中からコールすることで並列計算が可能となる．
ほぼデファクトスタンダードとなっているメッセージパッシングライブラリに，MPIており，その実装系であるmpichは，ほとんどのメーカのプロセッサに対応したものが準備されている．
また，商用のはん用並列計算機には，それぞれのアーキテクチャに最適化されたMPIが実装されている．
MPIは共有メモリ，分散メモリ，共有・分散メモリのどの形態の計算機システムにおいても用いられる．
表13･1にMPIのサポートするいくつかの機能を示す．
並列化指示文 用いる計算機システムのコンパイラが自動並列化の機能を有する場合には，それを利用することができる．
しかしながら，すべての逐次プログラムが完全に自動的に並列化されることはなく，多くの場合，ワークシェアリングやデータシェアリングのための指示文（directive，ディレクティブ）をプログラムの中に挿入する．
おもに，共有メモリの並列計算機システムにおいて，このような並列化が行われる．
共有・分散メモリのノード内並列化も同様である．
指示文はプログラムの中で見掛け上コメント行のように書かれるが，コンパイル時にオプションを指定することによって，その指示文が解釈されるようになる．
システムによって指示文の形式が異なると，プログラムのポータビリティ（可搬性，移植性）が失われるため，これを統一化した規格がOpenMP である．
図13･22にOpenMPの簡単な例を示す．
なお，要素プロセッサがベクトルプロセッサの場合，ベクトルベクトル計算に関する指定も指示文の挿入によって行われる．
並列言語 Fortran90にいくつかの指示文を加え，ムのタイプは以下の3種類に分類される．
データシェアリングを意識しなくてよい共有メモリ型並列計算ッサ間の通信は必要ないが，用いる並列化支援ライブラリによってを使い分ける必要がある．
リダクション演算（配列要素の積や内積型並列計算機あるいは分散・共有メモリ型並列計算機では，大規模タをあらかじめ分散させ（データシェアリング)，複数のプロセッセッサ（あるいはノード）でも同じプログラムが実行されるが，演である．
データ並列とSPMDの二つの用語を同義に用いることもではプロセッサ（あるいはノード）ごとに異なるデータに対し，異においては，境界条件を交換しながら，それぞれの個別の物理事象はデータ並列，SPMD，MPMDのようなタイプに分類できると述る．
MPI INIT MPIの起動．
MPI COMM RANK コミュニケータ内のプロセスの認識．
MPI WAITALL 各プロセスの同期．
し，それらの算術計算結果を全プロセスMPI SEND,MPI RECV 1対1ブロッキング通信（送信，受信)．
MPI WAITとともに用いる．
c.並列プログラミング方法 b項において，並列プログラム図13･22 OpenMPの記述例( !
$OMPで始まる文がOpenMPの指示文．
てコンパイラが自動的に並列化を行う．
にメモリをどう割当てるかによって，並列計算機システムには共有る．
また，要素プロセッサのレベルでは，スカラプロセッサ，ベクプリケーションプログラムのレベルにおける並列化戦略をまとめあらかじめプログラムやデータを分析して処理の逐次性や並列性を必要があることに注意しなければならない．
ポートする自動並列化のための指示文，もしくはOpenMPを逐次う．
もしくは，MPIなどのメッセージパッシングライブラリを用.分散メモリ型 MPIなどのメッセージパッシングライう．
もしくは，HPFを用いる．
ード間の並列化についてはⅱ項の分散メモリ型の戦略，ノード内のは，ノード間，ノード内の両方についてMPIなどのメッセージパ.ベクトルプロセッサ 要素プロセッサがベクトルプロセベクトル化は再内側のDOループに適用される．
演算にループ依存め，コンパイラはベクトル化しない．
しかしながら，配列データのル処理しても正しい結果が得られるようにできる場合がある．
このお，当然のことながら，指示文の仕様はコンパイラに依存する．
