# 13・2・3 アプリケーションレベルにおける並列処理

# 13・2・3 アプリケーションレベルにおける並列処理

> **章**: 第13章 シミュレーション支援法
> **節**: 13・2 ベクトル・パラレル

a.並列化の対象 プログラムを作成するときに，計算処理の
流れをフローチャートで整理することが多いが，その標記法の中に
並列処理を司る記号は見出せない．このことは，プログラムの基と
なる計算アルゴリズムは，通常，逐次処理のみから成ることを意味
(ａ) 共有メモリ型
(ｂ) 分散メモリ型
(ａ)ベクトル処理
(ｂ)スカラ処理
図13･ 20 加算演算［C(K)＝A(K)＋B(K)のDOループ］におけるベク
トル処理とスカラ処理の概念（R：データ入力，C：指数部比
較，S：指数部合わせ，A：加算，N：正規化，W：データ出
力）
(ｃ) 共有・分散メモリ型
図13･21並列計算機におけるネットワーク，メモリ，プロセッサの構成
している．したがって，開発されるアプリケーションプログラム
リ間でネットワークを介してデータを送受信したり，プロセスの起
は，最初は逐次処理のプログラムであるといえる．プログラムが実
動や同期などの制御を行う機能をサポートしたライブラリ群であ
際に計算機上で稼働するには，コンパイラによって，それぞれのハ
る．FortranやCなどのライブラリとして，逐次プログラムの中
ードウェア環境に適応したロードモジュールに変換されなければな
からコールすることで並列計算が可能となる．ほぼデファクトスタ
らない．しかしながら，現在，並列処理に関していえば，コンパイ
ンダードとなっているメッセージパッシングライブラリに，MPI
ラが並列ハードウェア環境の違いや並列処理をFortranやCなど
（message passing interface) がある．MPIは単に規格を指し
の高級言語から完全に隠ぺいし，1プロセッサ上で開発された逐次
ており，その実装系であるmpichは，ほとんどのメーカのプロセ
処理プログラムから，並列計算機環境でも効率的に実行できるよう
ッサに対応したものが準備されている．また，商用のはん用並列計
なロードモジュールを生成するということはない．すなわち，アプ
算機には，それぞれのアーキテクチャに最適化されたMPIが実装
リケーションプログラムのレベルにおいて，何らかの並列化を意識
されている．MPIは共有メモリ，分散メモリ，共有・分散メモリ
した手続きを加えなければならない．
のどの形態の計算機システムにおいても用いられる．表13･1に
並列化を意識しなければならない対象には，ワーク（あるいはタ
MPIのサポートするいくつかの機能を示す．
スク）とデータがある．並列プログラミングとは，ワークやデータ
.並列化指示文 用いる計算機システムのコンパイラが自
をどのように分散し，どのように同時実行するかをプログラムの中
動並列化の機能を有する場合には，それを利用することができる．
で指示することにほかならない．これらはそれぞれ，ワークシェア
しかしながら，すべての逐次プログラムが完全に自動的に並列化さ
リング，データシェアリングとも呼ばれる．ワークを分散する形態
れることはなく，多くの場合，ワークシェアリングやデータシェア
には，複数のプロセッサでDOループを分担して処理する，複数の
リングのための指示文（directive，ディレクティブ）をプログラ
プロセッサで異なるプログラムを同時に実行する，などがある．ま
ムの中に挿入する．おもに，共有メモリの並列計算機システムにお
た，分散配置されたノードのメモリ量に応じてデータを分散し，ノ
いて，このような並列化が行われる．共有・分散メモリのノード内
ードごとに異なるデータに対して同じ演算を施す，などがある．実
並列化も同様である．指示文はプログラムの中で見掛け上コメント
際にこうしたワークシェアリングやデータシェアリングをプログラ
行のように書かれるが，コンパイル時にオプションを指定すること
ムで指示するには，後述のように，メモリ分散方式の違いやプロセ
によって，その指示文が解釈されるようになる．システムによって
ッサタイプの違いなどのハードウェア環境に応じて，いくつかの方
指示文の形式が異なると，プログラムのポータビリティ（可搬性，
式がある．例えば，並列化を支援する通信ライブラリ（MPI
移植性）が失われるため，これを統一化した規格がOpenMP で
やPVM などのメッセージパッシングライブラリ）を選択して
ある．図13･22にOpenMPの簡単な例を示す．
それらをアプリケーションプログラムとリンクさせる，アプリケー
なお，要素プロセッサがベクトルプロセッサの場合，ベクトルベ
ションプログラムの中に並列化の指示文（OpenMP など）やベ
クトル計算に関する指定も指示文の挿入によって行われる．
クトル化の指示文を挿入する，並列言語（HPF など）を用い
.並列言語 Fortran90にいくつかの指示文を加え，
る，などである．
Fortranの拡張として定義された言語にHPF（high performance
ッサタイプの違いなどのハードウェア環境に応じて，並列プログラ
ムのタイプは以下の3種類に分類される．
配列要素に対する演算を複数のプロセッサでワークシェアリングす
る．データシェアリングを意識しなくてよい共有メモリ型並列計算
機での並列処理スタイルを指すことが多い．共有メモリではプロセ
ッサ間の通信は必要ないが，用いる並列化支援ライブラリによって
は，プロセッサごとの局所変数とプロセッサ間で共通な大域変数と
を使い分ける必要がある．リダクション演算（配列要素の積や内積
演算など）においては，この点に注意する必要がある．
型並列計算機あるいは分散・共有メモリ型並列計算機では，大規模
なデータについてはメモリの物理的な分散に応じて演算対象のデー
タをあらかじめ分散させ（データシェアリング)，複数のプロセッ
サ（あるいはノード）でそれらを分担して計算実行する．どのプロ
セッサ（あるいはノード）でも同じプログラムが実行されるが，演
算対象のデータはそれぞれに異なるというプログラミングスタイル
である．データ並列とSPMDの二つの用語を同義に用いることも
多い．
ではプロセッサ（あるいはノード）ごとに異なるデータに対し，異
なるプログラムが実行される．例えば，複数の物理事象の連成解析
においては，境界条件を交換しながら，それぞれの個別の物理事象
の解析が異なるプロセッサ（あるいはノード）上で実行される．
はデータ並列，SPMD，MPMDのようなタイプに分類できると述
べた．こうした並列プログラムは，以下のような方法で作成され
る．
DOループが分割され複数のスレッドによって同時実行される)
b.並列プログラムのタイプ メモリ分散方式の違いやプロセ
Fortran) がある．OpenMPが共有メモリ向け並列化インタフ
ェースであるのに対し，HPFは分散メモリにおける並列化を対象
としている．HPFでは，指示文によってデータシェアリングを指
.データ並列（data parallel ) DOループを分割して，
定すれば，残るワークシェアリングは，分散メモリ間の通信を含め
表13･1 MPIのサポートする機能の例
関数名 機能
MPI INIT MPIの起動．
MPI COMM SIZE コミュニケータの立上げ．
MPI COMM RANK コミュニケータ内のプロセスの認識．
MPI FINALIZE MPIの終了．
.SPMD（single program multiple data) 分散メモリ
MPI BARRIER 各プロセスの同期．
MPI WAITALL 各プロセスの同期．
MPI BCAST 一つの送信元から全プロセスにメッセー
ジを送信する．
MPI ALLREDUCE すべてのプロセスからメッセージを受信
し，それらの算術計算結果を全プロセス
に送信する．
MPI SEND,MPI RECV 1対1ブロッキング通信（送信，受信)．
MPI ISEND,MPI IRECV 1対1非ブロッキング通信（送信，受信)．
MPI WAITとともに用いる．
.MPMD（multiple program multiple data) MPMD
c.並列プログラミング方法 b項において，並列プログラム
.メッセージパッシングライブラリ（message passing
図13･22 OpenMPの記述例( !$OMPで始まる文がOpenMPの指示文．
library) メッセージパッシングライブラリは，分散されたメモ
てコンパイラが自動的に並列化を行う．
d.計算機システムに対応した並列化戦略 複数のプロセッサ
にメモリをどう割当てるかによって，並列計算機システムには共有
メモリ型，分散メモリ型，共有・分散メモリ型の三つの方式があ
る．また，要素プロセッサのレベルでは，スカラプロセッサ，ベク
トルプロセッサの違いがある．以下に，計算機環境に対応して，ア
プリケーションプログラムのレベルにおける並列化戦略をまとめ
る．なお，こうした並列処理やベクトル処理を効率的に行うには，
あらかじめプログラムやデータを分析して処理の逐次性や並列性を
判断し，必要に応じてデータを分割したり，演算順序を並べ替える
必要があることに注意しなければならない．
.共有メモリ型 個々の計算機システムのコンパイラがサ
ポートする自動並列化のための指示文，もしくはOpenMPを逐次
プログラムの中に挿入し，おもにDOループに対する並列化を行
う．もしくは，MPIなどのメッセージパッシングライブラリを用
いて並列化を行う．
.分散メモリ型 MPIなどのメッセージパッシングライ
ブラリを，逐次プログラムの中からコールすることで並列化を行
う．もしくは，HPFを用いる．
.共有・分散メモリ型 メモリが物理的に分散しているノ
ード間の並列化についてはⅱ項の分散メモリ型の戦略，ノード内の
並列化についてはⅰ項の共有メモリ型の戦略を組合わせる．もしく
は，ノード間，ノード内の両方についてMPIなどのメッセージパ
ッシングライブラリを用いる．
.ベクトルプロセッサ 要素プロセッサがベクトルプロセ
ッサの場合，コンパイル時にベクトル化のオプションを指定する．
ベクトル化は再内側のDOループに適用される．演算にループ依存
性がある場合にはベクトル処理してしまうと誤った結果となるた
め，コンパイラはベクトル化しない．しかしながら，配列データの
順序を並べ替えるなどの工夫によりループ依存性をなくし，ベクト
ル処理しても正しい結果が得られるようにできる場合がある．この
ような場合には，指示文を挿入して強制的にベクトル化する．な
お，当然のことながら，指示文の仕様はコンパイラに依存する．
