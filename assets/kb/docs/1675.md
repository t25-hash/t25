# 1・1・4 入出力装置と入出力インタフェース

> **章**: 第1章 情報システム
> **節**: 1・9 )。共有メモリアーキテクチャはCPU がメモリ上のデータに簡

ハードディスクドライブ（HDD:hard disk drive）は，二次記
憶装置として用いられる外部記憶装置であり，大容量で高速なアク
セスが可能である．磁性体を塗った高速の円盤にデータを読み書き
するもので，ランダムにアクセスできる．1990年代初頭で数十メ
ガバイトであった容量は現在，数十ギガバイトとなり，年々大容量
化している．データを保存する装置としては，従来，フロッピーデ
ィスクや磁気テープドライブなどが使われていたが，最近ではCD
（compact disk，compact disc）やフラッシュメモリ，DVD（dig-
ital versatile disk）などが使われるようになってきた．
人間とのインタフェースとなる外部入出力装置としては，キーボ
ードやマウス，ディスプレイがあるが，スキャナやタッチパッド，
ジョイスティックなどを含めて多様化している．
インタネットの発達に伴い，ネットワーク装置は最も重要な入出
力装置の一つになっている．従来はさまざまなネットワークがあっ
たが，現在ではイーサネット（Ethernet）が標準的に使われるよ
うになっている．1990年代に10Mbps （bit per second，1秒間で
転送するビット数）だった伝送速度は100Mbpsと高速化し，1
Gbpsの転送速度をもつギガビットイーサネットが普及しつつあ
る．
CPUと外部の入出力装置でデータのやり取りを行う部分が入出
力インタフェースである．CPU自体は外部とのデータのやり取り
にバスを通してデータをやり取りするが，入出力インタフェースは
外部装置に合わせてバスの信号を変換する．
CPUが入出力インタフェースとの間でデータをやり取りする場
合には入出力命令を用いる．入出力装置からメモリにデータを読込
む場合，入出力命令を用いてデータをCPUに読込み，これをメモ
リに書き込むという動作を繰返す．しかし，この場合，入出力のた
めにCPUが介在するために，データ量が多くなるとCPUが占有
されてしまうという問題点がある．そのため，大量のデータを入出
力装置とメモリの間でやり取りする場合にはDMA（direct mem-
ory access）という方法を用いる．DMAはCPUを介さずに入出
力インタフェースとメモリ間でデータ転送を行うもので，これを制
御する回路をDMAコントローラという．CPUは入出力命令で
DMAコントローラに外部インタフェースと転送先のメモリのアド
レス，データ量などをセットすることにより，DMAコントローラ
に自動的に転送を行わせることができる．
外部入出力装置のインタフェースにはさまざまなものがあるが，
大きく分けて，シリアルインタフェースとパラレルインタフェース
の二つがある．
シリアルインタフェースとは，1本の信号線で1ビットずつデー
タを伝送するものである．パーソナルコンピュータでモデムなどの
接続に使われるRS- 232Cは最も普及しているシリアルインタフェ
ースで，最大のデータ転送速度は115.2kbpsである．最近では，
さらに高速なUSB（universal serial bus）やIEEE1394（Fire
Wire）が使われるようになってきた．当初，キーボードやマウス
などの比較的低速の機器を接続することを想定してUSB1.1（最
大データ転送速度12Mbps）が策定された．USB1.1が広く普及
したため，CDやHDDも接続されることが多くなり，高速データ
転送の需要が高まってUSB2.0（最高データ転送速度480Mbps）
が策定された．これにより，二次記憶装置を接続しても十分な性能
を発揮できるようになっている．なお，USB2.0はUSB1.1の上
位互換が確保されており，USB1.1の機器もそのまま接続できる．
IEEE1394は高速なシリアル転送を実現する規格で，最大400
Mbpsまでの規格が決められている．機器を数珠つなぎにする，い
わゆるディジチェーン接続することが可能で，複数の機器を同時に
接続することができる．この規格はディジタルビデオの外部接続に
採用されている．
このほかに，シリアルインタフェースとしては，赤外線で外部機
器と接続するIrDA（Infrared Data Association）がある．
パラレルインタフェースは，複数のデータ線で情報を一度に転送
する方式である．二次記憶装置として使われるディスク装置など大
量のデータの転送に使われる．PCなどで内蔵のディスクドライブ
の接続に使われているのがIDE（integrated drive electronics）イ
ンタフェースである．製造コストが安く，高速であるために広く普
及している．IDEを正式に標準化したものがATA規格である．
接続するためのパラレルインタフェースの一つである．当初の
SCSIは，8本の信号線で8ビットデータ転送を行い最大転送速度
は5MB（メガバイト)/ sであったが，その後，転送速度を向上さ
せてfast SCSIでは10MB/s，さらにultra SCSIでは20MB/sの
転送速度が可能になった．同時に，8ビットのデータ幅を16ビッ
図っている．ultra wide SCSIでは，40MB/ sの転送速度が可能で
ある．
中心とした団体で策定されたインタフェースの規格で，多くのPC
で使われている．データ転送幅は32ビットで，転送周波数は33
MHzのため，データ転送速度は133MB/sである．複数の外部
HDDがある場合などはデータ転送速度が足りなくなりつつある．
そのため，さらにサーバなどに用途に高速化した64ビット，周波
数が66MHzのPCI規格では，最大533MB/ sの転送速度となる．
その次の規格であるPCI - Xでは，転送周波数を133MHz，64ビ
ット幅では最大1066MB/ データ転送を速くするだけでなく，複数の周辺機器からのデータ転
送が行われたときに効率的にインタフェースが使われるように工夫
されている．
として使われているのが，AGP（accelerated graphics port）で
ある．三次元グラフィック処理など高度のグラフィック処理では，
メモリからグラフィック装置に高速なデータ転送が必要となるた
め，専用のインタフェースが開発された．バス幅は32ビットで，
転送周波数66MHzで動作するデータ転送速度266MB/sのモー
ドと，転送周波数133MHzで533MB/sの2倍速転送モードの2
SCSI （small computer system interface）はPCと周辺機器を
ト幅（wide SCSIと呼ぶ）にすることによって，さらに高速化を
PCI （peripheral components interconnect）は，インテル社を
sのデータ転送速度になる．PCI - Xでは
グラフィック表示のためのチップとCPUの間のインタフェース
(C)日本機械学会
7―6 α7編 情報・ソフトウェア
種類が規定されている．
(６)消費エネルギーを少なくする．
さらに，次世代の入出力インタフェースとしてPCI Expressが
発表されている．PCI Expressは，グラフィックスチップの接続に
利用されているAGP，従来のPCIなどを置換えるものとなる．特
う．
徴としては，シリアル伝送を採用していることで，一方向当たり2
本の信号線で構成され，双方向のデータ転送には最小構成（1ビッ
トシリアル転送）で4本の信号線を用いる．この最小構成を1レー
ンと呼び，必要に応じて複数のレーンを束ねることで，求められる
帯域幅の提供を可能にする．1レーン当たり転送周波数は3GHz，
データ転送速度は2.5Gbpsが見込まれている．
このほか，ノート型PCに装着するPCカードのためのインタフ
ェースの規格としてPCMCIA（Personal Computer Memory
Card International Association）がある．
1・2オペレーティングシステム
(２)shortest - job- first 1・2・1 計算資源管理
a.プロセス管理（CPU時間管理) 計算機内部での処理の流
れはCPU時間の経過とともに進む．どの処理にいつどれだけの
CPU時間を消費するかを管理するには，処理の流れを抽象化して
扱いやすくする必要があるため，プログラム実行制御の流れをプロ
セス（またはスレッド）としてとらえる．
ともある．
実在するプロセッサ数がプロセス（process）の数より少なくて
も，各プロセスは，そのプログラムが，あたかもそれぞれに与えら
れたプロセッサ（擬似プロセッサと呼ばれる）により処理されるよ
うにみなされる．具体的には，プロセスの状態は，それを処理する
プロセッサの状態，プロセスのプログラムが占有している記憶領域
憶の管理を行う．
や入出力の状態を併せたものと見ることができる．プロセスは通常
計算機システム内に複数個存在し，実プロセッサの割当てを受けた
プロセスが実行状態となって処理が進められる．実行中のプロセス
が，入出力待ちなどで処理の続行ができなくなったときは，停止状
態となる．プロセッサは，他の実行可能状態のプロセスに割当てら
れ，そのプロセスの処理が行われる．停止状態のプロセスは，実行
可能状態になるとプロセッサの割当てを待つ．すなわちプロセス
は，実行状態，停止状態，実行可能状態の三つの状態をこの順に遷
移する．
ためには，プロセッサのレジスタ値などの状態や記憶空間の状態を
切換えなければならない．このことをコンテクスト切換え（con-
text switching）という．コンテクスト切換えには少し時間がかか
るため，頻繁にプログラムの流れを切換える必要がある場合には，
切換えに要する時間が多くなってしまい肝心の処理が遅くなる．そ
のため，処理の状態全体ではなくその一部だけを切換える軽量プロ
セス（light weight process）が考えられた．そのひとつにプロセ
ッサの状態だけを切換え，記憶空間は切換えずに共有するスレッド
（thread）がある．スレッドは最初Mach OSで実装されたが，
Windows Win32 APIにも含まれ，UNIX系のOSには，Posix準
拠のP-Threadなどが実装されている．またJava仮想マシンでは
ユーザレベルでスレッドが使用できる．以下，スレッドも含めてプ
ロセスと表記することにする．
ロセス）以外のプロセスは，実行可能状態となったものの中から，
次にプロセッサに割当てられるものが選択され，プロセッサを割当
てられ実行される．この選択をプロセッサスケジューリングとい
う．
(１) CPU稼働率を高くする．
(２)スループット（throughput）を大きくする．
(３)ターンアラウンドタイム（turnaround time）を短くする．
(４)待ち時間（waiting time）を短くする．
図1･10 記憶階層
(７)アプリケーションから要求されたQoSレベルに基づいて，
CPU，メモリ，I / O，ネットワーク，エネルギーの管理を行
スケジューリングの目的に基づいて実際にスケジューリングを行
うため，スケジューリングアルゴリズムが考えられている．スケジ
ューリングは，まず，プリエンプティブ（preemptive）とノンプ
リエンプティブ（non- preemptive）に分けられる．プリエンプテ
ィブスケジュールでは，プロセスは，実行が中断されることがある
［実行を中断されたプロセスが後に続行されるものを，特にプリエ
ンプティブレジューム（preemptive resume）であるという］．中
断することがないスケジュールは，ノンプリエンプティブである．
さらに，基本的な例として以下のようなものがある．
(１)first - come first -served：早く来たものから順に処理をする．
：処理時間の短いものから処理する．
(３)priority scheduling：優先順位の高いものから処理をする．
(４)round- robin：一定の時間ずつ処理をし，時間内に処理でき
ないものは，待ち行列の最後尾につける．
これらは，例えばround- robinの待ち行列を複数用意し，それ
らの間に優先順位を付けるというように，組合わせて用いられるこ
b.記憶管理 記憶装置は，高速であるが高価なデバイスか
ら，低速であるが安価なものまで幅があるため，図1･10のように
階層的に組合わせて用いられる．高速な順に大まかに，キャッシュ
記憶，主記憶，補助（二次）記憶がある．OSでは主記憶と補助記
.主記憶管理 プロセスを実行するためには，その実行に
必要な主記憶領域を割当てなくてはならない．この割当て方は利用
可能な主記憶管理のためのハードウェアやプロセス管理の方式に合
っていることが求められる．
唯一のプロセスに主記憶を割当てる単一連続割当て方式は，主記
憶管理のためのハードウェアを要しない．多重プログラミングのた
めに主記憶に複数個のプロセスを割当てる場合，単一連続割当て方
式では，多数のプロセスによる主記憶の再利用を経ると細切れの未
使用領域（fragmentation）が多数生じるむだが起こる．これは細
停止状態になったプロセスの代わりに別のプロセスを実行させる
切領域を一つにまとめる操作（compaction）や，停止状態にある
プロセスを主記憶から二次記憶に退避（swap out）し，代わりに
近い将来にスケジューリングされるプロセスを主記憶に再配置
（swap in）するスワッピング（swapping）などで改善できる．こ
の場合，ベースレジスタを設けるなど，プロセスが再配置可能
（relocatable）であるようにしておく必要がある．これらの問題
は，主記憶を固定長のページ（page）に分割する方式で解決され
るが，プロセスのプログラムの大きさと区画の大きさがぴったり合
わないことによる若干のむだが生じうる．
仮想記憶（virtual memory）方式は，動的アドレス変換機構
（DAT）を用いて，通常は主記憶の容量よりも大きい仮想アドレス
空間［virtual address space，論理アドレス空間（logical address
割込み処理，カーネルプロセス（OS自身が必要とする仕事，プ
スケジューリングの目的には以下のようなものがある．
(５)応答時間（response time）を短くする．
(C)日本機械学会
