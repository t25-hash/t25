# 1・3・1 アルゴリズムとデータ構造

> **章**: 第1章 情報システム
> **節**: 1・3 計算機ソフトウェア

a.基本データ型とそのデータ表現
.固定小数点表現と浮動小数点表現 現在利用されている
計算機の大多数はディジタル型計算機である．このディジタル型と
いう言葉は日本語では計数型と訳されるように，すべての数値は有
限個の離散的な値で表現される．このため，実数のようなちゅう密
な量を表現することは不可能であり，その中からいくつかの値を選
び出し，それらによってその付近の値を代表させることが必須とな
る．
ディジタル型電子計算機（以下では単に計算機と呼ぶ）では，基
本的な数値の表現方式として固定小数点（fixed point）表現と浮動
小数点（floating point）表現と呼ばれる二つの表現手段を併用す
るものが多い．
固定小数点表現の多くの場合においては，最も右のけたのすぐ右
に小数点が存在することを仮定しているため，これを特に整数表現
と称する．しかし，グラフィック演算や機器組込み用などの特殊な
用途では，小数点の位置を最も左のけたの左隣やその他さまざまな
位置に仮定するため注意が必要である．
一方の浮動小数点表現では，データ表現の中に数値そのものに関
する情報に加えて，小数点の位置に関する情報をも併せ持ってい
る．この数値そのものに関する情報の部分を仮数部，小数点の位置
に関する部分を指数部と呼ぶ．仮数部はその最も左のけたの左に仮
部の最上位けたは必ず1になるため，このけたを省略することも多
場合がある．また，数値を正規化すると指数部の数値が表現可能な
範囲を逸脱してしまう場合があり，特にそれが零に近いときには指
る．このような数値表現を非正規化数と呼ぶ．
簡略化することができる利点がある．また，加減算より乗除算のほ
容易にするために数値の絶対値と符号とを組合わせた表現を行う．
いためおもに下駄履き表示が用いられる．
符号ビットと31ビットの数値ビットを用い，－1×2
例えば，国家予算を整数として表現できるようになった．
化数で表現可能な範囲は32ビットの場合は10の－38乗から38乗
範囲を10の－4932乗から4932乗までとしている．
体に割当てられたビット数によらず一定としている．この場合には
表現可能な範囲は10の－79乗から75乗までとなる．
7―11
の小数点をおき，指数部で仮の位置から実際の小数点の位置までの
けた数を表示する．仮数部はその最上位けたが零以外の値になるよ
うに指数部の値を増減しながら左右にシフトされるが，この操作を
正規化と呼ぶ．また，仮数部の基数が2の場合は正規化された仮数
く行われる．指数部の基数が2よりも大きい場合は少ない仮数部の
けた数で広い範囲の数値を表現することが可能となるが，表現しよ
うとする値によっては仮数部内の数値の有効けた数が減ってしまう
数部に最小の値を与えて値を正規化することをあきらめることがあ
.符号の表現 数値を表現する場合に正負の符号をいかに
表現するかも問題となる．用途から加減算を容易にすることが重要
となる場合には，2の補数表示（2’s complement notation）もし
くは数値に定数を加えた値で表示する下駄履き表示を行う．これら
二つの表示方法においてはその値の増加方向が符号なし二進数の値
の増加方向とつねに同じになるため，加減算を行うハードウェアを
うが多く必要とされることが予想される場合には，乗除算の演算を
一般に，整数型の表現では1を加えたり引いたりといった加減算
の必要性が大きく，おもに2の補数表示が用いられる．また，浮動
小数点表現では乗除算の高速化が必須となるため絶対値表示が用い
られるが，この場合でも指数部については加減算しか必要とならな
.数値の精度（precision）と表現範囲 数値をより精度
良く，より広範囲にわたって表現するためには，数値を表現するの
に用いるメモリ領域をより多く割当てる必要がある．計算機におけ
る情報量の単位は二進法の一けたを示すビット（bit）であるが，
10ビットで十進法のおよそ3けたに相当する．
例えば，32ビットの符号付き整数を表現するには，1ビットの
（＝－2147483648）から2－1 （＝2147483647）までの範囲の整
数を，およそ9.3けたの精度で表現することができる．これが32
ビットの符号なし整数であれば0から2－1 （＝4294967295）ま
での範囲の整数を，また16ビットの符号付き整数であれば－1×
2 （＝－32768）から2－1（＝32767）までの範囲の整数をおよ
そ4.5けたの精度で表現することが可能となる．近年のプロセッサ
ではハードウェアで64ビットの符号付き整数を扱えるものが現わ
れた．これを用いれば十進法でおよそ19けたの整数を表現でき，
浮動小数点型の表現では，一つの数値に割当てられたビット数を
どのように仮数部と指数部とに割り振るかが問題となる．IEEE標
準に規定される浮動小数点形式では仮数部の基数に2を用いるが，
数値の符号に1ビットを割当てた後，32ビット浮動小数点形式で
は仮数部と指数部にそれぞれ23ビットと8ビットを，64ビット浮
動小数点形式ではそれぞれ52ビットと11ビットを割当てる．この
ため，浮動小数点表現の数値の精度は32ビットの場合はおよそ7
けた，64ビットの場合ではおよそ16けた程度となる．また，正規
まで，64ビットの場合は10の－308乗から308乗までとなる．ま
た128ビット浮動小数点形式の実装の一つでは，仮数部と指数部に
それぞれ112ビットと15ビットを割当て，正規化数で表現可能な
また，仮数部の基数に2以外を採用した例としてはIBM社の
360アーキテクチャ以降で用いられている16を基数とした表現形
式がある．この形式では指数部のビット数を7ビットとし，数値全
(C)日本機械学会
7―12 α7編 情報・ソフトウェア
もしも表現したい数値の有効けた数が，用いようとする数値表現
考えられる．
形式で表現できる精度を上回っている場合には，何らかの丸めを行
.整数型 正と負の比較的絶対値の小さな整数を表現する
って近似値として表現するほかない．あるいは，表現したい数値が
ために用いられる型で，代入・等値性判定・大小比較のほかに，加
正規化数で表現できる範囲から逸脱している場合には，非正規化数
減乗除算などの算術演算や，次の値や直前の値を求める演算が定義
を用いたり，零や正負の無限大で代表することになる．なお，浮動
される．
小数点数の表現体系の中に，無限大や演算結果が定義できないとき
.符号なし整数型 零以上の比較的絶対値の小さな整数を
に代用される値など，正確には数値でない値を表現するためのビッ
表現するために用いられる型で，代入・等値性判定・大小比較のほ
トパターンなどを定義することが行われている．
かに，加減乗除算などの算術演算や，次の値や直前の値を求める演
b.数値演算の誤差（error) 浮動小数点型で表現された数値
算が定義される．整数型とは表現可能な範囲が異なる．
は加減算を行う場合に誤差を発生する危険性がある．例えば精度が
.実数型 比較的絶対値の小さな実数を表現するために用
52ビットの表現をとっている場合に，指数部の値の差が52を超え
いられる型で，代入・等値性判定・大小比較のほかに，加減乗除算
る二数を加減算すると，絶対値の小さいほうの数値が絶対値の大き
などの算術演算が定義される．また，ちゅう密な実数を離散的な数
いほうの数値に影響を及ぼすことなく失われてしまう．すなわち，
値で代表させるため，数値の表現にはある程度の誤差を伴う．
零でない について＋＝が成り立ってしまうことになり，こ
.列挙型 その型に属するデータがとりうる値をすべて列
れをけた落ちと呼ぶ．
挙したデータ型で，値そのものは数値ではなく定数識別子であるこ
一方，ほとんど同じ絶対値をもつ二数を加減算すると，それらの
とが多い．代入・等値性判定・大小比較のほかに，次の値や直前の
符号によっては非常に零に近い結果を生じることがある．このと
値を求める演算が定義される．また，先頭の（最小の）要素から数
き，元の二数が十分な精度をもった値であっても，この演算によっ
えた順番を示す数値との相互変換も重要である．この型は順序尺度
て一気に有効けたを失ってしまって，結果として得られる数字のう
を実現するもので，算術演算は定義されない．
ち意味のあるけた数がほんの少しになってしまう場合があり，これ
.文字型 列挙型の特殊なケースで文字セットのすべての
を情報落ちと呼ぶ．
要素を列挙した列挙型と考えられる．
これら2種類の誤差は，いずれも計算機内で保持される数値の有
.論理型 列挙型の特殊なケースで真と偽とを要素とする
効けた数が有限であることに起因するものであり，いずれも加減算
列挙型である．代入・等値性判定・大小比較のほかに，論理演算が
によって発生するものである．これを避けるためには，演算を行う
定義される．
前に計算式を変形するなどしてけた落ちや情報落ちを発生しにくい
.部分範囲型 整数型・符号なし整数型・列挙型の一部の
演算順序に変形しておく必要がある．
範囲の値をとりうるデータ型で，上限と下限を指定して定義する．
c .アルゴリズム（algorithm）と計算量（complexity) アル
.集合型 列挙型もしくは整数型・符号なし整数型・列挙
ゴリズムとは，問題の解答を求めるために必要な計算の手順のこと
型の部分範囲型がとりうる値の集合のべき集合（powerset）をと
である．アルゴリズムにはすべての解答を求めた後に（ほかに解答
りうる値とするデータ型である．集合の積・和・差演算や特定の要
がないことを示すために）停止することが必要であり，またつねに
同じ計算経過をたどって同じ解答を出さなければならない．この意
味では，オペレータが途中で演算を停止させることを前提としてエ
ンドレスループにしたり，乱数を用いて解答を求めたりしたプログ
ラムは，正確にはアルゴリズムとは呼べないことになる．
データの分量を示す代表的な値（例えばデータ点数など）を と
したとき，例えば計算に必要な時間を の関数として表現するこ
とができる．この関数の＝∞の極限における最高次の項から係
数を除いたものを時間計算量（time complexity）のオーダと呼
び，同様に計算に必要な記憶領域に関するものを領域計算量
（space complexity）のオーダと呼ぶ．例えば，あるアルゴリズム
に従った計算に必要な時間を と表現できるなら，そのアルゴ
リズムの時間計算量は であると書き，これを「オーダ の
2乗」と読む．もしもアルゴリズムの計算量が入力データのサイズ
に依存しなければ1であり，比例するなら である．
ても必要になる時間や記憶容量を評価する手掛かりとなり，計算機
の速度や記憶容量の向上が目覚しい近年においても，どの程度の規
模の問題であれば現実に解答を得られるかという点で重要な概念で
ある．
する集合を示すものであり，記憶装置中では全く同じビットパター
ンで表現されるデータも異なった型に属するなら異なった解釈を与
えられる．さらに，コンパイラなどでは変数に対してもそれが属す
るデータ型を定義し，実行時のデータの解釈の高速化を図ってい
る．
り，あるデータに適用可能な演算操作の集合やその演算結果のデー
タ型はそのデータの属する型によってのみ決定される．すなわち，
データに適用可能な演算操作集合はそのデータの属する型の属性と
.キュー（queue）とスタック（stack) 線形リストでア
素を含むか否かといった演算が定義される．
.配列型 任意の一つの型に属する複数個のデータをひと
まとめにしたデータ構造である．要素データの型がすべて同じなの
で，それらに対して演算処理を施す場合には同質の処理を繰返し行
うことが多く予想されるため，要素データの指定を添字と呼ばれる
アルゴリズムにはそれに対応した計算量の概念が存在する．入力
順序を示す数値を用いて行う．また，要素データの数が有限かつ既
知であることを前提としているため，配列をメモリ内に割付けてそ
の任意の要素のメモリ内での位置をあらかじめ決定することができ
る．すなわち任意の要素にアクセスするための計算量が1であ
る．
配列の要素データに対しては，任意の要素に対して任意の順番で
参照と代入ができる．また，配列型データ全体に適用可能な演算操
作には代入や等値性判定がある．
.構造型 複数個の任意の型に属するデータをひとまとめ
にしたデータ構造である．構造型のデータのとりうる値の集合は，
各要素のとりうる値の集合の直積となる．各要素の型が互いに異な
アルゴリズムのオーダはそのアルゴリズムを利用する限りどうし
っていることから，個々の要素にははっきりとした機能の差がある
ことが期待されるため，要素データの指定には識別子を用いて行
う．
要素データに対しては任意の要素に対して任意の順番で参照と代
入ができる．また，構造型データ全体に適用可能な演算操作には代
d.データ型（data type）と基本演算 データ型はデータが属
入や等値性判定がある．
.線形リスト（linear list) 任意の一つの型に属する複数
個のデータをひとまとめにしたデータ構造である点は配列型と同様
であるが，要素データの数が定められていない点が異なる．このた
め，あらかじめメモリ中に記憶領域を固定することは不可能であ
る．また，アクセスできる要素はリスト中の一つのみに限られ，他
同じ型に属するデータに対しては同じ演算操作が適用可能であ
の要素をアクセスしたいときはアクセス可能な要素から前後に手繰
っていかなければならない．任意の要素にアクセスするための計算
量は である．
(C)日本機械学会
