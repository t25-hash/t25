# 1・4・4 TCP/IP におけるアプリケーションプロトコルの例

> **章**: 第1章 情報システム
> **節**: 1・4 ネットワーク

ここでは，TCP/ IPにおけるアプリケーションプロトコルの例と
して，電子メール（SMTPとPOP）およびWWW（HTTP）を
a.SMTP まず，TCP/IPで用いられる代表的な電子メール
プロトコルSMTP（simple mail transfer protocol）について簡単
に述べる．詳細については，文献(３)を参照されたい．
このプロトコルでは，図1･28に示すクライアント・サーバモデ
ルを仮定している．クライアントは，ユーザから送信された電子メ
ールを受取り，指定された宛先に示された受取り先のサーバにこの
電子メールを送付する役割を果たす．サーバはこれを受取り，自分
が管理するユーザ宛であることを確認してから，そのユーザ用に設
けられた所定のファイルシステムに電子メールを書き込む．このよ
うに，SMTPにおけるクライアントとサーバは電子メールを転送
する動作をすることから，MTA（mail transfer agent）と呼ば
れ，ユーザとMTAとの間のインタフェースとなるMUA（mail
SMTPの通信は，クライアントからサーバへコマンドを送り，
それに対して，サーバがクライアントに対して応答を返すことで行
われる．クライアントから送られるコマンドは，コマンドの種類を
表すアルファベットで始まる文字列で構成される．これに対して，
サーバからの応答は，応答の種別を表すコード番号から始まる文字
列で構成される．コマンドと応答はともに，改行文字CR（ASCII
コード13）とLF（ASCIIコード10）で終端される．
図1･29に，taro＠gama.ac.jpからhanako＠frog.ac.jpと
ichiro＠frog.ac.jpに同一内容の電子メールをSMTPにより送信し
たときの例を示す．ここで，「S:」で始まる行はサーバからクライ
アントへ，「C:」で始まる行はクライアントからサーバへ送られた
メッセージを示す．以下では，この例を用いてSMTPの動作を説
b.セキュリティとプライバシー保護 基地局と通信端末間の
明する．
.周波数ホッピング方式 周波数ホッピング（frequency
図1･28 SMTPのモデル
.直接スペクトラム拡散方式 直接スペクトラム拡散
.通信データの暗号化 通信データ自体を暗号化すること
秘密鍵暗号方式は暗号化・復号の動作が高速である反面，通信相
(C)日本機械学会
