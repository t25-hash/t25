# 3・1・1 運動学

> **章**: 第3章 ロボティクス/知能システム
> **節**: 3・1 運動制御

ロボットは、いくつかの剛体からなるリンク機構が関節で結合され構成される。運動学(kinematics)は、ロボットを構成する各リンクの形状や相対的関係から、三次元空間内におけるロボットの手先の位置姿勢やその速度に関する関係を取扱う。a. 座標系および回転行列図3・1に示すように、基準座標系をΣ、その原点をOとし、Oと原点を共有し剛体に固定した動座標系をΣとする。ここで、空間内のあるベクトルは、それを表す座標系の姿勢(方向)が異なれば、要素の値は異なる。そこで、どの座標系で表現しているかを明確にする場合は、のように右上に座標系を表す添字を付して表すものとする。また、ベクトルや行列の転置は、のように右上に添字を付して表すものとする。したがって、回転行列の9個の要素のうち独立なものは3個であり、剛体の姿勢の自由度が3であることに対応している。ここで、オイラー角φ,θ,ψについて説明する。図3・4に示すように、まず、座標系Σに対して軸まわりにφ回転した座標系をΣとし、Σに対して軸まわりにθ回転した座標系をΣとする。そして、Σに対して軸まわりにψ回転した座標系がΣとなる。c. 同次変換図3・5に示すような二つの座標系ΣとΣの間の位置、姿勢の関係を考える。同次変換行列を導入することにより、座標系の回転変換と平行移動が、一つの行列を掛けることにより表現できる。ただし、各回転角が微小である場合は、近似的に回転後の方向は回転の順序によらず同一とみなすことができる。d. ロボット・マニピュレータ(manipulator)の運動学(kine-matics)ロボット・マニピュレータは、いくつかの剛体からな図3・5二つの座標系ΣとΣの関係図3・6 リンクと関節図3・7 リンクパラメータおよび座標系るリンクが関節によって直列に連結され、その先端にはハンドが取付けられているものとする。各関節には1個のアクチュエータがあり、1自由度の回転または直線運動を行うものとする。多自由度を有する関節の場合(例えば、ユニバーサルジョイント)は、それと等価ないくつかの1自由度の関節からなる機構とみなすことができる。マニピュレータの運動を考える場合、通常リンクごとに1個の座標系を設定し運動の記述を行う。各座標系は任意に設定することができるが、デナビット・ハーテンバーグの表記法(Denavit-Har-tenberg representation,DH 表記法)がよく用いられる。. リンクの記述図3・6に示すように、自由度のマニピュレータのベース(台座)側から順にリンク1,2,...,と番号をつける(リンク0はベース)。各関節において、回転関節(rotary joint)の場合はその回転軸を、また直動関節(pris-matic joint)の場合は直動方向に平行な任意の直線を関節軸とする。空間内の二軸(直線)の相対的な関係は次の二つの変数,αによって記述できる。まず図3・7に示すように、隣接する関節軸と+1の距離を二軸の共通法線の長さ(リンク長さ、link length)で定める。二軸が平行な場合、共通法線は無数存在するが、そのうちの任意の1本を定める。次にその共通法線に垂直な平面を考え、この平面に関節軸と+1を射影し、その射影された2直線のなす角度をα(リンクのねじれ角、link twist)とする。このように4個のパラメータ(リンクパラメータと呼ばれる)でリンク機構を記述する方法は通常、デナビット・ハーテンバーグの表記法と呼ばれている。. リンク座標系ここで、各リンクに固定された座標系を1個ずつ定める。ここでは、前者のリンクのベース側関節軸上にΣを設定する方法を説明する。また、ベース(リンク0)に固定した座標系Σは動かず、マニピュレータの運動学における基準座標系となる。関節1の関節変数が零のときのΣに一致するようにΣをとれば、記述が簡単になる。このときは、=0,α=0、さらに、関節1が回転関節の場合は=0、関節1が直動関節の場合はθ=0とおいたことに相当する。なお、このように定めたΣが基準座標系として適当でない場合は、望みの座標系Σを基準座標系として設定し、ΣからΣへの同次変換行列を導入すればよい。残りの3個の値は定数である。なお、以上の方法では、各リンクへの座標系の配置は一意には定まらない。一般に、零となるリンクパラメータが多いほど、解析が容易になり、計算量も少なくなるので好ましい。したがって、軸の方向、共通法線、関節軸など任意性のあるものについては、なるべく多くのリンクパラメータが零となるように定めるのがよいとされている。これから、ハンド座標系Σをベース座標系Σに変換する同次変換行列は、次式のようになる。式(3・31)より、関節変位ベクトルが与えられたとき、ハンドの位置姿勢ベクトルは一意に定まる。また、マニピュレータに何らかの仕事をさせるときには、ハンドの位置姿勢ベクトルあるいはその軌道などが与えられることになり、それを実現する関節変位ベクトルを求めることが必要になる。関節変位ベクトルからハンドの位置姿勢ベクトルを求める問題を、順運動学(direct kinematics)問題といい、逆に、ハンドの位置姿勢ベクトルから関節変位ベクトルを求める問題を、逆運動学(inverse kinematics)問題という。一般に、逆運動学問題のほうが順運動学問題よりも難しく、一般的な構造を有するマニピュレータに対するこの逆問題の解法は見いだされていないが、手法として、1対象となる機構の特徴を用いて解析解を求める方法2数値的な反復解法がある。通常のマニピュレータでは、隣接する関節軸が平行あるいは直交していることが多く、その場合には解析的に求めることができる。6自由度マニピュレータにおいて、隣接する3個の関節軸が1点で交わるときは、閉じた形の解が求まることが示されている。産業用ロボットでは、ハンド側の3関節がこの条件を満たすものが多い。その解法は次のようになる。ただし、解は複数個得られる。e. ヤコビ行列(Jacobian matrix)。マニピュレータの速度、角速度リンクに固定された点Pの速度およびリンクの角速度を考える。なお、ハンドに固定された点の場合は以下の式において、=とすればよい。これを時間で微分して、速度の関係は次式で表される。この式(3・34)は、単に位置ベクトルに対してだけでなく、任意のベクトルに対して二つの座標系における時間微分の関係を与える重要な関係式である。ここで、関節変位をdだけ微小変位させたとき、ハンド先端の位置姿勢の微小変位dは式(3・47)で定まる。逆に、ハンド先端の位置姿勢をdだけ変化させたいときの関節変位dは、もしヤコビ行列の逆行列が存在すれば、次式から計算できる。さらに、式(3・50)を時間で微分して、次のように、ハンド先端の位置姿勢と関節変位の加速度の関係が得られる。ヤコビ行列の具体的な表現ハンド先端Pの位置ベクトルは直交座標系の3個のパラメータで表されるが、姿勢の表現方法はいくつか考えられる。したがって、式(3・53)は次式になる。以上をまとめると、ヤコビ行列は一般に次のように表される。式(3・55)および(3・28)より、次の関係が得られる。なお、ハンド先端Pとしては、ハンド(座標系Σに固定された任意の点を考えることができる。. ハンドの速度を実現する関節速度ハンド先端の位置姿勢の速度ベクトルが与えられて、それを実現する関節速度を求めたい場合がある。このときの姿勢を特異姿勢(singular configuration)といい、その点を特異点(sin-gular point)という。特異姿勢のときは自由度が縮退し、ハンドは自由度の運動ができない。また、特異姿勢の近傍では、指定されたハンド先端の速度を実現するために要求される関節速度が非常に大きくなるため、この特異点を避ける工夫が重要となる。この場合によく用いられるのが次の疑似逆行列(pseudo-inverse matrix)である。そして、機構の平衡を保つためには、式(3・73)の逆符号の関節駆動力を発生させればよい。なお、関節軸方向(まわり)以外の力(モーメント)は、機構全体に作用し平衡が保たれる。
