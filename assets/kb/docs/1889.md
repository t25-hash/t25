# 18・2・2 デナビット・ハーテンバーグの記法

> **章**: 第18章 ロボットアームの力学
> **節**: 18・2 ロボットアームの運動学

の長さと呼ばれる．
軸に対して 軸のなす角度をαとする．
αはリンクのねじれと呼ばれる．
αの正方向は 軸の方向とする．
Oから 軸方向に沿って測った 軸と 軸の共通法線の足の位置を とする．
軸に対して 軸のなす角度をθとする．
θの正方向は 軸の方向とする．
以上定義した四つのパラメータの中で，回転ジョイントではθが，直動ジョイントでは がジョイントの動きを表すジョイント変数（joint variable）となる．
他の三つのパラメータはリンクの形状を表すリンクパラメータ（link parameters）である．
デナビット・ハーテンバーグの記法では，リンク座標系は必ずしも一意に定まらない．
例えば，スライドジョイントでは， 軸の方向は決まっても，その位置は一意に定まらない．
デナビット・ハーテンバーグの記法では，このようなときは，解析者が適宜決定するものとしている．
また，以上述べたリンク座標系の定義からは，先端リンクすなわちエンドエフェクタに固定する座標系は決定されない．
先端リンクの座標系の原点は，通常，その一つ手前のリンク座標系の原点に一致させる．
さて，いずれにせよ，リンク座標系が定義されれば，各座標系間の相対位置，姿勢関係を解析することにより，ロボットの運動を解析することができる．
ΣとΣの位置，姿勢関係は，ΣのΣに対する姿勢の変換行列 と，OからOへ至る位置ベクトル により表すことができる．
ここで， の左肩字－1Oに一致させる．
を用いると，リンク機構を組織的に解析することが可分表示したベクトルを， とすると，ロボットアームの運動は，機構を構成する各リンクの空間運動により決定される．
したがって，その運動を解析するためには，各リンクの空間運動を解析すればよい．
各リンクを剛体と仮定すれば，その運動は，各リンクに固定した座標系の運動により表すことができる．
O- を固定したとしよう．
この座標系をリンク座標系（linkcoordinate system）と呼び，Σで表す．
Σは，原点Oの位置と各座標軸の単位方向ベクトル，， により表すことができる．
リンク座標系の選び方は，種々考えられるが，一般に用いられる記法は，デナビット・ハーテンバーグの記法（Denavit -Hartenbergこれを図18･5に示す．
図18･5は，回転ジョイントに対するリンク座標系の設定を示す．
直動ジョイントに対するリンク座標系の設定については，文献を参照されたい．
リンクには，台座側から，台座に固定されたリンクを0として，先端リンクへ向って，1 ,2 ,3,…と番号を付ける．
ジョイントには，やはり台座に近い側から先端へ向けて，1,2 ,3,…と番号を付ける．
番めのリンクに固定されるリンク座標系Σ(O-）は次のように定義される．
＋1番めのジョイントの回転または直動軸を 軸とする．
軸の方向は回転または直動の正方向とする．
軸と 軸の共通法線と 軸の交点を原点Oとする．
軸と 軸が交わるときは，その交点を原点Oとする．
Oはは，このベクトルがΣで成分表示されていることを示す．
軸と 軸の共通法線を 軸から 軸方向に延長した軸を 軸とする．
軸と 軸が交わるときは×に平行に 軸をとる．
向きはどちらでもよい．
は，ジョイント変数の関数となっている．
