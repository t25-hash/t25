# 20・3・2 除振装置の振動制御

> **章**: 第20章 制振および振動・衝撃の絶縁
> **節**: 20・3 能動的振動制御

a.除振装置の制御とその目的 精密機器の支持機構である除も同様に与えることができる．
モード座標への変換は，不減衰角固有振動数をΩとする一般小さい順に並べたモードマトリックスを導入することによって，モード変位ベクトル と変位ベクトルられる．
このとき除振装置の微分方程式は，となり，この両辺に左から を掛けて，さらにモード座標のマト度を独立に扱うことができる．
c .制御系設計と特性 剛体モデルを可観測（observable），信号と6点以上のアクチュエータが必要とされる．
このとき用いる号や力をそのまま出力できるわけではなく，個別座標から制御座標できる限り同じ位置，同じ向きに配置して直接フィードバック制御ここではより一般的な手法として座標変換を用いる．
座標信号を取り出すには，,,で表し，その検出方向に直交する平面内のセンサの位置座ことによって を求めることができる．
アクチュエータの個別座標での操作信号を としたときその座対角行列 から＝で与えられ，個別座標の操作力から直交標系の操作力は のように与えられる．
センサの場合と同様定義できる．
述することが可能になる．
示すが，このようにモデル化された制御対象に対するコントローラ表的なものは最適レギュレータ（optimal regulator）理論である．
御の評価関数として用い，これを最小とする制御を実現するもので状態ベクトルを改めて＝ とし，操作入力を として外乱ベクトルを ,＝ とし，さらに＝ と作入力＝－＝－を求めればよい．
この はリカッによって定義される．
また，ここには記さないが減衰マトリックス制御について述べる．
除振装置の制御目的には，振動の絶縁，直接外乱の制振，位置・姿勢の保全，を挙げることができ振装置の運動は，次のように微分方程式で表すことができる．
この方程式の状態表現を求めると，システムマトリックスをが得られる．
可制御（controllable）に制御するためには，独立した6点以上のセンサ，アクチュエータは制御座標（直交座標やモード座標）の信に変換することが必要である．
もちろんセンサ，アクチュエータをを行うという手法（direct velocity feedback）も実用的であるが，直交座標に平衡に，かつ独立に配置された6点のセンサから直交を与える がわかればよい．
センサの個別座標での検出方向を標を回転成分の向きに符号を合わせて，センサの数だけ縦に並べる， 方向に配置している場合は次のようになる．
は制御理論を用いて求めることができる．
制御理論の中でも最も代この理論は応答と制御力の二次形式（linear quadratic）の和を制あり，LQ制御とも呼ばれている．
これに基づくフィードバック制御器 を仮定し，モード座標系のすると，状態方程式は，と表される．
の解として与えられる．
