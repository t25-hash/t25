# 22・4・1 理論モード解析

> **章**: 第22章 振動・音響の計算法
> **節**: 22・4 モード解析

由度の剛性行列と質量行列を得ることができる．
a.不減衰系 不減衰系の固有値解析（22･3･3項）で得られるモードを求めることである．
これらは，の固有値問題から求めることができる．
ここで， と は有限要素法で得た剛性行列と質量行列を表す．
ここで，行列 は 組の固有モードを各列に組込んでできる 行この式は数学的には一般的固有値問題として取扱われる．
固有値λとそれに対応する固有ベクトル は 組得られる．
そこで，その番めの固有値をλ，固有ベクトルを と表せば，固有値と固有角振動数Ω( rad/s）および固有振動数 (Hz）の関係は，ここでは以下に周波数領域のみの運動方程式について取扱おう（時間領域についても全く同様に展開できる)．
を減衰項を省略したに代入し，その両辺に左側から固有モード行列の転置行列を掛ければ，となり，運動方程式はモード座標上に変換できる．
振動解析の基本は，対象物の動特性，すなわち固有振動数と固有組の固有モードは互いに一次独立な関係にあるから，いかなる強制振動応答でもそれらの一次結合で表現できる．
すなわち，なお，その自由度を とする．
列の固有モード行列と呼ばれる行列である．
そして， とωが一次結合の各固有モードの寄与度を表すベクトルである．
それらは物理空間座標上での応答変位を固有モードを一般化座標とした場合のモード座標（modal coordinate）上で表現するものであり，固有モード行列は物理空間座標とモード座標間の座標変換行列とみなすことができる．
であり，固有モードは固有ベクトルにほかならない．
実際の計算は，行列の自由度が一般的に大きいので，大自由度行列に適する固有値解析法である逆反復法やランチョス法またはサブスペース法などの数値計算手法を利用する．
これにより実用的に必要な低次のいくつかの固有値および固有ベクトルのみを効率よく計算することができる．
