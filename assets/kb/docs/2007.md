# 22・5・1 一次元の音響問題の境界解法

> **章**: 第22章 振動・音響の計算法
> **節**: 22・4 モード解析

a.一次元音響問題の例 一次元の音響問題の例として，長さ両端を＝0，＝とする．
両端で に比例して変化する音圧め， の未知関数Φを用いて，音場を定める速度ポテンシャルφして，関数Φを定める微分方程式る固有モード（採用固有モード）を求め，さらに不採用のほかの固を得る．
境界条件として例えば，＝0で音圧，＝で粒子速度が与えられているとする．
これをΦで表すと，境界条件は，＝0において関数の値Φ＝φが，＝において微分Φ/ ＝が与えのように表現する．
結合領域では分系2から内力として作用力を受けるので，結合領域の自由度に対応する剰余コンプライアンス行列を求めることが重要である．
の下側の式から，の関係が導ける．
これらを平均的に零に近づけるための条件として，を考えることができる．
ここで は重み関数である．
これがいまの問題の重み付き残差の条件式である．
に適当な関数を与えたとき，上式を満たすΦが，求める近似解である．
値については，その値を代入したものを意味する．
ここで例として取上げた以外の別の境界条件の問題でも，代入する値が異なるだけで，重み付き残差の条件式はと同じになる．
以下を問題の重み付き残差の条件式とする．
c.基本解 ≠ξのとき0，＝ξのとき∞（ただし点＝ξを含む に関する積分値は1）となるデルタ関数をδ,ξあるいはδ－ξと書く．
一般的にはを解いて全系に対する固有振動数を求める．
ら，その方程式の自由度は採用モード数までさらに縮小することができる．
すなわち，の関係式を利用してのように自由度縮小ができる．
物理座標上での固有モードは，上記の一般化座標上への座標変換の行程を逆進すれば求めることができる．
の音響管の音場解析を考える．
音響管の軸に沿って 軸を定め，あるいは粒子速度が与えられているとする．
この問題を解析するたをφ＝Φとおく．
これを波動方程式に代入すると， を定数と有モードの影響を補正するための剰余コンプライアンス行列を求める．
そして分系1の変位をられる．
b.重み付き残差の条件式 22･5･1･a項の問題を，重み付き残差法（weighted residual method）で解くことを考える．
となるので，これにを代入して，両辺左側から採用固有モード行列の転置行列を掛けて固有モードの直交性を考慮すれば，と式変形できる．
ここで，左辺係数行列の と はそれぞれモード剛性行列とモード質量行列であり，対角行列の形式である．
と書き直される．
ここでこの式は，境界条件として与えられているが導かれる．
そこで，分系2についても同様の式展開を行い，内力を作用・反作用の法則により消去するように結合すると，全系に対する方程式が次のように構成できる．
タ関数δ,ξを加えた方程式の解を基本解（fundamental solu-tion）という．
の基本解Φ,ξは，＝－ξ を用いである．
d.境界法による解析 において，重み関数 として，上述の基本解Φ,ξを採用する．
このときから，を得る．
ここで，である．
は，領域の内部の点＝ξにおけるΦの値が，境界におけるΦ,の値を用いて得られることを示している．
未知関数Φを求めるため，においてξ→0およびξ→とすると，境界におけるΦ,の間の二つの関係式を得る．
