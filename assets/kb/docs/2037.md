# 1・4・5 ノンパラメトリックモデル同定法

> **章**: 第1章 システム解析
> **節**: 1・4 システム同定法

白色化フィルタを設計し，入出力データをフィルタリング次式よりインパルス応答を推定する．
同定法 特徴（◎は利点，×は問題点)◎ むだ時間，時定数，定常ゲインなどに関する情報◎ 現場で最も利用されている同定法の一つである．
× 得られる同定結果は限定的である．
× 外乱や測定雑音に弱い．
◎ むだ時間，時定数，定常ゲインなどに関する情報を直接同定できる．
◎ 特別な入力を必要としない．
◎ SN比が悪くてもデータ数が多ければ大丈夫．
→ 閉ループデータは利用できない．
◎ システムに仮定される条件は線形性のみ．
◎ 着目する周波数帯域を重点的に同定できる．
× 低周波の同定に時間がかかる．
◎ 外乱のスペクトル密度も同時に推定できる．
× 窓長の選定が必要．
このように予測誤差から構成される評価規範 を最小にすmethod）という．
ただし， は予測誤差ε,の確率密度関一方，,,ε,として，二次関数と選んだ場合を最小二乗法（least square method）という．
→ 閉ループデータは利用できない．
に対する評価規範 を最小にする推定値を計算することになる．
いので，直接，閉ループシステム同定データは利用できない．
（spectral analysis method）を用いると，システムの周波数特性のになる．
φτ，φτを計算する．
例えば，次のようにすればよい．
対応するスペクトル密度の推定値を計算する．
周波数伝達関数と外乱のスペクトルの推定値を，それぞれスペクトル解析法では，システムに仮定される条件は線形性のみでい．
同定実験により，入出力データ,;＝1,2,…を得る．
相関法を用いると，むだ時間，時定数，定常ゲインなどに関する情二乗ノルムや対数尤度などが用いられる．
未知パラメータ の推定値［ とする］は，を直接同定できる．
× 同定入力の振幅が同定精度を左右する．
× 入力と外乱とは独立でなければならない．
◎ 利用が容易である．
◎ システムに仮定される条件は線形性のみ．
× 入力と外乱とは独立でなければならない．
◎ 利用が容易である．
× 入力が純粋な正弦波でないと，推定精度が大きく変動する．
より決定される．
ここで，arg minは を最小にする といるように推定値を計算するパラメータ推定法を総称して，予測誤差数を表す．
→ 同定対象へ負担がかかる．
このとき，は次式になる．
したがって，この場合の最尤推定値は最小二乗推定値に一致す報を直接同定できる．
一方，入力と外乱とは独立でなければならなb.スペクトル解析 次に手順を与えるスペクトル解析法みならず，外乱のパワースペクトル密度も直接推定することが可能 個の入出力データより，相関関数の推定値φτ，ただし，τは窓関数であり， は窓長である．
次のように計算する．
一方，相関法と同様に入力と外乱とは独立でなければならなノンパラメトリックモデルの同定法について表1･4にまとめた．
