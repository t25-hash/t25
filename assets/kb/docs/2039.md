# 1・4・7 部分空間法

# 1・4・7 部分空間法

> **章**: 第1章 システム解析
> **節**: 1・4 システム同定法

a.特異値分解法 状態方程式
＋1＝＋
＝＋ (1･154)
で記述される多入出力システムのインパルス応答データから，状態
空間モデルを実現する特異値分解法（singular value decomposi-
tion method）を紹介する．
ホー（Ho）とカルマン（Kalman）によって提案されたインパ
ルス応答からの最小実現は確定的な方法であったため，雑音に汚さ
れた実データに適用することは難しかった．この問題点に対して，
1970年代後半，クング（Kung）は特異値分解という手法を使うこ
とによって，信号成分と雑音成分を分離する方法を提案した．ここ
ではクングの方法を特異値分解法と呼び，その手順を次にまとめ
る．
(１)対象のインパルス応答を，次に列挙する方法のいずれかを
用いて求める．
･インパルス信号を対象に加え，直接インパルス応答を測定する．
･ステップ信号を対象に加え，ステップ応答を測定し，それを差分
することによりインパルス応答を得る．
･対象の入出力データからインパルス応答を推定する．例えば，
ⅰ.相関法
ⅱ.FIRモデルを用いた最小二乗法
(２)インパルス応答列からハンケル行列を構成する．
(３)ハンケル行列を特異値分解する．そして，特異値の大きさ
を参考にしてシステムの次数を選ぶ．
(４)最小実現に基づいて状態空間モデルを計算する．
(５)伝達関数が必要な場合には，次式より計算する．
＝－＋
b.部分空間法 特異値分解法はインパルス応答のような出力
データのみからシステム同定を行っていた．それに対して，部分空
間法（subspace method）は通常の入出力データから状態空間モデ
ル（state space model ）を同定できる有力なシステム同定法であ
る．
対象とする多入力・多出力システムが雑音を考慮した状態空間モ
デル
＋1＝＋＋ (1･155)
＝＋＋ (1･156)
によって記述されるとする．ここで，状態変数 に関する事前
情報は利用できないものと仮定する．したがって，システムの入出
力関係を記述する任意の行列 ， ， ， を推定することが，
ここで考えている同定問題である．
まず問題を簡単にするために，入出力信号 ， だけでは
なく，状態変数 も測定可能であると仮定する．すると，式
(1･156)は次のように変形できる．
＝＋ (1･157)
ただし，
＝ ＋1
＝
＝
＝
式(1･157)は，未知パラメータベクトル に関して線形回帰式にな
っているので，最小二乗法によって を推定することができる．
また， の共分散行列も，モデルの残差の総和として簡単に推
定することができる．
以上では，状態変数ベクトル が測定可能としてきたが，現実
にはこれを推定しなければいけない．この問題に関しては，すべて
表1･6パラメトリックモデルの同定法
同定法（モデル) 特徴（◎は利点，○は性質，×は問題点)
◎ 線形回帰モデルに基づく方法なので，逆行列演算で推定値が計算できる．
○ 高周波帯域に重みがかかった同定法である．
最小二乗法（ARX)
× 同定モデル次数を高くとる必要がある．
× 式誤差が有色性の場合には推定値にバイアスがのる．
拡大最小二乗法（ARMAX) ◎ 式誤差が有色性であっても，システムの動特性を同定できる．
× 雑音モデルを準備する必要がある．
◎ 式誤差が有色性であっても，システムの動特性を同定できる．
補助変数法（ARX)
◎ 必要以上にモデル次数を高くとらなくてよい．
× 入力と外乱とは独立でなければならないので，閉ループ同定実験データには適用できない．
◎ システムの動特性と雑音モデルを分離して同定できる．
◎ 時間応答シミュレーションに適している．
出力誤差法（OE)
◎ 同定法は全帯域にわたって平坦な周波数特性をもっている．
× 非線形最適化計算を行う必要がある．
◎ システムの動特性と雑音モデルを分離して同定できる．
出力誤差法（BJ )
◎ 時間応答シミュレーションに適している．
× 非線形最適化計算を行う必要がある．
◎ ほとんどすべての線形システム同定問題に対応できるモデル構造（最小二乗法，拡大最小二乗法，出力誤差法を特殊な
予測誤差法
場合として含んでいる）．
× 非線形最適化計算を行う必要がある．
◎ 多変数系へ容易に拡張できる．
◎ 非線形最適化演算を含まない．
部分空間法（状態方程式)
◎ ユーザが設定するパラメータが比較的少ない．
× 雑音に弱い場合がある．
の状態変数ベクトル は，入出力信号から， 段先出力予測器
＋,＝1,2,…,
の成分の線形結合として構成できることが知られている．ここで，
する．
はモデルの次数（状態変数の次元）である．すると，これらの予
測器を構成することができ，これらの成分の中から基底として次を
選ぶ．
ARXモデルの次数をいろいろ変化させて最適な次数を決定する．
れたモデルの極と零点を 平面上にプロットし，極零相殺の有無を探
す．もしも極零相殺があれば，それらを無視したモデルを再構成する．
ARMAXモデル，OEモデル，BJモデルを利用してモデル構築を行
う．
表1･7モデル構造の決定手順
(１) むだ時間の決定 次に示すいずれかの方法でむだ時間を決定
(ａ) 相関法を用いてインパルス応答を推定し，むだ時間を読み取る．
(ｂ) 低次（例えば四次）のARXモデルに対して，例えばクロスヴ
ァリデーションを適用し，むだ時間を決定する．
(２) システム次数の決定 (１)で決定されたむだ時間を用いて，
(３) (２)で決定された次数は高次である可能性が強いので，推定さ
(４) (３)で決定された次数を用い，さらに高度なモデルである
＋1
＝
…
＋
によって状態空間実現の基底は決定され，これは問題が良条
件になるように選定される．予測器＋は ， ，
1≦≦の線形関数であり，直接入出力信号を線形射影することに
よって効率良く決定できる．
以上のような考え方に基づく多入出力システムの同定法を部分空
間法と総称する．フィルタ としてどのようなものを選定するか，
どのような数値計算アルゴリズムを用いるかによって，さまざまな
部分空間法に分類できるが，ここではその詳細については省略す
る．
部分空間法の特徴をまとめると，次のようになる．
(１) 部分空間法は入出力データからシステム同定を行うことが
できるため，不安定系や閉ループ系などへも適用することが可能で
ある．
(２) SVDやQR分解などの数値的に安定なアルゴリズムを利
用しているため，計算精度が高い．
(３) 多項式ブラックボックスモデルに対する予測誤差法のよう
に，非線形最適化計算を行うことなくシステム同定が行える．
(４) 多入出力システムへの拡張が容易である．
パラメトリックモデルの同定法について表1･6にまとめた．
