# 2・2・2 幾何形状を処理するための数学理論

> **章**: 第2章 設計情報の表現と伝達
> **節**: 2・1 節で述べられているように、従来の設計では図面が情報伝達

a. ベクトル記法とベクトル演算ベクトルとは、大きさと方向をもつ量である。その線分の長さがベクトルの大きさを、始点から終点への矢印がその方向を表す。三次元空間では、一つのベクトルは三つの自由度をもつ。すなわち、座標の三つの要素である。座標系は直交している必要はない。ある座標系の各軸に対応した三つ1―28設計工学の単位ベクトル(unit vector)は基底(basis)として利用される。通常、ベクトルが位置を表す場合、そのベクトルの始点は原点に固定されていて、原点0,0,0からの差であることは明示しない。これはまた、ベクトルの絶対値(absolute value of vector)とも呼ばれる。ベクトルと方向が一致し、しかもその大きさが1であるベクトルを、ベクトルの方向の単位ベクトルと呼ぶ。内積の値はスカラ値であり、二つのベクトルの絶対値の積に、それらのベクトル間の角度θの余弦値(cos)を掛けたものである。したがって、次の等式が成り立つ。内積は、掛ける順番には依存しない。は、その大きさが二つのベクトルの絶対値の積に、それらのベクトル間の角度θの正弦値(sin)を掛けたものであるベクトルとする。ベクトル積の順番を逆にすると、その結果の方向は逆方向になる。スカラ三重積(scalar triple product)は、二つのベクトルのベクトル積ともう一つのベクトルの内積である。この値が零であるならば、任意の二つの要素ベクトルが同じか反対の方向をもっているか、または、三つのベクトルは同一平面内にある。また、負であるなら裏面にある。これは、微分の定義から明らかなことである。二つのベクトルのベクトル積または内積の微分する過程は、スカラ変数の積を微分するのと同様である。ベクトル積の微分の場合、以下のとおりであるが、微分する順番を変えることはできない。 直線、平面式直線および平面の式は次のようになる。(4) 平面のパラメトリック表現:上記の表現は、陰関数表現である。すなわち、座標値が方程式で拘束されている。これに対し、座標値を直接パラメータで表したものが、パラメトリック表現である。曲線は1パラメータであるが、曲面は2パラメータとなる。(2) 線分と平面の交点(intersection of line and plane):式(2・17)を式(2・18)に代入すると、線分と平面間の交点座標を求める式となる。これは、2個の平面が平行となる場合や、3個の平面が交線を共有する(同軸関係となる)場合、すなわち、3法線が同一平面上の場合である。(4) 4個の平面が交点を共有する条件:この条件は、交点(頂点)が平面上にあるかどうかの判定に使用される。これは、行列式で書き表すことができる。e. 座標系の変換と物体の移動・回転。座標変換(coordinate transformation)の基礎空間に位置する点ベクトルを、どの座標系で表しているかを明示するために、肩字として座標系を付けて、のように表記する。座標の表記にも、同じ形式を用いる。これは式(2・28)より直接、座標系どうしの単位ベクトルの内積からも定まる。. 座標系の移動座標系の移動を行列で表現するには、四次元を用いなければならない。. 物体移動と座標系の変換座標系を変換する場合と、物体を移動、回転する場合の関係を明らかにする。次に、この軸回りに点を回転して、その後、元の座標系に戻す.一般的には、このような点の移動行列を下記のようにで表す。′の位置は、以下により与えられるとする。行列は、式(2・40)より以下の形式となる。このことは、点′は実座標系から見ると、虚の点となることを意味する。鏡映を2回繰返すと、平行移動または回転を表すことができる。. 透視投影透視変換(perspective transformation)は平行投影と違って、遠近感のある図を得ることができる。このためには、変換行列の第4列の1行めから3行めの要素が零でなくなる。立方体を真上から見ると一点透視図が得られ、座標系を回転して透視投影を行うと、二点透視図が得られる(図2・78)。軸方向の稜線は平行である。透視変換行列は、回転行列の代わりに視点、注視点、上向きベクトルから、座標系を定義することで定めることもできる。グラフィックスでの設定では、両方が使用される。f. 曲線の性質。曲線の性質を示す接線方向や曲率を求めるため、その定義に必要な線素(弧長)を定める。ρを曲率半径と呼び、円の中心を曲率中心と呼ぶ。. 空間曲線の諸量空間曲線上の点における座標系を定義する。この平面は曲線が接している平面であるから接触平面と呼ばれる。は空間曲線の場合には、2階微分ベクトルの方向が含まれる側をとる。後ろの二つを主法線ベクトル(principal normal vector)、陪法線ベクトル(binormal vector)と呼ぶ。それぞれの座標系の基準ベクトルの線素による微分を考える。τは空間曲線の捩率(torsion of curve、またはねじり率)を表す。次に、曲線上の1点周辺の形状を近似して表すことを考える。これらは、空間曲線の各投影面での近似形状を表す。g. 自由曲面の幾何学。この曲線の微分をとると、曲面上の1点における接線ベクトルを表す。これらを,と書き(他のパラメータを一定として微分するので偏微分となる)、曲面の基本ベクトルと呼ぶ。これらは、その点での接平面を規定する。. 法断面と法曲率i項の記号を用いて、曲面上の断面線での曲率を考える。法平面と曲面との交差を法断面と呼ぶ。次に、法断面の曲率を考える。式(2・64)を代入すると法曲率が求まる。法平面を法線の回りに回転すると、法曲率の値は変化し、極大値と極小値をもつ。この曲率値を主曲率(principal curvature)と呼ぶ。これには最大、最小の二つの値,があり、次の二次方程式の根である。を平均曲率(mean curvature)、をガウス曲率(Gaussian curvature)と呼び、この二つの値でおおざっぱな形状がわかる。すなわち、が正であれば、曲面はどちら側(表側か裏側)かに曲がっており、その大きさはほぼ平均曲率である。が零であれば、円柱のように片方の成分が直線であり、すべての点でこの条件を満たせば、この曲面は展開可能である。この式は、主曲率をもつ法断面の方向を示している。最大、最小の二つの方向は直交している。これを主方向(principal direction)と呼ぶ。曲面上の点で、式(2・75)を満たすものをつないでできる曲線を、曲率線(lines of curvature)と呼ぶ。曲率線は曲面上に直交する網を作る。主曲率が重根をもつと、方向が定まらなくなる。このような点は特異点となり[臍点(umbilic)と呼ぶ]、直交網がここに集まらなくなる。部分的には、球か平面となっている形状である。ただし、θはその点の軸からの角度である。この式をオイラーの公式という。
