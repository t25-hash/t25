# 2・5・2 サンプル値制御系のモデル

> **章**: 第2章 線形制御
> **節**: 2・5 サンプル値制御

2･5節の始めに述べたとおり，サンプル値制御系は連続時間フィ装されるディジタル補償器と置換えたものである．
ディジタルコン制御対象の入出力信号と異なる．
離散時間信号として取扱うことができる．
るので，その集合は離散的である．
の変換であり，量子化とは連続的な値からディジタル計算機で表現リングの影響のみ述べる．
を内部安定化する．
をサンプリングした離散時間信号 は 0,である．
ば，離散時間 設計問題の解は存在しない．
の は離散時間 設計問題の一つの解である．
ステップ3： に対して を次式で定める．
ここで周期 はサンプリング周期と呼ばれる．
多くが用いられる．
の を理想サンプラ（ideal sampler）（ω＝2π， ：整数）で表される連続時間信号の周波数成分をサ（aliasing）という．
ωはサンプリング角周波数（sampling frequency）と呼設計問題は，補題1に基づき以下の手順で解くこ器の前にはローパスフィルタが置かれ，アンチエリアシングフィル離散時間信号 が連続時間信号 にD-A変換されるとき，式期しているという．
多くの場合D- A変換器のモデルとして ,τ＝とし一方，D-A変換器は離散時間信号を連続時間信号へ変換する．
離散時間システム，の零次ホールド と置換えた図記述の簡単のため，， をまとめて と書き，いる．
御系 は安定ではない．
したがって以下＝0を仮定する．
計は近似を用いた以下の二つの手順のいずれかにより行われていた設計手順における の離散化では， の伝達関数を次式で［仮定3］ において＝0である．
単一正弦波入力に対する応答が単一正弦波とはならない．
が安定化補償器であってもタスティン変換で定めた によっ点間の系の挙動を無視している．
てサンプル値制御系の時不変モデルを示す．
と のフィードバック結合を ,と表す．
すなわち，の入出力安定性（1･3節参照）について，次のことが知られてが成立する．
はリフティングによって の情報が失われ入出力信号をリフティングすることで，連続時間システムは離散は線形システムであるが，以下の特徴がある．
で定めると，仮定3のもとで は次の状態空間表現をもつ．
ただし，以下の定義を用いた．
に対して連続時間制御系設計手法を用いて連続時間補 のサンプル点上の特性から離散時間システム を定次の状態空間表現をもつ．
て伝達関数 を定義することができる．
sis) 与えられた が仮定4を満たすとき以下の仕様を満たすただし，固定した∈に対して， は 0,上の作用素である．
サンプル値制御系の内部安定性は，より定義1に従っは等価である．
次の定理は定理1に対応する．
［定理8］ サンプル値制御系が内部安定であるための必要十分条件は， のすべての固有値が の要素であることである．
つ可検出性でなければならない．
したがって，2･5･3項以後のサンプル値制御系の設計問題においては以下を仮定する．
［仮定4］ において,は可安定かつ,は可検出でが可安定かつ可検出性であれば，ほとんどの については可安定かつ可検出である．
したがって，サンプル値制御には2種類の信号が混在しており，サンプル値制御系の大きな特徴となっている．
a.A-D変換とD-A変換 サンプル値制御系は，混在する2種類の信号を相互変換することでフィードバック系を構成していA-D変換器はサンプリング（sampling）と量子化（quantiza-tion）を行う．
サンプリングとは連続時間信号から離散時間信号へ可能な値への変換である．
ここでは量子化の影響は無視し，サンプを求めよ．
サンプリングは通常周期的に行われる．
すなわち連続時間信号［定理7］ が仮定2を満たすとき離散時間 の1点 に対してただし，Λは以下を満たす複素行列解をもつための必要十分条件は，以下の～が成立することを満たす を求める（連続時間 設計）．
の場合A-D変換器のモデルとして ＝としたという．
ンプリング後に区別することはできない．
この現象をエリアシングを理想サンプリングした離散時間信号をそれぞれ， とするばれる．
エリアシングの影響を小さくするため，A-D変換タと呼ばれる．
ただし， ×0,である［通常# ＝# ］．
の を零次ホールド（zero-orderb.解析と設計の枠組 以下では連続時間制御系の設計の枠組連続時間信号をサンプリングしたとき，角周波数がω＋ωエリアシングはサンプル値制御系の取扱いを困難とし，性能に悪（2･4節参照）の補償器をの理想サンプラ，線形時不変2･77のサンプル値制御系を考える．
図2･77のサンプル値制御系(2･335)で与えられる連続時間有限次元線形時不変システムであり， の状態空間表現をで表す．
［補題2］ ≠0，≠0とする．
≠0ならばサンプル値制換えたシステムであるから， の入出力も連続時間信号である．
償器 を求め， が を近似するように を定める．
定める（タスティン変換，Tustin transformation）ことが多い．
が， は周期システム（周期は）である．
エリアシングのため離散時間時不変システムの手法を直接適用することはできない．
設め， に対して離散時間制御系設計手法を用いて を定める．
て系の安定性が確保されるとは限らない．
設計手順はサンプルc項以後 から を直接設計する方法を示すが，まず準備としc .サンプル値制御系の時不変モデル サンプル値制御系の解（lifting）により時不変モデルに変換する手法がある．
リフティンである．
すなわち，連続時間信号 に対して関数値をとる離散時で定めるとき， から への変換をリフティングという．
∈ならば∈ 0,かつ∈であり，さらに，ることはないことを示している．
時間システムに変換される．
時間システムとしてのモデルを与えている．
よって， 変換を用いを求めよ．
を内部安定化する．
を最小化する．
て定義することができる．
仮定3のもとで内部安定性と 安定性着する．
ただし離散時間システム は以下で定義される．
(ａ) λ＝ λ≧0を満たさない．
は以下を満たのいずれかが成立するならば，,の可安定性は,のく．
ただし，ωはサンプリング角周波数である．
