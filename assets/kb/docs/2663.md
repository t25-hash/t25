# 3・4・5 システムの信頼性

> **章**: 第3章 設計のための個別方法論
> **節**: 3・4 信頼性の設計

考えられるが，他の方法として，一機能に対して複数の要素を併用し，その一つが故障しても全体としての機能を維持できるようにすることがある．
このような考え方のシステムを冗長システムと呼ぶ．
その構成方法には，並列システム， 冗長システム，待機冗長システムなどがある．
並列システムは，信頼性ブロック図では図3･70のように表される．
この場合，すべての要素が故障したときにシステムが故障となるので，各要素の不信頼度を （＝1,…,）とすると，システムの信頼度は以下の式で表される．
の信頼度を とすれば，直列システムの信頼度は要素の信頼度の冗長システムは， 個の要素中 個が正常に動作していればシステムが正常に動作するものである．
すべての要素が同一信頼る．
待機冗長システムは，主要素が故障した場合に待機している予備って，冷予備，温予備，熱予備に分類される．
冷予備は，待機中のるが，機能的に接続されていない場合である．
また，温予備は，待る．
解析目的に応じて選択する必要がある．
うなシステムの属性を一般にフォールトトレランス（fault toler-ードウェアを冗長化するだけでなく，故障発生時に要素の機能関係法を機能冗長と呼ぶ場合がある．
また，故障時に機能の一部でも維これに類似した用語にフェイルセイフがあるが，これは故障発生時に停止するなどしてたとえ信頼性を下げてでも安全性を確保するここれまでは，一つのアイテムの故障について考えてきた．
しかし，一般に機械は複数の要素からなる．
そこで，これらの要素の信頼性と，それが組合わさったシステムの信頼性との関係を考える必要がある．
a.直列システム（series system) 直列システムは，システムを構成しているどの要素が故障してもシステムの故障となるものであり，図3･69のように表される．
信頼性工学の分野では，このような要素の機能的な関係を示す線図のことを信頼性ブロック図積となる．
度 のとき， 冗長システムの信頼度は以下の式で与えられの要素が任務を引き継ぐシステムである．
待機中の予備の状態によ要素が作動状態にない場合で，熱予備は，待機要素が作動状態にあ機中に動作に必要な一部のエネルギーの供給を受けている場合であシステムに冗長性をもたせる目的は，要素に故障が発生してもシステムとしての機能を保持できるようにすることであるが，そのよance）と呼ぶ．
システムをフォールトトレランスにするには，ハを再構成して要求機能を維持するという方法もある．
このような方持し動作を続行しようとすることをフェイルソフトという．
