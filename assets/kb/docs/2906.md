# 4・1・3 一次元データの要約

# 4・1・3 一次元データの要約

> **章**: 第4章 測定データの処理
> **節**: 4・1 静的データの処理

測定データは，(１)測定対象が本質的にもっている分布，(２)測
定環境の変動，(３)測定機器や技術上の問題に起因する誤差変動な
どにより，ある広がり（分布）をもつのが普通である．したがっ
て，測定データを要約する値としては，分布の位置を表す代表値の
ほかに，分布の広がり（散布度）を表す要約値があり，さらに必要
な場合には，分布の形状［ひずみ（skewness ）や尖り（kur-
tosis ）の度合い］に関する要約値を用いることもある ．
a.分布の位置を表す要約値（代表値）
ue）と呼び，データの個数を ，計測値を （＝1,…,）とした
とき，次式で計算する．
(4･1)
＝＋＋…＋
＝
∑
平均値を求める際，除算によってはるか下のけたまで数値が得られ
ることがあるが，それをそのまま示すのは，意味がないだけでな
く，誤解を生むもととなるため，4･1･2項に述べたように，数値を
適切に丸めて表示したい．一般に，平均値 の分布の広がりは，
元の分布の1になるため，サンプル数の平方根に比例してけ
た数を増やしていく（丸めの幅を縮める）のが合理的であろう．こ
のような背景から，JIS Z9041-1：1999「データの統計的な解釈
方法―第1部：データの統計的な記述」では，表4･1のような規定
が設けられている．
.メディアン 例えば， 個のデータ （＝1,…,）を
小さい順に並べ換え，それらを （＝1,…,）と表したとき，
次式で求められる値をメディアン（median）または中央値と呼ぶ．
＝(が奇数の場合） (4･2･ a)
＝＋
2 (が偶数の場合） (4･2･ b)
すなわち，メディアンは，それより小さい測定データの数とそれよ
り大きい測定データの数が，ちょうど等しく50 %となるような代
(4･3)
偏分散（標本分散）と呼ばれ，検定や推定（4･1･7項参照）に利用
－1 (4･4)
本標準偏差 を単に標準偏差と呼び，その値を用いるのは，分
.平均値 測定データの算術平均を平均値（average val-
表値であり，例えば，極端に値が離れた少数のデータ（外れ値）に
それほど影響を受けないという頑健性（robustness ）を備えている
ことから，特に分布がゆがんでいるような場合の代表値として適し
ている．
b.分布の広がりを表す要約値（散布度）
.分散 平均値から各測定値までの距離（偏差）は，分布
の広がりを表す一つの目安であり，次式で計算される偏差の二乗平
均を分散（variance）という．
＝
∑－
一方，測定データがある母集団から抜き取られたサンプル（標
本）である場合，計算で得られる分散の値も，ある分布をもつ．次
式で求める は，その分布の期待値が測定データの母分散と同じ
になるように，偏差の二乗和を自由度－1で除したもので，不
される重要な統計量である．
∑－
＝
一般に，単に分散といえば，不偏分散を指し，要約値としても，式
(4･4)を用いて計算することが多い．
.標準偏差 標準偏差（standard deviation）は分散の正
の平方根であり，測定データと同一の単位（次元）をもつことか
ら，直観的に把握しやすく，分布の広がりを表す特性値として最も
よく用いられる．特に断りがなければ，式(4･4)から求められる標
表4･1 平均値のけた数
測定値の測定単位 測定値の個数
0.1，1，10などの単位 ― 2～20 21～200
0.2，2，20などの単位 4未満 4～40 41～400
0.5，5，50などの単位 10未満 10～100 101～1000
平均値のけた数 測定値と同じ 測定値より
1けた多く
測定値より
2けた多く
散の場合と同じである．JIS Z9401-1：1999では，標準偏差は有
分の1とすることとしている．
効数字3けたに丸めることになっている．
.範囲 標準偏差は，測定データ全体の平均的なばらつき
を表現する特性値であるが，データ全体が収まる範囲（range）を
示すほうが良いときもある．これは，材料の表面粗さ（凹凸の程度
式(4･8)を利用することがある．
に対する要約値）を表すのに，算術平均粗さ だけでなく，最大
＝1＋log
高さ を用いることがあるのと似ており，式(4･2)と同様の記号
を用いれば次式で表される．
＝－ (4･5)
範囲 は，わかりやすく，かつ簡便な要約値であるが，外れ値
に敏感である．そのため，範囲を表す別の方法として，±2また
は±3の範囲を示すこともある．これは，測定データが完全な正
規分布に従うとき，平均値を中心としたそれぞれの範囲に入る確率
線で結んで表示する．
が，前者では約95 %，後者では99.7 %になるという性質を反映し
たものである．
c .分布の位置と広がりの両方を用いた要約値
.変動係数 標準偏差を平均値で除した値を変動係数
（coeﬃcient of variation）と呼び，次式で計算する．
＝ または (4･6)
変動係数は，代表値に対する散布度の程度を表しており，管理の正
確さを表す指標としてもよく用いられる．例えば，ねじの締付け力
別な違いはない．
をトルクで管理しようとする場合，摩擦変動が問題となるが，管理
の精度を左右するのは，摩擦係数のばらつき（散布度）そのもので
はなく，変動係数のほうである．また，寸法などの特性は，基準寸
法が大きくなるとばらつきも大きくなる（または公差を大きくでき
る）という傾向にあることから，その評価にも変動係数を用いるの
が適している．
.平均と範囲 代表値と散布度を組合わせれば，測定デー
タ全体の様子を簡潔に示すことができ，ひずみの小さいデータに対
しては，±2や±3などの表現が有効である．この場合に
は，有効けた数を小さいほうにそろえて表示する．
のもつ性質を定量的に表すものであるが，全体の傾向を把握するた
めには，データの度数分布を表示するのがよい．以下では，その代
表例であるヒストグラムおよび累積度数図について説明する．
を，幅 の区間に区切り，それぞれの区間に入るデータの度数
を，長方形の柱の高さとして図示したものをヒストグラム（histo-
gram）という．縦軸を相対度数で表せば，ヒストグラムの形は，
ータのもつ分布のおおよその形を表すことができる．JIS Z9401-
1：1999では，区間の幅 は，測定値の範囲 から，区間の数
が5から20程度になるように決め，級の境界を最小測定単位の2
標準偏差 2.31 0.0106
一方，データ数または区間の数が少なすぎれば，分布の形を正し
く表すことはできない．そこで，データ数 と区間の数 の関係
を決める目安として，式(4･7)で示すスタージェスの公式 または
log21＋3. 32･ log (4･7)
＝ (4･8)
.累積度数図 図4･1の折れ線で示される累積度数図は，
測定値がある値以下となる割合を表したもので，累積分布関数
と対応する．通常，各区間の上側境界値に対応する累積相対
度数（cumulative relative frequency）をプロットし，それらを直
b.データの近似（分布関数による表現) 度数分布などで表
されるデータ群を単純な（きれいな）関数形で近似し，その数学的
な性質を利用したい場合には，確率プロット（probability plot ）
を利用する．なお，一般にデータは，ある母集団から抜き取られた
サンプルである場合が多く，その場合には，母集団の分布形を推定
することが重要であるが（4･1･7･ a項参照)，両者の違いは，単に
母集団（サンプルを無限に集めたときの度数分布の極限値）の存在
を想定するか否かという点だけであり，当てはめの行為自体には特
.正規分布の当てはめ 正規分布は，平均値および分散と
いう二つのパラメータによって記述される左右対称の分布であり
［式(4･19)参照］，図4･1のような場合には，データの平均値 お
よび分散 をそのまま利用して近似できるが，データ数が少なく
度数分布が描けないような場合には，正規分布に当てはまるかどう
かがわからないため， と の関係が直線となるような座標系
にデータ,をプロットし，そこからμとσを決める．正
規分布は，＝－μσの変換を施せば，平均値0，標準偏差1の
標準正規分布となるため，プロットには が等間隔になるような
目盛を用いればよい．
