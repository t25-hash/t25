# 4・2・3 周波数解析

> **章**: 第4章 測定データの処理
> **節**: 4・2 動的データ処理

計測された信号にどのような周波数成分が含まれているかを調べ（spectrum analysis）といい，動的データ処理の基本となるべきもa.フーリエ変換 周期 をもつ時系列信号 は次式のフれぞれ周波数＝2πにおける振幅と位相角を表す．
無限に続form) を定義する．
複素量ωの複素振幅ωを振幅スペクトル（amplitudeび，角周波数ωの成分の振幅，位相を表す．
振幅，位相スペクトにより時間信号 を再生できる．
FFTの計算量は2logとなる．
このため，周波数解析にはは，まずアナログ信号の計測データのA- D変換を行って標本化す標本化周波数（sampling frequency） との間に，次式を満足しこれを標本化定理（sampling theorem）と呼ぶ．
標本化定理が成できない．
（＝0～31）を図4･20に示す．
周波数スペクトルは最大値奇数次の周波数成分が無限に続くことになる．
これに対応して図が得られているが，本来存在しないその定理が成立していないために生じたエリアシング誤差である．
このの前段階でアナログ低域通過フィルタによる高周波成分の除去を行間を基本周期としてその整数倍の周波数スペクトルを求めるよう定なわち区間の始点と終点のデータ値が異なっている場合，誤差を生…,－1）を乗じて強制的に周期性を付与して解析を行う．
この重れる窓関数を表4･4に示す．
表4･4の中の は窓の幅を表し，標ある．
例として，以下の計測信号関数を用いてFFT解析を行った結果を図4･21に示す．
矩形（rec-検出できており，周波数分解能が高いことがわかる．
ning）窓のみ検出できており，振幅の小さな周波数成分の検出にることを周波数解析（frequency analysis）またはスペクトル解析のである．
ーリエ級数（Fourier series）に展開できる．
をフーリエ変換すれば，次式が得られる．
図示すれば図4･19となる．
取扱うことはできないので，有限時間内において一定間隔で標本化る．
離散フーリエ変換および離散フーリエ逆変換（IDFT:inverse， はともに一般に複素量である．
るため，標本数が多くなると計算時間が膨大となる．
この問題を解である．
標本数を基底2のべき乗，すなわち＝2とする．
計算れる同一の計算を除外して演算回数を大幅に減じることができる．
一般にFFTを用いる．
この際，計測データに含まれる上限の周波数を とすれば，なければならない．
立していなければ，周波数解析結果から原信号の計測データを再生じる．
これをエリアシング誤差（aliasing error）という．
例として，3周期の矩形波信号 （＝0～63）のFFT結果に対して正規化してデシベル表示している．
原信号のフーリエ級数4･20の3周期の解析結果では＝32－1 （＝1,2, …）の成分他の成分は，高次成分に対して標本化ような誤差を防ぐためには，一般にデータの標本化（A- D変換）う．
このようなフィルタをアンチエリアシングフィルタ（anti -c .窓関数を用いた周波数解析 離散フーリエ変換は有限の区義されている．
したがって，その解析区間に周期性がない場合，すじる．
この誤差を防止するために，原信号に重み （＝0,1,みを窓関数（window function）と呼ぶ．
このように，目的に合わせて窓関数を選択する．
一般的にはハニング窓を使用するとよい．
