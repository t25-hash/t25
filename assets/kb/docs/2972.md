# 4・1・1 連続系のMRACS

> **章**: 第4章 適応・学習制御
> **節**: 4・1 モデル規範型適応制御系（MRACS）

テムの強正実性とリアプノフの方法から導出されるのに対して，後者をベースにしたものは正規化手法を併用した最小二乗，最小二乗平均，指数重み付き最小二乗平均，ハイブリッド型などの各種こう配法から導出される．
以上のような分類の組合せによってさまざまなMRACSが考えられるが，実現可能で，かつ，構成上意味をもつのは表4･1における．
以下では，次数，相対次数 の1入力1出力プラントを考え，前述の仮定が満たされている場合のMRACSの基本構成法を述べる．
a.プラントの表現と制御目的 プラントの入力を，出力を，ラプラス演算子を として入出力間の伝達関数 をmodel matching）の制御器と同じ構造になっているので，プラントの次数や相対次数があらかじめ既知でなければならない，また，保証する観点からプラントが非最小位相系であることが許されなム外乱や観測雑音が存在してはならない．
これらの仮定は非常に厳で現実には，次数や相対次数のミスマッチ，微小なむだ時間，外乱が漸近的に許容範囲内におさまるようなMRACSの構成法が必要参照）．
ラメータの推定値に基づいた設計計算を経て制御器の可調整パラメ方式では，制御入力の合成方法と適応機構の設計法がいくつか存在存在する．
を満足するベクトル信号である．
～ に依存した未知パラメータである．
を必要とする方法と必要としない方法に細分化される．
と入力表現に基づく場合に分けられる．
入力モデル型 ― ― ― ―則 出力モデル型 ― ― ― ―入力モデル型 ― ― ― ―れる．
して次のを用いる．
存した未知パラメータである．
は， を安定かつモニックある．
相遅れを補償して安定度を改善するための非線形減衰項である．
なtion＋integration）とした構成法と等価である．
個別に設計する．
入力合成則はで＝0としたものにる．
以下の構成法DDS1～DDS4では，CE原理に基づく構成で問題とは，出力誤差 から推定パラメータの変化率に依存した成分を量 を用いることなく 番めの推定パラメータαまたはtuner）なる適応則が必要になる．
と割り算を含むDyCE原理に基づく入力合成則から構成される．
はλ＋λの最小実現である．
と割り算を必要としないDyCE則から構成される．
構成法のμの値は，それぞれμ＞μλ，μ＞μλ，μ＞μλ，である．
計算 が行われる．
適応則：以下の を最小化するこう配法情報を利用し射影アルゴリズム（projection algorithm）を適応則以上の構成法における応用的な適応則やMRACSの安定性なる．
が未知あるいは特性が途中で変動するプラントを制御するための制御系の一つで，プラントパラメータに不確かさがあっても任意の参照入力に対して制御系の特性を良好に保持できる特徴を有する．
MRACSの制御器は，制御系の出力（プラント出力，制御量）が規範モデルの出力（目標出力）に漸近的に一致するように適応的に構成される（ほとんどの基本構成は図4･1のようになる）．
実際の適応は，制御器の可調整パラメータが適応機構でオンライン調整されることによって行われる．
ここで規範モデルとは，参照入力から制御量に至る制御系の望ましい特性を記述したモデルで，通常，速応性や減衰特性などを考慮して決定される．
漸近安定なMRACSを実現するためには，一般的に以下の仮定が必要になる．
制御入力合成部が完全モデルマッチング（exact制御器はプラントの零点を相殺する働きをもつため，その安定性をい．
さらに，適応機構が正しく動作するためには，制御系にシステしいもので，すべてが満たされることは現実にはあり得ない．
そこや雑音などの不確かさが存在しても安定に動作し，かつ，追従誤差であり，これはロバスト適応制御法として知られている（4･1･3項応則によって直接調整する直接法と，適応則が生成するプラントパータが間接的に調整される間接法に大別される．
さらにそれぞれのし，目的に応じた組合せによってさまざまなタイプのMRACSが御器と同じであるが制御器の可調整パラメータの最適値が未知であとする．
る安定な適応制御系を構成することである．
次の安定かつモニックな設計多項式を およびδ （δ：一意に定まる．
このとき，プラントは次のように表現できる（なも可能である）．
く方法に分けられる．
取り除いたサブベクトルである．
な 次の設計多項式として，(4･12)を満たすベクトルで以下では，＝＋λ，λ＞0として(4･14)を用いる．
性を利用して，リアプノフの方法（Lyapunov method）によって組合せを同時に設計するもので，後述する構成法DCS2と比べるて済む利点を有する．
ただし，＝ε，＝＞0は適応ループの位お，DCS1は＝0として適応則を比例＋積分型（propor-.構成法DCS 2 この構成法では，制御入力と適応則を等しいが，適応則は以下の正規化こう配法アルゴリズムが使われloop）の位相余裕が90°回復する．
なる適応初期の過渡現象がSMCの導入によって改善される．
はλ＋λ の最小実現である．
算型のDyCE則から構成される．
はλ＋λ の最小実現である．
算を必要としないDyCE則から構成される．
－β0，,,はλ＋λの最小実現，また，各μ＞μλを満たすように与える必要がある．
ただし，.構成法ICS 1 これはCE原理に基づくMRACSを間接特性の概略が推定値から把握できる利点を有する．
構成法DCS2と同じ (4･46)として表されるが，この関係が推定値でも成り立つものとして設計.構成法IDS 1 これはDyCE原理に基づくMRACSものである．
(注意) 制御入力合成時に割り算を含む構成法では， の下限に併用して零割りを防止する処置がとられる．
