# 1・2・1 状態遷移行列

> **章**: 第1章 システム解析
> **節**: 1・2 線形システム論

状態方程式で表される系の解の挙動を調べることにする。それは基本的には状態が時間経過につれてどのように変化していくかがわかればよい。それを表現するのが状態遷移行列(state transitionmatrix)である。特に対象系が時間的に性質を変えない[これを時不変系(time invariant system)という]とき、状態遷移行列は行列指数関数(matrix exponential function)になる。状態方程式の解は、状態遷移行列(または行列指数関数)を用いて表現することができる。右辺は任意のについて収束するので、この定義は可能であること、および行列指数関数は×行列となることに注意する。ただし、は逆ラプラス変換を表す。また、性質(6)から行列の固有値がのラプラス変換の極となることがわかる。さらに詳しくは、文献(19)(20)などを参照されたい。このとき。このことと定理1の(4)を組合わせると、行列指数関数の別の計算が可能である。これは例1の計算結果と一致している。b. 時不変系の解行列指数関数を用いると、線形時不変系の解を表現することができる。実際に定理1の(1)(2)を用いると、式(1・69)のは初期条件のある微分方程式(1・68)を満たしていることがわかる。行列指数関数を状態遷移行列ともいう。式(1・71)の第1項は、初期条件に依存する項であり、零入力応答(zero input response)と呼ばれる。第2項は、入力に依存する項であり、零状態応答(zero state response)と呼ばれる。このような正則行列による変換があるとき、式(1・72)(1・73)と式(1・75)(1・76)は同値であるといい、そのような変換を同値変換という。また式(1・75)(1・76)の伝達関数は、式(1・74)となり、同値変換によって伝達関数は変わらないことに注意する。d. 線形時変系の解状態遷移行列を用いると、線形時変系の解を表現することができる。e. 非線形系の解軌道に沿った線形近似時変線形系の枠組が必要となる一つの例として、非線形系の解軌道に沿って線形近似を行う場合がある。次の状態方程式で表した非線形系を考える。係数行列は式(1・85)(1・86)に示すように、非線形系(1・81)(1・82)が時不変系であっても一般には時間に依存する。つまり線形近似系は線形時変系になる。特に、0,0=0である場合を考える(平衡点をもつ系の場合、一つの平衡点を原点にとることに対応する)。
