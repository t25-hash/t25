# 1・4・5 ノンパラメトリックモデル同定法

> **章**: 第1章 システム解析
> **節**: 1・1 にまとめた。特に、ロバスト制御の登場によって、制御対象の

a. 相関法インパルス応答を推定する同定法である相関法(correlation method)の手順を次にまとめる。(2) 白色化フィルタを設計し、入出力データをフィルタリングする。◎現場で最も利用されている同定法の一つである。×得られる同定結果は限定的である。×外乱や測定雑音に弱い。×同定入力の振幅が同定精度を左右する。過渡応答法◎むだ時間、時定数、定常ゲインなどに関する情報を直接同定できる。◎特別な入力を必要としない。◎SN 比が悪くてもデータ数が多ければ大丈夫。×入力と外乱とは独立でなければならない。→閉ループデータは利用できない。相関法◎利用が容易である。◎システムに仮定される条件は線形性のみ。◎着目する周波数帯域を重点的に同定できる。×低周波の同定に時間がかかる。→同定対象へ負担がかかる。周波数応答法◎システムに仮定される条件は線形性のみ。◎外乱のスペクトル密度も同時に推定できる。◎スペクトル推定に関するさまざまなツールが利用できる。×窓長の選定が必要。×入力と外乱とは独立でなければならない。→閉ループデータは利用できない。スペクトル解析法フーリエ解析法(ETFE)◎利用が容易である。×入力が純粋な正弦波でないと、推定精度が大きく変動する。報を直接同定できる。一方、入力と外乱とは独立でなければならないので、直接、閉ループシステム同定データは利用できない。b. スペクトル解析次に手順を与えるスペクトル解析法(spectral analysis method)を用いると、システムの周波数特性のみならず、外乱のパワースペクトル密度も直接推定することが可能になる。例えば、次のようにすればよい。(3) 周波数伝達関数と外乱のスペクトルの推定値を、それぞれ次のように計算する。一方、相関法と同様に入力と外乱とは独立でなければならない。ノンパラメトリックモデルの同定法について表1・4にまとめた。
