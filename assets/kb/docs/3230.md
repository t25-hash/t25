# 1・4・2 項で与えた多項

> **章**: 第1章 システム解析
> **節**: 1・1 にまとめた。特に、ロバスト制御の登場によって、制御対象の

式ブラックボックスモデルに対するシステム同定問題は、ひとたびモデル構造が決まれば、モデルを構成するパラメータの推定問題に帰着する。そこで、パラメータ推定のための評価規範として、,,ε,(1・141)=1∑요を設定する。ここで、,,ε,は予測誤差の大きさを測る任意の正のスカラ値関数であり、関数,,ε,としてどのような測度を選ぶかは、同定結果の利用目的に依存するが、通常、6―23二乗ノルムや対数尤度などが用いられる。未知パラメータの推定値[とする]は、=arg min(1・142)より決定される。ここで、arg minはを最小にするという意味である。このように予測誤差から構成される評価規範を最小にするように推定値を計算するパラメータ推定法を総称して、予測誤差法(PEM : prediction error method)と呼ぶ。例えば、式(1・141)中の,,ε,として、,,ε,=-log ε,(1・143)を選んだ場合を最尤推定法(maximum likelihood estimationmethod)という。ただし、は予測誤差ε,の確率密度関数を表す。一方、,,ε,として、二次関数,,ε,=ε,(1・144)と選んだ場合を最小二乗法(least square method)という。さて、予測誤差系列が平均値零で分散σの正規性白色雑音の場合を考えよう。このとき、式(1・143)は次式になる。,,ε,=12σε,+12 log2πσ(1・145)式(1・145)の右辺第2項は定数なので、予測誤差が正規性白色雑音系列の場合には、,,ε,=ε,(1・146)に対する評価規範を最小にする推定値を計算することになる。したがって、この場合の最尤推定値は最小二乗推定値に一致する。b. 線形回帰モデルの場合。 最小二乗法ARX モデルやFIR モデルのような線形回帰モデルの場合、パラメータ推定のための評価規範はε,=1∑요-=1∑요=-2+となる。ただし、=1∑요=1∑요=1∑요式(1・147)のをに関して微分してとおくと、正規方程式(normal equation)=(1・147)が得られる。このとき、次の条件が満たされる場合、行列は正定値になる。(1) 同定入力が2次のPE性である。ただし、はシステムの次数である。(2) 同定対象は安定である。(3) とは共通因子をもたない。これらの条件が満たされる場合には、逆行列を用いて式(1・148)のようにパラメータ推定値を求めることができる。=(1・148)この同定法は一括処理最小二乗法、あるいはオフライン最小二乗法と呼ばれる。. 補助変数法式誤差が白色性でない場合にARX モデルに対して最小二乗法を適用すると、推定値にバイアスが生じてしまう。この問題に対処する代表的な方法を表1・5にまとめた。表1・5に示した中の補助変数法(IV 法:instrumental variablemethod)について簡単に紹介しよう。IV 法では、入力をフィルタリングすることによって、信号を生成する。すなわち、6―24制御システム表1・5 有色性雑音への対処法白色化に基づく方法無相関化に基づく方法拡大最小二乗法(ELS)同定法一般化最小二乗法(GLS)補助変数法(IV)逐次最尤法(RML)=そして、式(1・128)のむだ時間を含むARX モデルに-,=1,2,...,と-+1-,=1,2,...,を乗じ、に関して総和をとる。すると、式(1・128)中の雑音は無相関化され、システムの伝達関数について解くことができる。このとき、は補助変数と呼ばれる。補助変数法は非常に有力なシステム同定法であるが、無相関化に基づいているため、相関法などと同様に、一般に閉ループ同定実験データに適用できない。. 予測誤差法(一般的な多項式ブラックボックスモデル)予測誤差法の特殊な場合である線形回帰モデルのときには、予測誤差が未知パラメータに関して線形であったので、式(1・148)のように逆行列演算によってパラメータ推定値を計算することができたが、非線形回帰モデルの場合には、なんらかの数値的探索法によって-ε,=∑요=1∑요の最適点を求めることになる。したがって、予測誤差法ではdd=(1・149)を解くことによって、最小値を求めることができる。式(1・149)に対して、例えばニュートン・ラフソン法を適用すると、=-μ′′′(1・150)が得られる。ここで、のに関する2階微分であるはヘシアンと呼ばれる。. 逐次パラメータ推定これまでは多項式ブラックボックスモデルに対する一括処理形式のパラメータ推定法を与えた。本節では逐次パラメータ推定(recursive parameter estimation)、適応パラメータ推定、あるいはオンライン推定と呼ばれる方法について述べる。まず、逐次最小二乗法(RLS 法、recursive leastsquare method)のアルゴリズムを以下にまとめる。(1) 初期値パラメータ推定値と共分散行列の初期値をそれぞれ次のように設定する。0=0=γ、ただし、γ:正定数(2) 時間更新式それぞれの時刻において次の式を計算する。=-1+-1λ+-1εε=--1=1λ -1--1-1λ+-1ここで、λは忘却要素と呼ばれる1以下の正数であり、0.97~0.995くらいにとられることが多い。さらに、逐次パラメータ推定アルゴリズムの一般形は、次のように与えられる。(逐次パラメータ推定アルゴリズムの一般形)=-1+ε(1・151)ε=-(1・152)ここで、は時刻-1までに入手可能な観測値に基づいた出力の1段先予測値である。また、ゲインは現在の予測誤差をパラメータ推定値の更新にどのくらい影響させるかを決定するものであり、通常次のように選ばれる。=(1・153)ただし、は1段先予測値のに関するこう配(あるいは、こう配の近似)である。
