# 1・4・2 項で与えた多項

> **章**: 第1章 システム解析
> **節**: 1・1 にまとめた。特に、ロバスト制御の登場によって、制御対象の

式ブラックボックスモデルに対するシステム同定問題は、ひとたびモデル構造が決まれば、モデルを構成するパラメータの推定問題に帰着する。さて、予測誤差系列が平均値零で分散σの正規性白色雑音の場合を考えよう。このとき、式(1・143)は次式になる。したがって、この場合の最尤推定値は最小二乗推定値に一致する。b. 線形回帰モデルの場合。(1) 同定入力が2次のPE性である。ただし、はシステムの次数である。(2) 同定対象は安定である。これらの条件が満たされる場合には、逆行列を用いて式(1・148)のようにパラメータ推定値を求めることができる。. 補助変数法式誤差が白色性でない場合にARX モデルに対して最小二乗法を適用すると、推定値にバイアスが生じてしまう。この問題に対処する代表的な方法を表1・5にまとめた。表1・5に示した中の補助変数法(IV 法:instrumental variablemethod)について簡単に紹介しよう。すると、式(1・128)中の雑音は無相関化され、システムの伝達関数について解くことができる。補助変数法は非常に有力なシステム同定法であるが、無相関化に基づいているため、相関法などと同様に、一般に閉ループ同定実験データに適用できない。. 逐次パラメータ推定これまでは多項式ブラックボックスモデルに対する一括処理形式のパラメータ推定法を与えた。本節では逐次パラメータ推定(recursive parameter estimation)、適応パラメータ推定、あるいはオンライン推定と呼ばれる方法について述べる。まず、逐次最小二乗法(RLS 法、recursive leastsquare method)のアルゴリズムを以下にまとめる。(1) 初期値パラメータ推定値と共分散行列の初期値をそれぞれ次のように設定する。さらに、逐次パラメータ推定アルゴリズムの一般形は、次のように与えられる。また、ゲインは現在の予測誤差をパラメータ推定値の更新にどのくらい影響させるかを決定するものであり、通常次のように選ばれる。
