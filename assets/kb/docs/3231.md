# 1・4・7 部分空間法

> **章**: 第1章 システム解析
> **節**: 1・1 にまとめた。特に、ロバスト制御の登場によって、制御対象の

ホー(Ho)とカルマン(Kalman)によって提案されたインパルス応答からの最小実現は確定的な方法であったため、雑音に汚された実データに適用することは難しかった。この問題点に対して、1970年代後半、クング(Kung)は特異値分解という手法を使うことによって、信号成分と雑音成分を分離する方法を提案した。ここではクングの方法を特異値分解法と呼び、その手順を次にまとめる。(1) 対象のインパルス応答を、次に列挙する方法のいずれかを用いて求める。・インパルス信号を対象に加え、直接インパルス応答を測定する。・ステップ信号を対象に加え、ステップ応答を測定し、それを差分することによりインパルス応答を得る。・対象の入出力データからインパルス応答を推定する。例えば、i. 相関法ii. FIR モデルを用いた最小二乗法(2) インパルス応答列からハンケル行列を構成する。(3) ハンケル行列を特異値分解する。そして、特異値の大きさを参考にしてシステムの次数を選ぶ。(4) 最小実現に基づいて状態空間モデルを計算する。(5) 伝達関数が必要な場合には、次式より計算する。それに対して、部分空間法(subspace method)は通常の入出力データから状態空間モデル(state space model)を同定できる有力なシステム同定法である。したがって、システムの入出力関係を記述する任意の行列,を推定することが、ここで考えている同定問題である。すると、式(1・156)は次のように変形できる。以上では、状態変数ベクトルが測定可能としてきたが、現実にはこれを推定しなければいけない。この問題に関しては、すべて表1・6パラメトリックモデルの同定法同定法(モデル)特徴(◎は利点、○は性質、×は問題点)◎線形回帰モデルに基づく方法なので、逆行列演算で推定値が計算できる。○高周波帯域に重みがかかった同定法である。×同定モデル次数を高くとる必要がある。×式誤差が有色性の場合には推定値にバイアスがのる。最小二乗法(ARX)拡大最小二乗法(ARMAX)◎式誤差が有色性であっても、システムの動特性を同定できる。×雑音モデルを準備する必要がある。補助変数法(ARX)◎式誤差が有色性であっても、システムの動特性を同定できる。◎必要以上にモデル次数を高くとらなくてよい。×入力と外乱とは独立でなければならないので、閉ループ同定実験データには適用できない。◎システムの動特性と雑音モデルを分離して同定できる。◎時間応答シミュレーションに適している。◎同定法は全帯域にわたって平坦な周波数特性をもっている。×非線形最適化計算を行う必要がある。出力誤差法(OE)出力誤差法(BJ)◎システムの動特性と雑音モデルを分離して同定できる。◎時間応答シミュレーションに適している。×非線形最適化計算を行う必要がある。◎多変数系へ容易に拡張できる。◎非線形最適化演算を含まない。◎ユーザが設定するパラメータが比較的少ない。×雑音に弱い場合がある。ここで、はモデルの次数(状態変数の次元)である。すると、これらの予測器を構成することができ、これらの成分の中から基底として次を選ぶ。以上のような考え方に基づく多入出力システムの同定法を部分空間法と総称する。フィルタとしてどのようなものを選定するか、どのような数値計算アルゴリズムを用いるかによって、さまざまな部分空間法に分類できるが、ここではその詳細については省略する。部分空間法の特徴をまとめると、次のようになる。(1) 部分空間法は入出力データからシステム同定を行うことができるため、不安定系や閉ループ系などへも適用することが可能である。(2) SVD やQR 分解などの数値的に安定なアルゴリズムを利用しているため、計算精度が高い。(3) 多項式ブラックボックスモデルに対する予測誤差法のように、非線形最適化計算を行うことなくシステム同定が行える。(4) 多入出力システムへの拡張が容易である。パラメトリックモデルの同定法について表1・6にまとめた。
