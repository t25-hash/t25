# 2・2・1 項で述べた最適レギュレータなどの状態フィードバック制

> **章**: 第2章 線形制御
> **節**: 2・1 項で述べる最適レギュレータのほかに、閉ループ極を指定され

御は、その実現のためにすべての状態を直接測定する必要がある。しかし、現実の多くのシステムではそれは難しい。このような状態の推定機構には、確定的な環境のもとで状態を推定するオブザーバ(observer)と、統計的性質のわかった外乱や観測雑音が加わる確率的な環境のもとで状態を最適推定するカルマンフィルタ(Kalman filter)とがある。さらにオブザーバには、すべての状態を推定する全次元オブザーバと、出力情報を利用することにより推定すべき状態の数を最小に減らした最小次元オブザーバなどがある。以下ではこれらの状態推定器を順に説明する。a. 全次元オブザーバ(full-order observer)線形システム(2・36)(2・37)の状態ベクトルを推定しよう。=この式から明らかになるように、この方法は、(1)安定系にしか使えない、(2)推定誤差の収束の速さが変えられない、という原理的な欠陥を含んでいる。式(2・49)の推定器は、システム(2・36)と同次元であることから、全次元または同次元オブザーバと呼ばれ、修正ゲインはオブザーバゲインと呼ばれる。b. 最小次元オブザーバ(minimum-order observer)線形システム(2・36)の個の状態は、その一部もしくはその線形結合が式(2・37)の出力として直接測定できる。この考え方に基づき次元を最小化したオブザーバを最小次元オブザーバという。以上よりシステム(2・36)(2・37)の状態を推定する最小次元オブザーバは、式(2・54)~(2・56)をまとめた式(2・62)(2・63)で表される。その構成手順を以下にまとめておく。ただし、システム(2・36)は可観測であり、rank =と仮定する。手順1:式(2・64)の行列が正則になるように行列を定める。手順2:状態方程式(2・51)(2・52)の係数行列を次のように定める。手順4:オブザーバの係数行列,を式(2・59)~(2・61)で定める。手順5:式(2・62)と式(2・65)で記述される最小次元オブザーバを構成する。また、それにより推定器の次元を低くできる。このとき、オブザーバの定義より定常状態ではに一致するから、定常状態に限れば等価的に状態フィードバックが実現できたことになる。次に、オブザーバを用いてこのように実現された制御系全体の性質を調べてみる。簡単のため全次元オブザーバ(2・49)を用いた場合を考察する。まず式(2・36)(2・48)(2・66)と式(2・50)より容易に次式を得る。これは状態フィードバックゲインとオブザーバゲインが独立に設計できることを意味し、制御と推定の分離と呼ばれている。白色雑音とは、時間的に無相関な信号であり、そのスペクトル密度が白色光のように全周波数帯域で一定になることからこのように呼ばれるが、現実には存在しない理想化された信号である。この推定問題は、もともとカルマンが統計的性質をもつ信号が白色雑音を入力とする線形動的システムの出力として表現できる点に着目し、(1)その生成過程を上記の状態空間モデルで表現し、(2)その状態を最適推定する、という全く新しいアプローチによって、雑音に埋もれた信号を検出するフィルタを設計しようとしたのが動機となっている。簡単のため、制御入力がない場合に対してこの最適推定問題を述べると以下のようになる。定理2で与えられる最適フィルタをカルマンフィルタ(Kalmanfilter)という。一般にカルマンフィルタは、システムが時変で、雑音が非定常な場合に対して求まっている。上の結果はそれをシステムと雑音がともに定常な場合に限定して得られたものであり、特に定常カルマンフィルタ(stationary Kalman filter)と呼ばれる。定常カルマンフィルタは、そのゲインの求め方が定理1で述べた最適レギュレータのゲインの求め方と双対をなしており、その意味で最適推定問題は最適レギュレータ問題の双対問題と呼ばれる。また定理2の(2)で述べた定常カルマンフィルタの安定性から、その推定誤差の平均値は零に収束する。この形は、先に2・2・2・a項で述べた全次元オブザーバ(2・49)と同じである。この点に注目して以下では、フィルタの形を式(2・75)よりもさらに限定された全次元オブザーバの形に制限し、その中で推定誤差が最小になるような最適オブザーバとして式(2・83)のカルマンフィルタを導出することにする。これは式(2・83)のカルマンフィルタについて成立する式であるが、容易にわかるようにこの式は-が安定となるような任意のゲインをもつ式(2・83)のオブザーバについて成立する。こうしてカルマンフィルタは、全次元オブザーバ型フィルタの中で推定誤差の共分散行列が最小になるものとして特徴付けられた。f. LQG 制御(LQG control)
