# 2・2・1 項で述べた最適レギュレータなどの状態フィードバック制

> **章**: 第2章 線形制御
> **節**: 2・1 項で述べる最適レギュレータのほかに、閉ループ極を指定され

御は、その実現のためにすべての状態を直接測定する必要がある。しかし、現実の多くのシステムではそれは難しい。その場合には、直接測定できるシステムの入力と出力からなんらかの方法で状態を推定し、その推定値をの代わりに用い6―39る必要がある。このような状態の推定機構には、確定的な環境のもとで状態を推定するオブザーバ(observer)と、統計的性質のわかった外乱や観測雑音が加わる確率的な環境のもとで状態を最適推定するカルマンフィルタ(Kalman filter)とがある。さらにオブザーバには、すべての状態を推定する全次元オブザーバと、出力情報を利用することにより推定すべき状態の数を最小に減らした最小次元オブザーバなどがある。以下ではこれらの状態推定器を順に説明する。a. 全次元オブザーバ(full-order observer)線形システム(2・36)(2・37)の状態ベクトルを推定しよう。その最も簡単な方法は、制御対象の動特性のコピー=+(2・47)を用いることである。そのときの推定誤差を=-(2・48)とすると、式(2・36)(2・47)よりは次式で定まる。=この式から明らかになるように、この方法は、(1)安定系にしか使えない、(2)推定誤差の収束の速さが変えられない、という原理的な欠陥を含んでいる。その理由は観測データを活用していないことにあり、この情報を活用した次の推定器を考える。=++-(2・49)制御対象をコピーした先の推定器(2・47)との大きな違いは、出力の推定誤差に比例した修正項が加わっている点である。この違いにより、式(2・49)の推定器を用いたときの推定誤差は=-(2・50)となり、対,の可観測性のもとで修正ゲイン行列を適切に選べば、-の固有値を任意に指定することができる。その結果、式(2・47)の推定器がもっていた欠陥は完全に解消され、状態の推定誤差は、初期値0,0と入力の値によらず、任意の速さで零に収束させることができる。式(2・49)の推定器は、システム(2・36)と同次元であることから、全次元または同次元オブザーバと呼ばれ、修正ゲインはオブザーバゲインと呼ばれる。b. 最小次元オブザーバ(minimum-order observer)線形システム(2・36)の個の状態は、その一部もしくはその線形結合が式(2・37)の出力として直接測定できる。したがって、以外の-個の状態変数あるいは、とは独立な-個の線形結合=を推定すれば、全状態もしくはそれを座標変換したが推定できることになる。この考え方に基づき次元を最小化したオブザーバを最小次元オブザーバという。以下ではまず、出力が状態の一部に等しい、つまり、=, =となる場合についてこのオブザーバの設計法を述べ、次にそれを一般の場合へ拡張する。線形システム(2・36)(2・37)を上記の状態変数の分割に合わせて=++(2・51)=++(2・52)=(2・53)と表す。は出力として直接測定できるから==(2・54)であり、のみ推定すればよい。そのためのオブザーバを=+(2・55)とおく。ただしは-次元システム=++(2・56)の状態とする。このオブザーバの推定誤差を=-(2・57)とすると、式(2・51)~(2・57)より容易に=-+-+--++--(2・58)を得る。これより推定誤差が=,,のいかんによらず、つ6―40制御システムねに零に収束するためには、式(2・58)右辺のそれぞれの係数行列が零となるように、オブザーバの係数行列,,を=-(2・59)=-+(2・60)=-(2・61)かつが安定行列となるように定めればよい。そしてシステム(2・36)(2・37)が可観測であれば、対,が可観測であることが容易にわかるので、が任意の安定な固有値をもつように式(2・55)の係数行列を定めることができる。以上よりシステム(2・36)(2・37)の状態を推定する最小次元オブザーバは、式(2・54)~(2・56)をまとめた式(2・62)(2・63)で表される。=++(2・62)+=0(2・63)ただし,,はそれぞれ式(2・59)~(2・61)で与えられる。以上の議論は、出力が状態の一部に等しい(つまり=,0の)場合のものであるが、一般のに対しても、適当な-×行列を用いて状態の座標変換=≡(2・64)を行えば、前述の場合に帰着される。したがって上記の最小次元オブザーバで式(2・63)を=0+(2・65)と置換えるだけで、一般の場合に対する最小次元オブザーバが得られる。その構成手順を以下にまとめておく。ただし、システム(2・36)は可観測であり、rank =と仮定する。手順1:式(2・64)の行列が正則になるように行列を定める。手順2:状態方程式(2・51)(2・52)の係数行列を次のように定める。욇쐍욈욪욇쐍욇=욈욪욇=욇욈욪욇手順3:式(2・59)のが安定になるようにを定める。手順4:オブザーバの係数行列,,を式(2・59)~(2・61)で定める。手順5:式(2・62)と式(2・65)で記述される最小次元オブザーバを構成する。c. 線形関数オブザーバ(linear function observer)以上述べた2種類の状態推定器は、いずれも線形状態フィードバック制御=を実現するためのものであるが、その目的のためには必ずしもすべての状態を推定する必要はなく、状態の線形関数ω=が推定できればよい。また、それにより推定器の次元を低くできる。このようなオブザーバを線形関数オブザーバといい、最小次元オブザーバと同様に次の線形システム=++ω=+の形で与えられる。これが線形関数のオブザーバ、つまりω∞となるための必要十分条件は、が安定行列であり、かつ次の関係式を満たす×行列が存在することである。=+=+=最小次元オブザーバは上記の線形関数オブザーバで=とおいた特別なものであり、その構成法から、この必要十分条件が=-として満たされることが容易にわかる。d. オブザーバを用いた状態フィードバック制御系一般に状態ベクトルが直接測定できない場合には、オブザーバによるその推定値を利用して擬似的に=-(2・66)と実現することになる。このとき、オブザーバの定義より定常状態ではに一致するから、定常状態に限れば等価的に状態フィードバックが実現できたことになる。次に、オブザーバを用いてこのように実現された制御系全体の性質を調べてみる。簡単のため全次元オブザーバ(2・49)を用いた場合を考察する。まず式(2・36)(2・48)(2・66)と式(2・50)より容易に次式を得る。=-+=-これをまとめると制御系全体のダイナミクスは=--と表される。したがって閉ループ系の特性方程式は-+-+=0となり、制御系全体の固定値は、状態フィードバック系の固有値λ욡-とオブザーバの固有値λ욡-とに分離されることがわかる。これは状態フィードバックゲインとオブザーバゲインが独立に設計できることを意味し、制御と推定の分離と呼ばれている。e. カルマンフィルタ(Kalman filter)d項までで扱った確定的な入力が加わるシステムと異なり、e項では外乱や雑音などの不規則信号が加わる線形確率システム=++(2・67)=+(2・68)を考える。ここに:次元の状態、:次元の制御入力、:次元の観測出力である。また、:次元のシステム雑音(不規則外乱)、:次元の観測雑音とする。以下、,はともに白色雑音(white noise)であり、その統計的性質については、ともに平均が零で、共分散行列がそれぞれ,であることがわかっているものとする。また、とは無相関とする。つまり、=0=0(2・69)τ=δ-τ, =(2・70)τ=δ-τ, >0(2・71)τ=0(2・72)と仮定する。ここでδ-τはディラック(Dirac)のδ関数である。白色雑音とは、時間的に無相関な信号であり、そのスペクトル密度が白色光のように全周波数帯域で一定になることからこのように呼ばれるが、現実には存在しない理想化された信号である。以下の準備のもとに、線形確立システム(2・67)(2・68)の時刻における状態を、時刻までの制御入力と観測出力の測定値に基づいて最適推定する問題を考える。この推定問題は、もともとカルマンが統計的性質をもつ信号が白色雑音を入力とする線形動的システムの出力として表現できる点に着目し、(1)その生成過程を上記の状態空間モデルで表現し、(2)その状態を最適推定する、という全く新しいアプローチによって、雑音に埋もれた信号を検出するフィルタを設計しようとしたのが動機となっている。簡単のため、制御入力がない場合に対してこの最適推定問題を述べると以下のようになる。(最適推定問題、optimal estimation problem)式(2・69)~(2・72)の性質をもつ白色雑音,が加わる線形定常確率システム=+(2・73)=+(2・74)の状態の推定値のうちで、(1) 出力の観測値τ,≦τ≦に関して線形、つまり適当な荷重関数,τを用いて=욪,ττdτ(2・75)と表され、(2) 定常状態における推定誤差=-の二乗平均(平均二乗誤差)=(2・76)が最小になる(つまり最小分散推定値になる)ものを求めよ。この問題の解(最適フィルタ)は、式(2・75)の推定値が平均二乗誤差(2・76)を最小化するための必要十分条件である直交射影の定理、つまりτ=0, τ<(2・77)を基に、以下のように与えられる。[定理2]対,は可観測、対,は可制御とするとき、(1) 上記の最適推定問題の解、すなわち最適フィルタは=+-(2・78)で与えられる。ここにフィルタゲインは、定常状態における推定誤差の共分散行列を用いて=(2・79)で定義され、は次のリカッチ方程式のただ一つの正定解である。+-+=0(2・80)(2) 最適フィルタ(2・78)を=-+(2・81)と表したとき、-は安定(つまり最適フィルタは安定)である。定理2で与えられる最適フィルタをカルマンフィルタ(Kalmanfilter)という。一般にカルマンフィルタは、システムが時変で、雑音が非定常な場合に対して求まっている。上の結果はそれをシステムと雑音がともに定常な場合に限定して得られたものであり、特に定常カルマンフィルタ(stationary Kalman filter)と呼ばれる。定常カルマンフィルタは、そのゲインの求め方が定理1で述べた最適レギュレータのゲインの求め方と双対をなしており、その意味で最適推定問題は最適レギュレータ問題の双対問題と呼ばれる。また定理2の(2)で述べた定常カルマンフィルタの安定性から、その推定誤差の平均値は零に収束する。この事実は、式(2・73)(2・74)と式(2・81)から得られるの状態方程式=-+-(2・82)を用いて容易に示すことができる。(注意)上記の推定問題では、推定値を式(2・75)のように出力データに関して線形なものに限定しているが、雑音,と初期状態にガウス性の仮定を加えれば、最小分散推定値は観測値の線形関数になることから、カルマンフィルタは観測データに基づくすべてのフィルタの中で最適になることが知られている。定理2の結果を制御入力を含む場合に拡張すると、式(2・78)のカルマンフィルタは=++-(2・83)となる(ただしの定義は変わらない)。この形は、先に2・2・2・a項で述べた全次元オブザーバ(2・49)と同じである。この点に注目して以下では、フィルタの形を式(2・75)よりもさらに限定された全次元オブザーバの形に制限し、その中で推定誤差が最小になるような最適オブザーバとして式(2・83)のカルマンフィルタを導出することにする。まず、式(2・67)と式(2・83)から推定誤差=-に関して式(2・82)が成立し、-の安定性と式(2・69)~(2・72)で述べた,の統計的性質よりの共分散行列はリアプノフ方程式-+-++=0(2・84)を満たすことがわかる。これは式(2・83)のカルマンフィルタについて成立する式であるが、容易にわかるようにこの式は-が安定となるような任意のゲインをもつ式(2・83)のオブザーバについて成立する。そこで、このリアプノフ方程式の解をと記し、カルマンフィルタのゲインを安定なオブザーバゲインと区別して特にと記す。このとき6―41≧(2・85)を示せばよい。以下では簡単のために=,=と記す。明らかに=であり、-+-++=0(2・86)が成立する。式(2・84)から式(2・86)を引き、の定義式を用いると-はリアプノフ方程式--+--+--=0を満たすことが容易にわかる。仮定より-は安定であるから、リアプノフ方程式の解の性質より-≧0、つまり式(2・85)を得る。こうしてカルマンフィルタは、全次元オブザーバ型フィルタの中で推定誤差の共分散行列が最小になるものとして特徴付けられた。f. LQG 制御(LQG control)
