# 2・7・5 LMI による制御系設計

> **章**: 第2章 線形制御
> **節**: 2・7 LMI に基づくシステム解析と制御系設計

:出力フィードバック系図2・88の制御系を考えよう。制御対象の状態空間実現を次式で与える。例として、制御問題を考えよう。状態フィードバックゲインを求める場合と同様、決定変数(すなわち対称行列と補償器の実現)に対する変数の変換、あるいはフィンスラーの定理に基づく変数の消去を通して、式(2・480)のようなBMI条件と等価的なLMI条件を導くことができる。以下では、LMIを解くことにより動的な出力フィードバック補償器を設計するアプローチとして変数消去および変数変換の両アプローチを述べる。前者は、LMIにおける変数が少ないこと、低次元補償器を求めうることが特徴であるが、適用できるLMI条件は限定される。一方後者では、変数の個数が多く、低次元補償器の設計のために用いるのは困難であるが、前者に比べより多くの種類のLMIを扱うことができ、また多目的設計にも適用できる。a. 変数消去(variable elimination)によるアプローチ制御問題(control problem)を考えよう。ランク条件を伴うLMIを効率良く、かつ確実に解くアルゴリズムは存在しないが、ある程度満足のいく解を与えるヒューリスティックな解法がいくつか知られている。は固有値分解により計算できる。その解より直接補償器が与えられる。保証はされていないが、トレースの最小化の結果しばしばランクが下がる解が得られるといわれている。式(2・476)の制御対象の係数行列として次式を考える。この値は、次数の制限がない場合に達成可能な閉ループ系のノルムの値の下限値(の近似)である。γ=γ×1.01に固定し、式(2・481)(2・482)(2・484)の制約のもと、今度は式(2・485)を最小化した。その結果得られた,について、-の固有値は8.013×10,3.763×10,6.477×10となった。これより、一次の補償器で同程度の性能を実現する補償器が存在するものと期待できる。そこで二つの小さい固有値を零とみなして=1とおいてみる。2入出力系なので特異値は周波数ごとに2個となる。比較のため、フルオーダ(==3)の補償器による閉ループ系の特異値プロットを図2・89の点線に示す。両補償器とも、閉ループ系のノルムとしてほぼ同じ値を達成していることがわかる。b. ロバスト制御問題(robustcontrol problem)定理8の結果は、ロバスト制御問題に適用できる。図2・90に、不確かさをもつ出力フィードバック制御系を示す。係数行列は式(2・490)のようになる。このとき、式(2・488)の任意のΔに対して閉ループ系は、ロバストに内部安定かつγ未満の性能を満たす。ロバスト性能判定の系1の場合と異なり、式(2・491)および(2・492)をすべての変数,について線形にすることはできない。この種の、スケーリング行列に関する非凸性を有する問題については文献(93)を参照されたい。c. 変数変換(change of variables)によるアプローチ再び式(2・476)の制御対象と式(2・477)の補償器による制御系を考える。まず、式(2・480)で表される制御問題について考えよう。また、他の制御性能についても同様にして決定変数に関する等価なLMI条件を得ることができる。まず、極の存在領域の指定に関しては、図2・85の各領域(a)~(d)について以下のようになる。各LMIにおいて、解が得られればそのから式(2・497)で求めた補償器はその制御性能を達成する。d. 多目的制御系(multipurpose control system)の設計変数変換による設計法を用いて、複数の制御仕様を満たす出力フィードバック制御系の設計を行うことができる。2・7・4項で示したLMI条件を連立させることによって、対応する制御仕様すべてを満たす補償器を求めることができる。なお、変数消去のアプローチではこのことは必ずしも成り立たないので注意を要する。以下では例題を用いて説明する。[例4]図2・91に示すような、ばねで結合された2慣性系を考える。また、インパルス的な外乱′が負荷に加わるとする。制御対象の状態方程式を次のように記述する。また二つの制御出力,′おのおのに対して出力方程式を用意している。式(2・500)の係数行列は次式とする。この問題に対して、c項に基づきLMI最適化問題を以下のように定式化する。式(2・501)の最適化問題が可解であれば、ノルム条件と極の存在領域の条件が満たされ、またγがノルムの上界を与える。この最適化問題では各LMIに対して共通の変数を用いており、その結果両者に共通のを強制することになっている。このため式(2・501)のLMI条件は元の仕様に対して十分条件となる。最適化計算の結果得られた補償器は次式となった。閉ループ系の極を図2・92に×印で示す(なお、図の範囲外に-44.6なる極がある)。図2・92より、仕様のとおり指定の領域に存在していることがわかる。比較のため、極指定条件を含めずに求めた補償器による閉ループ系の極を図2・92の○印に示し、インパルス応答を図2・93の点線に示す。極指定条件により、振動的な応答が回避されていることがわかる。
