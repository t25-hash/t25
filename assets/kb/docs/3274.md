# 3・1・1 制御のための微分幾何学(differential geometry)

> **章**: 第3章 非線形制御
> **節**: 3・1 フィードバック線形化

非線形状態方程式を扱うのに必要な微分幾何学の概念は以下のとおりである。以下の定義は微分幾何学の本の定義と多少異なることがあるが、本質は同じである。a. 関数のベクトルによる偏微分スカラ関数φのに関する偏微分を次のように行ベクトルの形で表す。ベクトル値関数に関しても同様である。近傍という名前からがつねに狭い範囲でしかないと誤解されることが多いので注意が必要である。この近傍も大きいこともあれば小さいこともある。d. 座標変換と微分同相写像(diffeomorphism)座標変換とは状態の座標系を変換するものである。非線形制御理論では関数やベクトル場を扱うため、これらの性が保存されるような座標変換が必要となる。そこで、ここでは微分同相な座標変換のみを扱うものとする。この定理は与えられた関数が座標変換であるか(逆変換が存在し、逆変換もであるか)を判断するときに用いる。ベクトル場の各要素が関数であるときベクトル場という。リー微分を繰返すときには次の記号を使う。i. リー微分と可識別性(可観測性)次の入力のないシステムの出力から状態を推定する問題(可観測性)を考える。これは可識別性と呼ばれ、線形システムの可観測性と同等のものと考えることができる。j. 線形システムの可観測性とリー微分線形システムの可観測性とリー微分の関係を考えるために、以下の入力のないシステムを考える。また、spanは実ベクトルの線形結合からなる実ベクトルの集合を表す。インボリューティブの概念は分布(distribution)に対して定義されることも多いが、ここの定義と本質的には同じである。この概念は次のフロベニウスの定理とともに用いられることが多い。m. リー括弧積とシステムの可制御性リー括弧積とシステムの可制御性を知るために以下のシステムを考える。さらに以下の入力を考えると、状態はこれらの方向以外にも移動できる。もし、これらが本の線形独立なベクトルで表されるならば、状態は任意の方向に移動できる、つまり、ある意味で可制御と考えることができる。ある意味でと書いたのはこの可制御性が線形システムでいう可制御性と完全には対応していないからであるが、ここでは詳細は省略する。n. 線形システムの可制御性とリー括弧積次の線形システムについて考えよう。
