# 3・1・2 状態方程式の線形化

> **章**: 第3章 非線形制御
> **節**: 3・1 フィードバック線形化

a. テイラー展開の一次近似線形化(approximate lineariza-tion)一般に用いられている線形化手法は以下のようなテイラー展開の一次近似に基づいたものである。例えば状態フィードバックを用いるならば=となり、コントローラは線形である。b. 座標変換と非線形フィードバックを用いた厳密な線形化(exact linearization)テイラー展開の一次近似を用いた線形化はシステムの線形近似であり、狭い範囲でしか有効ではなかった。ここでは状態方程式∑で表されたシステムに対して、非線形フ6―9 1ィードバックと座標変換を用いて厳密に線形化する方法について説明する。システム∑に対して次の座標変換とフィードバックを考える(は新しい入力)。この線形化は近似ではなく厳密な線形化である。この線形化されたシステムに対しては線形制御理論を用いてコントローラを設計することができる。つまり、線形化されたシステムを安定化するフィードバックに基づいて設計されたフィードバックは、元のシステム∑を安定化する。[定理]状態方程式∑に対して座標変換とフィードバックが原点近傍で存在して閉ループ系が線形化されるための必要十分条件は次の二つを同時に満たすことである。定理の条件が成り立つとき、線形化するための座標変換とフィードバックは次のように求められる。...dξd=ξ+...............c. 厳密な線形化を用いてロバストな制御系を設計するために厳密な線形化手法を用いてロバストな制御系を設計したい場合には、線形化した後の線形コントローラの設計に若干の工夫が必要である。基本的には線形制御理論を用いて線形化されたシステムに対して、ロバストな制御系(例えばLQ最適制御や制御)を設計すれば、全体としてロバストな制御系を設計できると考えられる。しかし、厳密な線形化手法は座標変換とフィードバックを用いてシステムのダイナミクスを矯正しているので、線形化されたシステムに対してロバストな線形コントローラを単に設計しただけでは、元の非線形システムに対してロバストなコントローラを設計していることにはならない場合があるので、注意が必要である。例えばLQ最適制御を基本としたロバストなコントローラを設計したいならば、次のようにしなければならない。一次近似モデルは近似モデルではあるが座標変換もフィードバックも用いていないため、少なくとも原点近傍のシステムの挙動はいちばんよく表しているモデルである。そのため、この一次近似モデルを用いて設計したLQ最適制御は少なくとも原点近傍ではLQ最適であり、かつロバストな制御則になっているはずである。
