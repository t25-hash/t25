# 4・1・1 連続系のMRACS

> **章**: 第4章 適応・学習制御
> **節**: 4・1 モデル規範型適応制御系(MRACS)

テムの強正実性とリアプノフの方法から導出されるのに対して，後
MRACS（model reference adaptive control system）は，特性
者をベースにしたものは正規化手法を併用した最小二乗，最小二乗
が未知あるいは特性が途中で変動するプラントを制御するための制
平均，指数重み付き最小二乗平均，ハイブリッド型などの各種こう
御系の一つで，プラントパラメータに不確かさがあっても任意の参
配法から導出される．
照入力に対して制御系の特性を良好に保持できる特徴を有する．
以上のような分類の組合せによってさまざまなMRACSが考え
MRACSの制御器は，制御系の出力（プラント出力，制御量）が
られるが，実現可能で，かつ，構成上意味をもつのは表4･1におけ
規範モデルの出力（目標出力）に漸近的に一致するように適応的に
るDCS1～DCS3，DDS1～DDS4，ICS1，IDS1の構成法であ
構成される（ほとんどの基本構成は図4･1のようになる）．
る．
実際の適応は，制御器の可調整パラメータが適応機構でオンライ
以下では，次数，相対次数 の1入力1出力プラントを考
ン調整されることによって行われる．ここで規範モデルとは，参照
え，前述の仮定が満たされている場合のMRACSの基本構成法を
入力から制御量に至る制御系の望ましい特性を記述したモデルで，
述べる．
通常，速応性や減衰特性などを考慮して決定される．
a.プラントの表現と制御目的 プラントの入力を，出力
漸近安定なMRACSを実現するためには，一般的に以下の仮定
を，ラプラス演算子を として入出力間の伝達関数 を
が必要になる．制御入力合成部が完全モデルマッチング（exact
model matching）の制御器と同じ構造になっているので，プラン
＝ ＝
＋ ＋ ＋…＋
＋＋＋…＋
トの次数や相対次数があらかじめ既知でなければならない，また，
制御器はプラントの零点を相殺する働きをもつため，その安定性を
保証する観点からプラントが非最小位相系であることが許されな
い．さらに，適応機構が正しく動作するためには，制御系にシステ
ム外乱や観測雑音が存在してはならない．これらの仮定は非常に厳
しいもので，すべてが満たされることは現実にはあり得ない．そこ
で現実には，次数や相対次数のミスマッチ，微小なむだ時間，外乱
や雑音などの不確かさが存在しても安定に動作し，かつ，追従誤差
が漸近的に許容範囲内におさまるようなMRACSの構成法が必要
であり，これはロバスト適応制御法として知られている（4･1･3項
参照）．
応則によって直接調整する直接法と，適応則が生成するプラントパ
ラメータの推定値に基づいた設計計算を経て制御器の可調整パラメ
ータが間接的に調整される間接法に大別される．さらにそれぞれの
方式では，制御入力の合成方法と適応機構の設計法がいくつか存在
し，目的に応じた組合せによってさまざまなタイプのMRACSが
存在する．
御器と同じであるが制御器の可調整パラメータの最適値が未知であ
を満足するベクトル信号である． は の第1成分 を
とする．ここで考える問題は，係数 ～，
～ が未知である
とき， が目標出力（規範モデル出力） に漸近的に追従す
る安定な適応制御系を構成することである．
b.直接法（direct method）のための別表現 －1次および
次の安定かつモニックな設計多項式を およびδ （δ：
時間微分演算子）とすると
δδ＝δδ＋δ (4･1)
を満たす－1のモニック多項式δと－1の多項式δが
一意に定まる．このとき，プラントは次のように表現できる（な
お，＝＋ε ［ ：強正実］なる一般化表現
も可能である）．
MRACSの基本的な構成法は，制御器の可調整パラメータを適
δ＝ (4･2)
＝βδ＋λ＋ (4･3)
＝ (4･4)
δ＋λ＝ (4･5)
ここに，∈，β∈ および∈ は の選択方法と
～，
～ に依存した未知パラメータである．∈ は
＝ ＋ (4･6)
制御入力の合成方法はまず，(１)基本構成がモデルマッチング制
＝, (4･7)
るため，その推定値を最適値とみなして制御入力を合成するCE原
理（certainty equivalence principle）に基づく方法と，(２)同定
モデルの出力がつねに目標信号に一致するように制御入力を合成す
表4･1 MRACSの構成法の分類
るSMC（surrogate model control）法，また，その十分条件であ
るDyCE原理（dynamic certainty equivalence principle）に基づ
入力合成
く方法に分けられる．さらに，それらの方法は入力合成時に割り算
CE SMC（DyCE)
直接法
を必要とする方法と必要としない方法に細分化される．一方，適応
割り算
割り算
割り算
割り算
機構の設計方法は，同定モデルがプラントの出力表現に基づく場合
あり
なし
あり
なし
と入力表現に基づく場合に分けられる．さらにそれぞれの場合に，
出力モデル型 DCS2 ― DDS2 ―
こう配型
適
入力モデル型 ― DCS3 ― DDS4
応
則 出力モデル型 DCS1 ― DDS1 ―
リアプノフ型
入力モデル型 ― ― ― DDS3
図4･1 MRACSの基本構成
入力合成
CE SMC（DyCE)
間接法
割り算
割り算
割り算
割り算
あり
なし
あり
なし
出力モデル型 ICS1 ― IDS1 ―
こう配型
適
入力モデル型 ― ― ― ―
応
則 出力モデル型 ― ― ― ―
リアプノフ型
入力モデル型 ― ― ― ―
(C)日本機械学会
