# 4・1・3 ロバスト適応制御(robust adaptive control)

> **章**: 第4章 適応・学習制御
> **節**: 4・1 モデル規範型適応制御系(MRACS)

適応制御システムは,フィードバック制御器内の可調整パラメータを現在までに得られたデータに基づいてオンラインで調整する手法であるため,基本データに外乱やモデル化誤差の影響があると,フィードバックループ内にある可調整パラメータのふるまいが,簡単に制御系を不安定化させてしまうことが知られている.したがって,外乱やモデル化誤差に対するロバスト性を考慮せずに適応制御系を構成することは現実的ではない.ここでは,ロバスト性を考慮したロバスト適応則(robust adaptive law)を紹介する.次の簡単な制御対象を考える.=θξ+η(4・73)ここで,∈は制御対象の出力,θ∈はシステムに含まれる未知パラメータベクトル,ζ∈はシステムの入出力データから入手可能な回帰ベクトル,η∈はシステムに混入する外乱やモデル化誤差の影響を表す信号である.例えば,制御対象が次のように出力形式(output form)で表現されている場合には,=+-+=+η式(4・73)の未知パラメータと回帰ベクトルは次のように表現できる(ただし,初期条件の影響を無視した).θ -ξξξ욊ただし,ξ,ξ욊は次のように入出力データから発生させる信号である.ξ=ξ+, ξ0=0ξ욊=ξ욊+, ξ욊0=06―118制御システム式(4・73)の制御対象について,η=0の理想的な場合について考える.まず,回帰ベクトルξが一様有界であるときには,次の適応則θ=Γξ, Γ=Γ>0-θξ(4・74)を用いると∈,θ∈,θ∈,∈,θ∈となり,ξ∈のときにはlim=0,limθ=0と示すことができる.また,ξ∈(一様有界)の保証がないときには,次の正規化適応則θ=Γξ, Γ=Γ>0-θξ(4・75)を用いると,∈,θ∈,θ∈,∈,θ∈と示すことができる.ここで,は正規化信号でありξ∈を満足するように生成する.例えば,式(4・76)の設定が簡単である.=1+ξ(4・76)しかし,システムは多かれ少なかれ外乱の影響を受け,モデル化誤差が存在すると考えるほうが一般的であり,η=0は成立しない.このとき,式(4・74)の誤差は,=θξ+η(4・77)となり,パラメータ誤差θθ-θの影響だけでなく,外乱,モデル化誤差の影響ηも誤差に含まれる.パラメータ調整則を駆動する誤差には,パラメータ誤差の影響のみが含まれている場合が理想であり,調整はうまくいきやすい.しかし,式(4・77)のような場合には,ηの影響もパラメータ誤差の影響であると思って間違った調整を行う危険性があり,さらに,調整パラメータが制御器内のパラメータである場合には,それが閉ループ内におかれているために,容易に全系の安定性が崩れる.そこで,これらの不安定性を回避するようなロバスト適応則を用いる必要がある.ここでは,リーケージ手法,射影アルゴリズム,不感帯法の三つのロバスト調整法を紹介する.a. リーケージ手法(leakage method)次のようなロバスト調整法を考える.これは,リーケージ手法の代表的手法であり,固定σ-修正法と呼ばれている.θ=Γξ-σθ:固定σ-修正法(4・78)ここで,σ>0なる一定の設定パラメータである.式(4・78)を採用したときの性能を解析しよう.次のリアプノフ関数の候補を考える.=12θΓθ(4・79)式(4・78)の解軌道に沿った,式(4・79)の時間微分は,=θΓθ=θΓ-Γξ-σθ=-θξ+σθθ=--η+σθθ=-+η+σθθ≦-+η+σθθ≦-+η+σθθ(4・80)となる.ここで,ηはη≦η,∀≧0なる外乱・モデル化誤差項の上界値である.さらに,次の二つの関係式を用いると,-+η≦-12-12 -η+12 η≦-12+12 ησθθ=σθθ-θ≦-σθθ+σθθ≦-σθθ+σθθ≦-12σθ+12σθとなるから,式(4・80)は,≦-12-12σθ+12 η+12σθ(4・81)となる.ここで,αを足して引くと,≦-α-2 -σ-αλ윯율융Γθ2 +η2 +σ2 θ(4・82)となり,0<α≦σλ윯읉윰Γのようにαを選ぶことができるから,式(4・82)は最終的に,≦-α+η2 +σ2 θ(4・83)となる.したがって,≧η2α+σ2αθなる領域θでは≦0となるのでθ∈となり,ξ∈ならば∈,θ∈となる.さらに,式(4・83)を積分すると,=θΓθ≦θ0Γθ0+η2α+σ2αθ(4・84)となるので,パラメータ誤差θは次の残差集合内に指数収束する.=θθΓθ≦ηα+σαθ(4・85)このロバスト適応則では,∈,θ∈は保証できず,η=0であってもlim=0は保証できない.この欠点を補うために,次のような二つのリーケージ手法(スイッチングσ-修正法,ε-修正法)が提案されている.θ=Γξ-σ욦θ:スイッチングσ-修正法(4・86)ただし,σ욦は次のようにパラメータ推定値ノルムθに依存して切換える.0, if θ<θ(4・87)σθθ-1, if θ≦θ≦2θσ욦=σ, if 2θ<θここで,θ:θ<θを満足する真のパラメータのノルム上界値(既知である必要がある)であり,σ>0である.式(4・87)では,θに対して連続的に切換えることにより,θ解の存在を保証することができる.式(4・86)を用いると,固定σ-修正法と同じく,∈,θ∈,θ∈を保証することができるが,残差領域は下記のように広がってしまう.=θθΓθ≦ηα+σα θ+4σα θ(4・88)また,∈,θ∈が保証できないことは固定σ-修正法と同じであるが,η=0の場合には,lim=0,limθ=0を保証することができる.もう一つのリーケージ手法として,ε-修正法を紹介する.θ=Γξ-θ:ε-修正法(4・89)ただし,>0は設計パラメータ.η=0の場合には,は零に向かい,式(4・89)のε-修正法におけるσの項も零に向かい,ξがPE信号persistently exciting signal,持続励振信号:次の条件を満足するα>0が存在することである,욧ξτξτdτ≧α,∀≧0 であるならば,lim=0を保証することが可能である.b. 射影アルゴリズム(projection algorithm)実際問題では,真のパラメータベクトルθが存在する,次の凸集合が既知であることが多い.θθ=θθ-θ≦0(4・90)この情報を利用した次のアルゴリズムを射影アルゴリズムという.Γξ, if θθ<θor if θθ=θθ=and Γξθ≦0(4・91)Γ-ΓθθΓθΓθξ, otherwiseただし,θ0∈のように選ぶ.射影アルゴリズムは,いつでも調整パラメータが凸集合内に存在することを保証することができ,実用的である.c. 不感帯法(dead zone algorithm)不感帯法は,誤差にはパラメータ誤差の情報だけではなく,外乱やモデル化誤差の情報も含まれているので,それらの情報については不感とし,パラメータ調整を行わないように改良したアルゴリズムである.θ=Γξ:不感帯法(4・92)ただし,は不感帯関数で下記のように与えられる.+η, if <-η0, if <η(4・93)-η, if >η許容範囲<ηに入ったならば,適応調整をしないことがわかる.ここで,ηは式(4・94)を満足する定数である.supη≦η<η(4・94)式(4・92)を採用したときの性能を解析しよう.次のリアプノフ関数の候補を考える.=12θΓθ(4・95)式(4・78)の解軌道に沿った,式(4・95)の時間微分は,=θΓθ=θΓ-Γξ=-θξ=--η(4・96)となる.ここで,-η+η>0, if <-η-η=0, if <η-η-η>0, if >ηであるから-η≧0となり,=--η≦0となる.したがって,∈,θ∈,θ∈であり,∈,θ∈を得る.不感帯法を実装する場合には,式(4・94)にあるように,ηの上界値を見積もる必要がある.
