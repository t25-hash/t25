# 5・2・3 状態評価ファジィ制御

> **章**: 第5章 知的制御
> **節**: 5・2 ファジィ制御(fuzzy control)

状態評価ファジィ制御は、人間による運転を制御対象システム自体のモデルを作成することなく、過去の操作経験に基づく状況の総合判断としてアルゴリズム化する方式である。例として、二つの観測データx とyから制御指令uを決定するような対象を考えると、「もし(If)x がA읉(高い)であり、yがB읉(高い)であるならば、(then)uをC읉(減らす)する」といった制御則により制御指令を決定する。ここで、「x がA읉」「yがB읉」「uをC읉する」といった命題は、その意味がファジィ集合により定義されたファジィ命題である。この状態評価ファジィ制御の制御則を一般的に示すと、If x is A읉and y is B읉then u읉(i=1~n)なるIf-thenルールで記述できる。このルールを用いて対象システムから観測したデータに基づきファジィ推論することにより、制御指令を決めていく(図5・13)。a. 設計手順温度と圧力の観測値から状態評価ファジィ制御の設計手順はステップ1:対象特性の理解と操作経験則の記述熟練者が、そのような状況を見て制御指令を決定しているのかを観察し、運転の経験則を記述する。例えば、ルール1:もし、温度が高く圧力が高ければ、燃料を減らす。ルール2:もし、温度が低く圧力が低ければ、燃料を増やす。のような経験則である。ステップ2:言葉の意味の定量化次に、上記経験則に記述されている状況を表す、「温度が高い」「圧力が低い」などの言葉を、図5・7で示したようなファジィ集合で定義する。システム状況を等分割する場合も多いが、各状況を意味する適切なラベル付けを行う。ステップ3:経験則のファジィ制御則化ステップ2で定義したファジィ集合を用いて、ルール1:If‘温度が高い’and‘圧力が高い’,then‘燃料を減らす’.ルール2:If‘温度が低い’and‘圧力が低い’,then‘燃料を増やす’.の制御則をIf-thenルールの形式で記述する。この制御則は計算機でその意味を用いて処理できる知識である。ステップ4:制御則と言葉の意味の調整記述した制御則とファジィ集合を実際の観測データに当てはめ、思いどおりの制御指令が計算されているかチェックする。この計算は、ファジィ推論のアルゴリズムとそこで使用する知識に分割されているため、不具合部分の確認を容易に行える。細かな調整はそこに影響を及ぼしているファジィ集合を調整することで解決できる。新たな意味付けが必要な場合には、それに対応した制御則を知識として追加する。また、あまり効果のない制御則は、統合化して知識6―142制御システムの洗練化を図る。b. 状態評価ファジィ制御の推論過程(図5・14)言葉によって定義された制御則(知識)をどのようにコンピュータで処理するかを説明する。. 前件部評価演算ファジィ制御器では、まず、各制御則の前件部(If部)に含まれているファジィ命題の評価を行う。過去や現時点で観測したシステム状態が、「温度が高い」「圧力が低い」といった人間の概念(主観)に基づき定義した各ファジィ集合욡욢=욋μに、どの程度適合しているかの命題適合度욡욢を求める。욡욢=μここで、は制御則番号を、は制御則内の命題番号を示す。この命題適合度の算出に当たっては、観測したシステム状態が一つの値(シングルトン)であるとし、メンバシップ関数μの値を適合度としている。次に、制御則の前件部に記述された各命題の適合度をファジィ演算を行い前件部適合度욡を求める。욡=욡,..,욡=min욡욢. 各ルール後件部の演算この前件部適合度と後件部に記述された「燃料を減らす」といった操作命題[ファジィ集合:μ욊욡]をファジィ演算(ここでは、各要素で乗算を求める代数積を使用)を行い、各制御則の操作指令の候補値(욡)を求める。욡=욋μ욌욡/=μ욊욡×욡/. 後件部の統合化さらに、複数の制御則より求まった制御指令候補値をファジィ演算(ここでは、各要素で最大値を求める論理和を使用)により統合化し、制御指令の推論結果()を得る。=μ/=maxμ욌욡/. 非ファジィ化運転支援用の表示であれば、このファジィ集合のままでもよいが、通常の制御を実行するためには、非ファジィ化して一つの制御指令値とする必要がある。ここでは、求まった制御指令のファジィ集合の重心を次式のように計算し、最終的制御指令としての燃料変化量を決めている。=μdμdこの方式により、セメントキルンや浄水場の薬品注入制御、シールド機械制御などが実用化されている。c. 関数型後件部方式(図5・15)対象システムの状態に応じて、適切な制御方式(PID 制御のパラメータなど)が経験的に求図5・15 関数型後件部とした状態評価ファジィ制御図5・14 状態評価ファジィ制御の推論過程図5・16 5個の三角形ファジィ集合による領域の等分割表5・1 ファジィ制御則群の表uyNBNMZOPMPBNBNBNBNMNMZONMNBNMNMZOZOxZONMNMZOPMPMPMNMZOPMPMPBPBZOZOPMPBPBまり、それらを切換え制御を実行している場合も多い。このような対象に対して、後件部をファジィ集合や実数値として記述せずに、観測した状態量,の関数(通常は、PID 制御などの関数)として記述する方式も提案されている。この方式で、制御則は、ファジィ集合で記述した領域ごとに、使用すべき制御指令の関数(計算式)を定義し、If x is A읉and y is B읉then u=f읉(x,y) (i=1~n)の形式で記述する。この関数は、PD 制御の場合には、=욣・+욃・ddの制御指令を求める関数となる。この推論方式では、通常の状態評価ファジィ制御の推論演算における前件部適合度욡を用いて、後件部の関数욡,の値を重み付けして統合化する。욡욡,=∑욡∑この方式は、ガラス溶融炉の制御などに応用され、特徴的なシステム状態に対して適切な制御パラメータが設計されている場合で、過渡的な状況における制御指令の補間に用いられている。d. 状態評価ファジィ制御方式の性質これら状態評価ファジィ制御方式は、制御則群の各If部とthen部に記述されている状態を入力と出力として、制御指令を決定する多入力多出力の非線形要素としてとらえることができる。例えば、入力は-1~+1の区間(A-D 変換器の出力±1V など)に正規化された状態量において、NB:負で大、NM:負で中位、ZO:零、PM:正で中位、PB:正で大などの五つの三角形ファジィ集合で領域を等分割する(図制御則は、2入力(x,y)1出力(y)の場合を例にとると、If x is NB and y is NB then u is NB...If x is PB and y is PB then u is PBの形式となる。25個の制御側をテーブル化して表5・1に示す。これらの制御則を用いてすべての状態量に対して、ファジィ推論した入出力結果は図5・17のようになる。ファジィ制御では、対象図5・17 ファジィ推論により生成する非線形面図5・18 予見ファジィ制御方式の構成システムの状況に応じて最適と思われる制御指令をリアルタイムに推論する。図5・10で示したように、平面である線形制御(u=k읋x+k윫y)を実現することもできるが、一般には、複雑な非線形の曲面となり、非線形制御系を実行することになる。また、演算の高速化や低価格化を目的とし、専用ハードウェアの開発や、事前に計算した計算結果をルックアップテーブルとしてメモリに保存しておく方法も用いられている。
