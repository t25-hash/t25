# 7・1・2 形式言語とオートマトン

> **章**: 第7章 離散事象システム
> **節**: 7・1 システム表現

a. 形式言語による離散事象システムの表現形式言語による離散事象システムのモデル化では、各事象に一つの記号を割当てることにより、システムのふるまいを発生した事象に対応する記号の系列として扱う。∑を事象の有限集合とする。∑により、空列εを含む∑の記号からなる有限長さの文字列[語(word)ともいう]全体の集合を表す。このとき、⊆∑を∑上の言語(lan-guage)という。離散事象システムにおいて発生可能な事象系列全体の集合は、∑上の一つの言語として表現される。文字列との連接(concatenation)とは、の直後にを続けて書いた文字列のことである。また、二つの言語,の連接とは=∈,∈のことである。ある言語のクリーネ閉包は、(1) =ε;(2) 욗=욗(≧1)としたとき、=욈욗により定義される。は任意個(0を含む)のの要素を並べてできる文字列全体の集合になる。ある文字列に対し、の先頭からのいくつかの記号からなる文字列をの接頭語(prefix)と呼びで表す(例:=ε,,,,)。また、言語に対し=∈をの接頭語閉包(prefix closure)という。言語は、に属する任意の文字列についてその接頭語がすべてに含まれるとき、すなわち=のとき、接頭語閉(prefix closed)であるという。b. オートマトンオートマトンとは、離散的な入力および出力をもつ機械を抽象化した数学モデルである。決定性オートマトン(deterministic automaton)は5項組=,∑,δ,,욇により定式化される。ここで、は状態の有限集合、∑は事象の有限集合、δ: ×∑は遷移関数であり、δ,σ=′は状態において事象σの生起により状態が′に遷移することを表す。任意の∈およびσ∈∑を与えたとき、δ,σは必ずしも定義されない。このような関数を部分関数という。したがって、遷移先が定義されないような状態∈および事象σ∈∑の組が存在しても構わない。また、∈は初期状態、욇⊆はマーク状態(marked state、あるいは目標状態)と呼ばれる状態の集合である。が有限集合のときは決定性有限オートマトンと呼ばれる。離散事象システムのモデル化では、上記のように遷移関数を部分関数として定義する。これは、各事象が生起可能かどうかが状態に依存して決まることを表現するためである。遷移関数が状態および事象列に対して定義されることをδ,!で表す。さらに、事象列の発生による状態の遷移を表現するために、次のように遷移関数δの定義域を×∑に拡張する。(1) δ,ε=;(2)δ,σ=δ′,σ,′=δ,(∈∑,σ∈∑)。すなわち、δ,は状態から事象列が生起した後に到達する状態を表す。決定性オートマトン=,∑,δ,,욇により、2種類の言語=∈∑δ,!および욇=∈∑δ,∈욇が定義される。は接頭語について閉じた言語であり、により生成される言語と呼ばれる。욇はによりマークされる言語(または、受理される言語)と呼ばれ、通常、一連の作業の完結に対応させる。明らかに욇⊆である。言語を生成する目的でオートマトンを用いるとき、それを特に生成器(generator)と呼ぶ場合がある。決定性有限オートマトンは状態遷移図(state transition dia-gram)と呼ばれる有向グラフとして表現できる。グラフの各頂点はオートマトンの一つの状態に対応させ、状態욡から状態욢へ事象σによる遷移があれば、すなわちδ욡,σ=욢ならば、グラフ上で頂点욡から頂点욢へσをラベルとする有向辺を描く。図7・1は決定性有限オートマトンの例である。事象集合は∑=α,β,γ、初期状態は、マーク状態はである。決定性オートマトンの定義において、状態遷移関数をδ: ×∑2に置換えたものを非決定性オートマトン(nondeterminis-tic automaton)という。非決定性オートマトンでは、事象の発生に伴う状態の遷移先が複数個存在することを許しているので、状態からの事象列による遷移先δ,は状態の集合になる。また、マークされた言語は욇=∈∑δ,∩욇≠により定義される。さらに、空列εによる状態遷移を許すように拡張した非決定性オートマトンを、ε-動作をもつ非決定性オートマトンと呼ぶ。c. 正則言語と正則表現言語は、ある決定性有限オートマトンが存在して=욇となるとき、正則(regular)であるという。有限状態の場合、決定性オートマトンにより受理される言語のクラスは、非決定性オートマトン、およびε-動作をもつ非決定性オートマトンにより受理される言語のクラスに等しい。正則言語を定義するためには、=욇となるオートマトンを与える方法のほかに、記号列として表現する正則表現(regular expression)がある。事象の有限集合∑上の正則表現とそれが表現する正則言語は、次のように再帰的に定義される。(1) は正則表現であり、空集合を表現する。(2) εは正則表現であり、集合εを表現する。(3) ∑の各要素に対しは正則表現であり、集合を表現する。(4) ,を正則表現、それらが表現する集合を,とするとき、+,,は正則表現であり、それぞれ集合∪,(連接)、(クリーネ閉包)を表現する。(5) 上記の(1)~(4)を有限回用いて導かれるもの以外は正則表現ではない。例えば、∑=,のとき、正則表現は言語のクリーネ閉包、すなわち文字列を任意個並べてできる文字列全体の集合を表現する。図7・1の決定性有限オートマトンにおいて、=αβγε+αε+α+β+γβ,욇=αβγαβである。d. 出力付きオートマトンオートマトンに出力を付けたモデルとして、ムーア機械(Moore machine)とミーリー機械(Mealy machine)がある。いずれにおいても、出力記号の集合および出力関数ξが定義される。ムーア機械ではξ: であり、出力が状態の関数として定義される。ミーリー機械ではξ: ×∑であり、状態遷移に付随して出力が定義される。二つのモデルは次の意味で等価である。一方の機械が与えられたとき、任意の入力文字列に対し、ムーア機械の入力εに対する出力を除いて等しい出力を与える他方の機械が存在する。e. 時間付き離散事象システム遅延やタイムアウトなどの事象発生に伴う時間制約を考慮するように拡張したモデルとして、時図7・1 決定性有限オートマトン6―165間付き離散事象システム(TDES : timed discrete event systems)がある。TDES のふるまいは、1単位時間の経過を表す特別な事象tick を導入したオートマトン=,∑욀욂욧∪tick,δ,,욇によりモデル化される。は時間なしのふるまいを表すオートマトン욀욂욧=,∑욀욂욧,δ욀욂욧,,욇、および各事象σ∈∑욀욂욧に与えられる二つの数値,により定義される。ここで、∈(:自然数の集合)および∈∪∞は、事象σが生起可能になってから実際に生起するまでの遅延時間の最小値および最大値を表す。すなわち、各事象σ∈∑욀욂욧はδ욀욂욧,σ!であるような状態に入ってからの経過時間が≦≦のときに生起する。図7・2に욀욂욧、各事象の遅延時間、およびそれらにより定義されるTDESを示す。
