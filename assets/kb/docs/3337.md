# 7・1・2 形式言語とオートマトン

> **章**: 第7章 離散事象システム
> **節**: 7・1 システム表現

a. 形式言語による離散事象システムの表現形式言語による離散事象システムのモデル化では、各事象に一つの記号を割当てることにより、システムのふるまいを発生した事象に対応する記号の系列として扱う。∑を事象の有限集合とする。∑により、空列εを含む∑の記号からなる有限長さの文字列[語(word)ともいう]全体の集合を表す。このとき、⊆∑を∑上の言語(lan-guage)という。離散事象システムにおいて発生可能な事象系列全体の集合は、∑上の一つの言語として表現される。b. オートマトンオートマトンとは、離散的な入力および出力をもつ機械を抽象化した数学モデルである。このような関数を部分関数という。したがって、遷移先が定義されないような状態∈および事象σ∈∑の組が存在しても構わない。が有限集合のときは決定性有限オートマトンと呼ばれる。離散事象システムのモデル化では、上記のように遷移関数を部分関数として定義する。これは、各事象が生起可能かどうかが状態に依存して決まることを表現するためである。さらに、事象列の発生による状態の遷移を表現するために、次のように遷移関数δの定義域を×∑に拡張する。言語を生成する目的でオートマトンを用いるとき、それを特に生成器(generator)と呼ぶ場合がある。決定性有限オートマトンは状態遷移図(state transition dia-gram)と呼ばれる有向グラフとして表現できる。図7・1は決定性有限オートマトンの例である。決定性オートマトンの定義において、状態遷移関数をδ: ×∑2に置換えたものを非決定性オートマトン(nondeterminis-tic automaton)という。さらに、空列εによる状態遷移を許すように拡張した非決定性オートマトンを、ε-動作をもつ非決定性オートマトンと呼ぶ。有限状態の場合、決定性オートマトンにより受理される言語のクラスは、非決定性オートマトン、およびε-動作をもつ非決定性オートマトンにより受理される言語のクラスに等しい。事象の有限集合∑上の正則表現とそれが表現する正則言語は、次のように再帰的に定義される。(2) εは正則表現であり、集合εを表現する。(5) 上記の(1)~(4)を有限回用いて導かれるもの以外は正則表現ではない。d. 出力付きオートマトンオートマトンに出力を付けたモデルとして、ムーア機械(Moore machine)とミーリー機械(Mealy machine)がある。いずれにおいても、出力記号の集合および出力関数ξが定義される。ミーリー機械ではξ: ×∑であり、状態遷移に付随して出力が定義される。二つのモデルは次の意味で等価である。一方の機械が与えられたとき、任意の入力文字列に対し、ムーア機械の入力εに対する出力を除いて等しい出力を与える他方の機械が存在する。e. 時間付き離散事象システム遅延やタイムアウトなどの事象発生に伴う時間制約を考慮するように拡張したモデルとして、時図7・1 決定性有限オートマトン6―165間付き離散事象システム(TDES : timed discrete event systems)がある。
