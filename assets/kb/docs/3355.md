# 7・5・3 ハイブリッドペトリネット

> **章**: 第7章 離散事象システム
> **節**: 7・5 ハイブリッドシステム

a. 時間ペトリネットトランジションの発火は瞬時に起こり,マーキングはからに瞬時に遷移すると考える確定的な時間を導入したペトリネットを時間ペトリネット(timed Petrinet)という.時間の導入方法の違いにより,発火遅延時間モデル,発火継続時間モデル,プレース時間モデルの3種類に大別される.ここでは,発火遅延時間モデルについて説明する.ネット構造を表す有向2部グラフ=,,Pre,Postと初期マーキングで定義されたペトリネットを考える.ただし,はプレースの集合,はトランジションの集合,Pre,はプレースからトランジションへの枝の重み,Post,はトランジションからプレースへの枝の重みを表す.発火遅延時間モデルでは,各トランジション욡に時間욡を割当てる.욡はトランジション욡が発火可能になってから実際に発火するまでの遅延時間を表す.発火可能となったトランジションは,発火するまでの間発火により取り去るトークンを各入力プレースにおいて確保する必要がある.このために,トークンは,予約(reserved)と非予約(nonreserved)の2種類の状態に分類される.時刻∈용においてプレースにある予約トークン数を,非予約トークン数を욗と書く.ただし,にあるすべてのトークン数は=+욗である.初期マーキングはすべて非予約であるとする.すなわち,各욡∈に対して욡0=욗욡0=욡である.時刻においてトランジション욡が発火可能となるのは,式(7・13)を満たすときである.욗≧Pre,욡, ∀∈●욡(7・13)욡が発火可能となると,욡の各入力プレース∈●욡においてPre,욡個のトークンが予約となる.つまり,욗=욗-Pre,욡(7・14)=+Pre,욡(7・15)となる.ただし,はの直前を表す.そして,時刻+욡のときにトランジション욡は発火し,욡の各入力プレースの予約トークンがPre,욡個取り去られ,各出力プレースに非予約トークンがPost,욡個投入される.b. 連続ペトリネット連続ペトリネット(continuous Petrinet)とは,トークンおよび枝重みとして実数値をとることを許したペトリネットである.連続ペトリネットが提案された動機の一つに,トークン数がかなり多い場合に可到達マーキング数の爆発により事実上解析が不可能になることが挙げられる.例えば,生産システムでは,各バッファに蓄えられるパーツの量を各プレースでのトークン数としたとき,バッファの容量がかなり大きい場合には,可達集合の要素数が爆発的に増大する.そこで,パーツの個数を整数ではなく実数で表現し,パーツの流れをフローとして近似して解析をするほうが実用上有効となる.時間を導入した場合には,6―185図7・22 連続ペトリネットの例図7・23 時間ペトリネットの挙動とその近似としての連続ペトリネットの挙動(実線:時間ペトリネット,点線:連続ペトリネットの挙動を表す)本来トランジションが離散的に発火することにより離散的にマーキングが変化するものを,フローとみなしたことによりマーキングが連続的に変化する,つまり,トランジションの発火が継続的になるという状況が必然的に生じてしまい,本来のペトリネットのふるまいをうまく近似するためにどのような発火規則を採用すればよいかという点が重要な問題点となる.ここでは,簡単のために,発火遅延時間モデルで表された時間ペトリネットのふるまいの近似として得られる連続時間ペトリネットの最もシンプルな発火規則を説明する.図7・22に示す時間ペトリネットを考える.初期マーキングを5,0とすると,プレースとのマーキングの時間変化は図7・23の実線のようになる.ここで,トランジションは0.1時間ごとに発火するので,単位時間当たり10個のトークンがからに流入し,同じく単位時間当たり10個のトークンがに流入していると考えられる.一般に,遅延時間がのトランジションにおいて,各入力プレースからは速度Pre,でトークンが流入し,各出力プレース′へは速度Post′,でトークンが流出すると考えればよい.つまり,時刻においては発火速度=1で連続的に発火していると考える.ところで,この例では定常状態においてはマーキングが0,5と1,4を周期的に繰返すことになる.しかし連続ペトリネットで近似した場合には,0,4になった瞬間にトランジションは発火が止まるが,同時ににはトークンが連続的に流入してくるので,その瞬間は発火を再開することになる.つまり,瞬時に発火の停止と再開が発生することになり,この発火規則では整合性がとれなくなる.さらに,へ流入するトークンの速度のほうがから流出するトークンの速度より小さいという点も考慮しなければならない.したがって,マーキングが零になっても発火が継続するような規則を導入する必要がある.そこで,すべての入力プレースのトークン数が非零のときの発火を強発火と呼び,そうでないときの発火を弱発火と呼ぶ.弱発火では,へ流入するトークンの速度を流出するトークンの速6―186制御システム度とする.換言すれば,流入するトークンの速度がPre,よりも大きければ強発火となり,その結果にトークンが蓄えられることになり,小さければトークン数は零のまま弱発火し続けることになる.したがって,この例では,トークンの変化は以下のようになる.=max5-5,0=min5,5この軌道は図7・23の点線のようになる.さらに,トランジション욡の発火速度욡は以下のようになる.=10,5,if <5otherwise=5トランジションが強発火または弱発火しているときの発火速度が一定値になるような発火規則を採用したペトリネットを定速度連続ペトリネット(constant speed continous Petri net)という.このほかにも,発火速度が入力プレースのトークン数に依存する可変速度連続ペトリネットや漸近的連続ペトリネットなどが提案されている.c. ハイブリッドペトリネットの定義生産システムにおいてパーツの流れなどを連続量で近似することは有効ではあるが,機械の立上げ,停止などを表現するには離散的なトークンを利用したほうがよい.そこで,連続値をとるトークンと離散値しかとれないトークンを共存させたペトリネットをハイブリッドペトリネット(hybrid Petri net)という.ハイブリッドペトリネットは,離散トークンをもつ離散プレース,連続トークンをもつ連続プレース,瞬時に発火する離散トランジション,連続発火する連続トランジションからなる.これらを節点とする有向グラフによってハイブリッドペトリネットは定義される.これらを区別するために図7・24に示す記号が用いられる.ネット構造は=,,Pre,Postで表し,枝の重みPre: ×용とPost: ×용は実数値をとる.各プレースとトランジションが連続,離散のいずれであるかを示す関数: ,を導入する.ここで,は離散を,は連続を表す.例えば=ならばは離散プレースを意味する.はハイブリッド関数と呼ばれる.マーキング: 용は非負実数値をとるが,離散プレースに対しては整数値に制限し,任意のトランジションに対してPre,とPost,も整数値に制限する.離散プレースと連続トランジションの間に枝があってもよいが,以下の条件を満たしていると仮定する.=∧=Pre,=Post,(7・16)トランジションの発火可能性を定義する.次式を満たす離散トランジションは時刻で発火可能であるという.욗≧Pre,, ∀∈●離散トランジションの発火規則は発火遅延時間モデルと同じ規則に従う.連続トランジションについては,弱発火と強発火の2種類の発火がある.弱発火となるのは,非予約トークンが零である入力プレースがあっても,ある連続トランジションからのトークンの流入がその入力プレースにあるときである.このようにトークンの流入があるとき,そのプレースは供給されている(supplied)と呼び,連続トランジションが以下の条件を満たすとき,時刻で発火(a) 離散プレース(b) 連続プレース(c) 離散トランジション(d) 連続トランジション可能であるという.(1) 各離散プレース∈●に対して욗≧Pre,(2) 各連続プレース∈●に対して,욗>0,もしくはは供給されている.すべての入力プレースに非予約トークンが存在するとき,は強発火可能であると呼び,非予約トークンがないが発火可能であるとき弱発火可能であるという.遅延時間をもつトランジションは,離散トランジションの場合には発火可能となってから,時間後に発火する.連続トランジションの場合には,最大発火速度=1で連続発火する.つまり,強発火の場合には発火速度はである.弱発火のときには,入力プレースへ流入するトークンの速度に依存した以下の速度で発火する.時刻におけるマーキングは式(7・17)で記述される.=+σ+요τdτ(7・17)ただし,は接続行列と呼ばれ,その,成分はPost욡,욢-Pre욡,욢となる.σは発火回数ベクトルでこの番めの要素は,時刻までにトランジション욢が発火した回数であり,もし욢=のときはつねに零である.τは時刻τにおける発火速度ベクトルで,この番めの要素はトランジション욢の時刻τにおける発火速度であり,連続発火していないときは発火速度は零となり,また욢=ならば恒等的に零となる.最近では,ハイブリッドペトリネットを拡張したバッチペトリネット,高水準ハイブリッドペトリネット,微分ペトリネットなどが提案されている.例えば,微分ペトリネットでは,連続プレースと連続トランジションに関連したトークンや枝の重みに負の実数を許すように拡張されている.d. 競合の解消一般にペトリネットには競合が存在する.競合とは複数個のトランジションが発火可能となるが,そのうちいくつかが発火すると,発火可能であった他のトランジションが発火不可能となることをいう.競合を解消するための規則を導入しておかないと,ペトリネットの挙動をシミュレーションすることができない.例えば一つのプレースから二つのトランジション,へ枝があるとき(図7・25)には,以下の6種類の競合がある.(1) =,==本質的に離散ペトリネットでの競合と同じで,トランジションに優先度を付けることにより競合を解消すればよい.(2) =,==(1)の場合と同様で,トランジションに優先度を付けて競合を解消する.(3) =,=,=離散トランジションと連続トランジションの発火は本質的に異なるので,優先度をトランジション個別に与えるという問題として扱うよりも,離散トランジションと連続トランジションのいずれに優先度を与えるのかという問題と見るほうがよい.離散トランジションを優先させる場合が実際には多い.(4) =,=,=競合の有無がの値に依存する.もし욗<Pre,ならば,は発火可能とはならず,が連続発火することになる.욗>Pre,ならば,Pre,個のトークンがの発火のた図7・24 ハイブリッドペトリネットの節点図7・25 連続プレースと連続トランジションからなる競合の例めに予約され,残り욗-Pre,がの連続発火のために使われる.욗=Pre,のときに競合が発生する.この場合には(3)の場合と同様,離散トランジションを優先的に発火させる場合が多い.(5) =,==のトークンをいずれのトランジションの発火のために使うかを指定する必要がある.しかし,トークンの利用時間を指定しないと,片方のみが連続発火し続けることになる.したがって,二つのトランジションを交互に連続発火させるための時分割規則を導入する必要がある.(6) =,==トランジションに優先権を付けて一方のトランジションのみを発火させることもできるし,連続値トークンを二つのトランジションでシェアし,両方が並行的に連続発火させることもできる.
