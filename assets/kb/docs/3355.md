# 7・5・3 ハイブリッドペトリネット

> **章**: 第7章 離散事象システム
> **節**: 7・5 ハイブリッドシステム

a. 時間ペトリネットトランジションの発火は瞬時に起こり、マーキングはからに瞬時に遷移すると考える確定的な時間を導入したペトリネットを時間ペトリネット(timed Petrinet)という。時間の導入方法の違いにより、発火遅延時間モデル、発火継続時間モデル、プレース時間モデルの3種類に大別される。ここでは、発火遅延時間モデルについて説明する。発火可能となったトランジションは、発火するまでの間発火により取り去るトークンを各入力プレースにおいて確保する必要がある。このために、トークンは、予約(reserved)と非予約(nonreserved)の2種類の状態に分類される。初期マーキングはすべて非予約であるとする。時刻においてトランジションが発火可能となるのは、式(7・13)を満たすときである。b. 連続ペトリネット連続ペトリネット(continuous Petrinet)とは、トークンおよび枝重みとして実数値をとることを許したペトリネットである。連続ペトリネットが提案された動機の一つに、トークン数がかなり多い場合に可到達マーキング数の爆発により事実上解析が不可能になることが挙げられる。例えば、生産システムでは、各バッファに蓄えられるパーツの量を各プレースでのトークン数としたとき、バッファの容量がかなり大きい場合には、可達集合の要素数が爆発的に増大する。そこで、パーツの個数を整数ではなく実数で表現し、パーツの流れをフローとして近似して解析をするほうが実用上有効となる。時間を導入した場合には、6―185図7・22 連続ペトリネットの例図7・23 時間ペトリネットの挙動とその近似としての連続ペトリネットの挙動(実線:時間ペトリネット、点線:連続ペトリネットの挙動を表す)本来トランジションが離散的に発火することにより離散的にマーキングが変化するものを、フローとみなしたことによりマーキングが連続的に変化する、つまり、トランジションの発火が継続的になるという状況が必然的に生じてしまい、本来のペトリネットのふるまいをうまく近似するためにどのような発火規則を採用すればよいかという点が重要な問題点となる。ここでは、簡単のために、発火遅延時間モデルで表された時間ペトリネットのふるまいの近似として得られる連続時間ペトリネットの最もシンプルな発火規則を説明する。図7・22に示す時間ペトリネットを考える。初期マーキングを5,0とすると、プレースとのマーキングの時間変化は図7・23の実線のようになる。ここで、トランジションは0.1時間ごとに発火するので、単位時間当たり10個のトークンがからに流入し、同じく単位時間当たり10個のトークンがに流入していると考えられる。ところで、この例では定常状態においてはマーキングが0,5と1,4を周期的に繰返すことになる。つまり、瞬時に発火の停止と再開が発生することになり、この発火規則では整合性がとれなくなる。さらに、へ流入するトークンの速度のほうがから流出するトークンの速度より小さいという点も考慮しなければならない。したがって、マーキングが零になっても発火が継続するような規則を導入する必要がある。そこで、すべての入力プレースのトークン数が非零のときの発火を強発火と呼び、そうでないときの発火を弱発火と呼ぶ。弱発火では、へ流入するトークンの速度を流出するトークンの速6―186制御システム度とする。したがって、この例では、トークンの変化は以下のようになる。このほかにも、発火速度が入力プレースのトークン数に依存する可変速度連続ペトリネットや漸近的連続ペトリネットなどが提案されている。c. ハイブリッドペトリネットの定義生産システムにおいてパーツの流れなどを連続量で近似することは有効ではあるが、機械の立上げ、停止などを表現するには離散的なトークンを利用したほうがよい。そこで、連続値をとるトークンと離散値しかとれないトークンを共存させたペトリネットをハイブリッドペトリネット(hybrid Petri net)という。ハイブリッドペトリネットは、離散トークンをもつ離散プレース、連続トークンをもつ連続プレース、瞬時に発火する離散トランジション、連続発火する連続トランジションからなる。これらを節点とする有向グラフによってハイブリッドペトリネットは定義される。これらを区別するために図7・24に示す記号が用いられる。はハイブリッド関数と呼ばれる。離散プレースと連続トランジションの間に枝があってもよいが、以下の条件を満たしていると仮定する。次式を満たす離散トランジションは時刻で発火可能であるという。連続トランジションについては、弱発火と強発火の2種類の発火がある。弱発火となるのは、非予約トークンが零である入力プレースがあっても、ある連続トランジションからのトークンの流入がその入力プレースにあるときである。このようにトークンの流入があるとき、そのプレースは供給されている(supplied)と呼び、連続トランジションが以下の条件を満たすとき、時刻で発火(a) 離散プレース(b) 連続プレース(c) 離散トランジション(d) 連続トランジション可能であるという。すべての入力プレースに非予約トークンが存在するとき、は強発火可能であると呼び、非予約トークンがないが発火可能であるとき弱発火可能であるという。遅延時間をもつトランジションは、離散トランジションの場合には発火可能となってから、時間後に発火する。つまり、強発火の場合には発火速度はである。弱発火のときには、入力プレースへ流入するトークンの速度に依存した以下の速度で発火する。最近では、ハイブリッドペトリネットを拡張したバッチペトリネット、高水準ハイブリッドペトリネット、微分ペトリネットなどが提案されている。例えば、微分ペトリネットでは、連続プレースと連続トランジションに関連したトークンや枝の重みに負の実数を許すように拡張されている。d. 競合の解消一般にペトリネットには競合が存在する。競合とは複数個のトランジションが発火可能となるが、そのうちいくつかが発火すると、発火可能であった他のトランジションが発火不可能となることをいう。競合を解消するための規則を導入しておかないと、ペトリネットの挙動をシミュレーションすることができない。離散トランジションを優先させる場合が実際には多い。この場合には(3)の場合と同様、離散トランジションを優先的に発火させる場合が多い。しかし、トークンの利用時間を指定しないと、片方のみが連続発火し続けることになる。したがって、二つのトランジションを交互に連続発火させるための時分割規則を導入する必要がある。
