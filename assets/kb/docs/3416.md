# 7・1・2 形式言語とオートマトン

> **章**: 第7章 離散事象システム
> **節**: 7・1 システム表現

対象をモデル化するときに，本来は連続値をとる状態を離散的に扱う場合（例：タンクの水量をバルブの開閉により制御するシステムでは，水面の高さは連続値をとるが，目的によっては，レベル低，中，高の3値のみをとる状態変数を考えれば十分な場合がある)．
事象駆動 事象（event）とは，ボタンが押される（特定の動作が起こる），あるいは，タンクの水量がある水準を超える（特定の条件が満たされる）など，離散的状態の瞬間的な遷移のことである．
状態の変化が微分方程式や差分方程式のように，時間の関数として与えられるようなシステムは，時間駆動（time-driven）であるという．
例えば，システムのクロックに同期して状の連接とは＝∈,∈のことである．
ある言語意のタイミングで発生する事象によって状態が変化するシステムは，事象駆動（event -driven）であるという．
事象生起のタイミングは事前には与えられず，また，クロックと同期する必要もない（例：計算機における多くの動作はクロックに同期して実行されるので時間駆動であり，一方，割込み処理は事象駆動である）．
このし，それらが相互作用を繰返しながら並行的に状態遷移を行っていのできる対象は，表7･1に示すように，計算機システム，通信シスマトン，形式言語，ペトリネット，代数的モデル，論理式，待ち行用目的から大きく次の二つに分類できる．
さらに次の二つに分類できる．
その他 プロジェクト管理，物流システム,,）．
また，言語 に対し ＝∈を の接頭字列についてその接頭語がすべて に含まれるとき，すなわちb.離散事象システムの例 離散事象システムとして扱うこと力をもつ機械を抽象化した数学モデルである．
決定性オートマトンc .モデルの分類 離散事象システムのモデル化には，オート合，δ:×∑は遷移関数であり，δ,σ＝′は状態 にお∈およびσ∈∑を与えたとき，δ,σは必ずしも定義されない．
このような関数を部分関数という．
したがって，遷移先が定義わない．
また，∈は初期状態，⊆はマーク状態（marked state，あるいは目標状態）と呼ばれる状態の集合である．
オペレーティングシステム，ソフトウェア設計，データベースシステム，VLSI設計，関数δの定義域を×∑に拡張する．
δ,ε＝；無人搬送システム，制御用ソフトウェア，組込みシステム，生産スケジューリンググループウェア，ワークフローシステムが定義される．
は接頭語について閉じた言語であり， によ語（または，受理される言語）と呼ばれ，通常，一連の作業の完結的でオートマトンを用いるとき，それを特に生成器（generator）と呼ぶ場合がある．
決定性有限オートマトンは状態遷移図（state transition dia-gram）と呼ばれる有向グラフとして表現できる．
グラフの各頂点はオートマトンの一つの状態に対応させ，状態 から状態 へ事象σによる遷移があれば，すなわちδ,σ＝ならば，グラフ上で頂点 から頂点 へσをラベルとする有向辺を描く．
図7･1は決定性有限オートマトンの例である．
事象集合は∑＝α,β,γ，初期状態は，マーク状態は である．
決定性オートマトンの定義において，状態遷移関数をδ:×ってからの経過時間 が≦≦のときに生起する．
図7･2に，各事象の遅延時間，およびそれらにより定義されるTDESに伴う状態の遷移先が複数個存在することを許しているので，状態a.形式言語による離散事象システムの表現 形式言語による離散事象システムのモデル化では，各事象に一つの記号を割当てることにより，システムのふるまいを発生した事象に対応する記号の系列として扱う．
∑を事象の有限集合とする．
∑により，空列εを含む∑の記号からなる有限長さの文字列［語（word）ともいう］全体の集合を表す．
このとき，⊆∑を∑上の言語（lan-guage）という．
離散事象システムにおいて発生可能な事象系列全体の集合は，∑上の一つの言語として表現される．
文字列 と の連接（concatenation）とは， の直後に を続けて書いた文字列 のことである．
また，二つの言語，態が変化するようなシステムは，時間駆動である．
これに対し，任としたとき，＝により定義される．
は任意個（0を含む）の の要素を並べてできる文字列全体の集合になる．
ある文字列 に対し， の先頭からのいくつかの記号からなる文字列を の接頭語（prefix）と呼び で表す（例：＝ε,,ような非同期的な事象生起は，複数のプロセスがシステム内に存在く状況において出現する場合が多い．
テム，生産システムなどさまざまな領域に存在する．
列ネットワークなど，さまざまなものが用いられる．
これらは，使さを検証するために用いるモデル．
時間を導入するか否かにより，を表現するためのモデル．
オフィスオートメーション交通システム 信号制御システム，航空管制システムに対応させる．
明らかに⊆である．
言語を生成する目語閉包（prefix closure）という．
言語 は， に属する任意の文＝のとき，接頭語閉（prefix closed）であるという．
b.オートマトン オートマトンとは，離散的な入力および出定式化される．
ここで， は状態の有限集合，∑は事象の有限集いて事象σの生起により状態が′ に遷移することを表す．
任意の.論理モデル（logical model) システムの論理的な正しされないような状態∈および事象σ∈∑の組が存在しても構 非時間モデル 事象の生起順序のみを考慮するモデル．
時間モデル 事象の生起に時間的制約をもつような対象る．
関数として定義する．
これは，各事象が生起可能かどうかが状態に依存して決まることを表現するためである．
遷移関数が状態 および事象列 に対して定義されることをδ, !
さらに，事象列の発生による状態の遷移を表現するために，次のように遷移非同期回路，通信プロトコルδ,は状態 から事象列 が生起した後に到達する状態を表す．
決定性オートマトン＝,∑,δ,,により，2種類の言語り生成される言語と呼ばれる．
TDESのふるまいは，1単位時間の経過を表す特別な事象tickを導入したオートマトン＝,∑∪ tick,δ,,によりモデル化される．
は時間なしのふるまいを表すオートマトン＝,∑,δ,,，および各事象σ∈∑ に与えられる二つの数値， により定義される．
ここで，∈（ ：自然数の集合）および∈∪∞は，事象σが生起可能になってから実際に生起するまでの遅延時間の最小値および最大値を表す．
すなわち，各事象σ∈∑はδ,σ !
であるような状態 に入∑2に置換えたものを非決定性オートマトン（nondeterminis-tic automaton）という．
非決定性オートマトンでは，事象の発生を示す．
