# 7・1・3 ペトリネット

# 7・1・3 ペトリネット

> **章**: 第7章 離散事象システム
> **節**: 7・1 システム表現

からの事象列 による遷移先δ,は状態の集合になる．また，
a.概要 ペトリネット（Petri net ）は並行的・非同期的・分
マークされた言語は＝∈∑δ,∩≠により定
散的なシステムを表現するためのモデルであり，離散事象システム
義される．さらに，空列εによる状態遷移を許すように拡張した
のモデル化に広く用いられている ．ペトリネットは図的な表現
非決定性オートマトンを，ε-動作をもつ非決定性オートマトンと
が可能であり，モデル化の際に直観的に理解しやすいモデルである
呼ぶ．
といえる．さらに，記述したモデルがそのままシミュレーションに
c.正則言語と正則表現 言語 は，ある決定性有限オートマ
より実行でき，その過程が図的に表現できるため，ソフトウェアツ
トン が存在して＝となるとき，正則（regular ）であ
ールによるモデル作成支援が有効である．ペトリネットには表7･2
るという．有限状態の場合，決定性オートマトンにより受理される
に示すようにいくつかの種類が存在する．
言語のクラスは，非決定性オートマトン，およびε-動作をもつ非
b.プレース/トランジションネット プレース/トランジショ
決定性オートマトンにより受理される言語のクラスに等しい．
ンネット（place/ transition nets ）とは4項組＝,,,
正則言語 を定義するためには，＝となるオートマト
である．ここで， はプレース（place）の有限集合， はトラ
ン を与える方法のほかに，記号列として表現する正則表現
ンジション（transition）の有限集合（∩＝ )，:×∪
（regular expression）がある．事象の有限集合∑上の正則表現と
×はプレースとトランジションの隣接関係を表す写像，
それが表現する正則言語は，次のように再帰的に定義される．
:は初期マーキング（initial marking）である．
(１) は正則表現であり，空集合 を表現する．
プレース/トランジションネットはプレースとトランジションと
(２)εは正則表現であり，集合εを表現する．
いう2種類の頂点をもつ有向2部グラフとして見ることができ，通
(３)∑の各要素 に対し は正則表現であり，集合 を表現
常，図7･3(ａ)のように描く．プレースは○で，トランジションは
する．
□（あるいは ）で，写像 はアーク（arc) により表す．
(４) ， を正則表現，それらが表現する集合を ， とすると
,＝≧1のとき 本のアーク（または本数を枝の上に書い
き，＋， ， は正則表現であり，それぞれ集合
た1本のアーク）を頂点 から頂点 へ向けて描く．また，初期
∪， （連接）， （クリーネ閉包）を表現する．
マーキングは各プレース 内に 個の●［トークン（token）
(５) 上記の(１)～(４)を有限回用いて導かれるもの以外は正則表
と呼ぶ］を描くことにより表す．
現ではない．
各プレースに存在するトークンの個数を表す写像:を
例えば，∑＝,のとき，正則表現 は言語 のクリ
マーキング（marking）と呼ぶ．マーキングはオートマトンにおけ
ーネ閉包，すなわち文字列 を任意個並べてできる文字列全体の
る一つの状態に対応し，初期値 からトランジションの発火によ
集合を表現する．
り推移していく．トランジション はマーキング において次の
図7･1の決定性有限オートマトンにおいて，＝αβγ
ε＋αε＋α＋β＋γβ，＝αβγαβである．
d.出力付きオートマトン オートマトンに出力を付けたモデ
ルとして，ムーア機械（Moore machine）とミーリー機械
（Mealy machine）がある．いずれにおいても，出力記号の集合
および出力関数ξが定義される．ムーア機械ではξ:であ
り，出力が状態の関数として定義される．ミーリー機械ではξ:
×∑であり，状態遷移に付随して出力が定義される．二つのモ
デルは次の意味で等価である．一方の機械が与えられたとき，任意
の入力文字列に対し，ムーア機械の入力εに対する出力を除いて
等しい出力を与える他方の機械が存在する．
e .時間付き離散事象システム 遅延やタイムアウトなどの事
図7･2 TDES
象発生に伴う時間制約を考慮するように拡張したモデルとして，時
表7･2ペトリネットの種類
モデルの種類 特徴
プレース/トランジションネット 基本モデル．事象生起の論理的関係
決定性時間ペトリネット 決定性時間の導入．
確率的時間ペトリネット 確率的時間の導入．
一般化確率ペトリネット 論理的記述と確率的時間の融合．
高水準ペトリネット 抽象データ型や1階述語論理による
図7･1決定性有限オートマトン
状態遷移規則の記述．
の記述．
(ａ)プレース/トランジションネット
条件を満たすときに発火可能であるという．
すなわち，トランジション は各入力プレース 上に，その間の
アークの重みである,個以上のトークンが存在するとき発火
可能になる．発火可能なトランジションは発火することができる．
(ｂ)CTMC
(ａ) 確率ペトリネット
(ｂ) の発火後のマーキング
(ｃ) の発火後のマーキング
図7･3プレース/トランジションネットと発火
∀∈:≧,
トランジション の発火により次のマーキング′に変化する．
図7･4確率ペトリネットと付随するCTMC
′＝＋,－,(∈)
トランジションの発火の様子を図7･3(ｂ)(ｃ)に示す．
キングに依存した値として与えられる．発火遅延時間の確率密度関
上記のプレース/トランジションネットでは，各プレースに任意
数は＝λで与えられ，その平均値は1λである．
個のトークンを置くことができる．これに対し，各プレースに対し
(２)CTMCの状態の集合は確率ペトリネットの到達可能なマ
て置くことのできるトークン数の上限が与えられたペトリネット
を，有限容量ペトリネット（finite capacity Petri net ）という．
ーキング集合とする．確率ペトリネットの到達可能なマーキング
にCTMCの状態 を1対1に対応させる．
初期マーキングからトランジションの発火により到達可能な各マ
(３)状態 から状態 への推移率を＝ ∑ λとする．こ
ーキングを頂点とし，一つのトランジションの発火による遷移をア
こで， はマーキング において発火可能であり，かつ，発火
ークとするような有向グラフを，プレース/トランジションネット
によりマーキングを に推移させるトランジションの集合であ
の状態遷移図という．プレース/トランジションネットの状態遷移
る．
図に対応するオートマトンは決定性である．
c .決定性時間ペトリネット プレース/トランジションネット
に決定性の時間を導入したモデルが時間ペトリネット（timed
Petri net ）である．時間の導入の方法により以下のように分類で
きる．
(１) 発火遅延時間モデル
各トランジションにそのトランジションが発火可能になってから
実際に発火するまでの遅延時間を付随させたモデル．
(２) 発火継続時間モデル
各トランジションに発火継続時間を付随させたモデル．
(３) プレース時間モデル
各プレース に次のような非負整数値θを付随させたモデ
ル．プレース に新しく追加されたトークンはθ時間後でない
とトランジションの発火に利用できない．
時間導入の別の方法として，時間付き離散事象システムと同様
に，各トランジション に二つの数値（発火可能になってから実
際に発火するまでの遅延時間の最小値 および最大値 ）を付随
させるモデルがある．トランジション は発火可能になってから
時間経過しないと発火できず，また，発火可能性が保たれてい
る限り， 時間経過するまでに必ず発火しなければならない．こ
のモデルをタイムペトリネット（time Petri net ) と呼び，時間ペ
トリネットと区別する．
d.確率的時間ペトリネット 各トランジションに対し，発火
可能となってから実際に発火するまでの遅延時間を指数分布に従う
確率分布で与えたモデルを，確率ペトリネット（stochastic Petri
net ) という．確率ペトリネットの状態遷移図は連続時間マルコフ
チェーン（CTMC:continuous time Markov chain）と同型にな
る．
図7･4は確率ペトリネットと対応するCTMCである．CTMC
の各状態と対応する確率ペトリネットのマーキング,,
,,は次のとおりである．状態1：1,0,0,0,0，
状態2：0,1,1,0,0 ，状態3：0,1,1,0,0 ，状態4：0,0,1,1,0，
状態5：0,0,0,1,1 ．状態間の矢印は，カッコ内に示したトラン
ジションの発火による状態遷移に対応している．
指数分布以外の確率分布を許すモデルを，拡張確率ペトリネット
（extended stochastic Petri net ）と呼ぶ．また，決定性の発火遅延
時間をもつトランジションと確率的な発火遅延時間をもつトランジ
ションの混在を許したモデルとして，決定性/確率ペトリネット
（deterministic and stochastic Petri net ）がある ．
処理時間が大きく異なるような動作が混在しているようなシステ
ムでは，短い処理時間をもつ動作は処理時間が零と考えても実際上
構わない．そして，大きな処理時間をもつ動作についてのみ時間を
割当てればよい．結果として，対応するCTMCの状態数を減らす
ことができる．このような目的で提案されたモデルとして，確率的
な発火遅延時間をもつ時間トランジションと，遅延零で発火する即
時トランジションの2種類のトランジションをもつ，一般化確率ペ
トリネット（generalized stochastic Petri nets ）がある ．
e.高水準ペトリネット 大規模なシステムをモデル化すると
き，通常のペトリネットでは同型な構造をもつ部分ネットが数多く
生成され，システムを記述するうえで大きな負担となる．この問題
を解消するために提案されたモデルとして，述語/トランジション
ネット（predicate/transition net ) やカラーペトリネット
（coloured Petri net ) などの高水準ペトリネット（high-level
Petri net ）がある．
カラーペトリネットでは各プレースに対して抽象データ型が割当
てられる．アーク上には変数や式を記述することができ，トランジ
ションには発火可能条件であるガード条件を記述することができ
る．図7･5はカラーペトリネットの例である．プレースInおよび
確率ペトリネットから，それに付随するCTMCは以下のように
作られる．
(１) 各トランジション に発火率λが固定値，または，マー
図7･5 カラーペトリネット
.可逆性（reversibility) 状態数が有限であり，かつ，
定常動作を続けるシステムには，必ずある状態から一連の動作を行
って元の状態に戻るという動作系列が存在する．到達可能な任意の
マーキングから初期マーキングが到達可能ならば，ペトリネットは
可逆であるという．
.公平性（fairness ) 一般的には，公平性は実行可能な
複数のプロセスが特定のプロセスに片寄ることなく実行されるとい
う性質である．公平性の定義にはさまざまなものがあるが，その一
つである有界公平性（bounded fairness ）は次のように定義され
る．二つのトランジション ， は，一方が発火しない間に他方が
Outにはデータ型Productが，プレースMachine poolにはデータ
型Machineが割当てられている．データ型は，整数，実数，文字
列など基本的な型に加え，要素を指定した集合，インデックス付き
集合，既存の型の組合せによる複合型などさまざまなものが可能で
ある．この例では，型Productは集合 Big,Small として，また
型Machineは集合,として定義されており，それらの各要素
が一つのトークン（色付きトークン）に対応する．そして，各プレ
ースには指定された型をもつトークンのみが存在を許される．
プレースIn内の式2Big＋1Smallは，データ型Productをもつ
トークンBigが二つ，Smallが一つ存在することを表す．トラン
ジションProcessにはガード条件が記述されており，プレースIn
から入力されるトークンの値により，プレースMachine poolのど
の種類のトークンを発火に用いるかを指定している．この例では，
まず変数 にプレースInの一つのトークンが，また，変数 にプ
レースMachine poolの一つのトークンが束縛される．ガード条件
［if＝Big then＝else＝］はアーク上の変数により定義さ
れた述語である．入力プレースにアークの重み以上トークンが存在
するというペトリネットの発火条件に加え，ガード条件を満たした
ときにのみトランジションが発火できる．この場合，＝Big，＝
，または，＝small ，＝の組合せに対してのみ，トランジシ
ョンProcessが発火可能である．発火により，プレースInから変
数 に束縛されたトークンがプレースOutに移動する．また，変
数 に束縛されたトークンはプレースMachine poolに戻される．
f .ペトリネットで扱われる問題 ペトリネットで扱われる代
表的問題は以下の性質の判定問題，あるいは，それらの性質が成り
立つようにシステムを設計する問題である．
.到達可能性（reachability) 与えられたマーキングが，
初期マーキングからトランジションの発火により到達可能であるか
どうかを判定する問題である．他の多くの問題が到達可能性の判定
問題に帰着することから，ペトリネットの解析における基本的問題
の一つである．
.活性（liveness ) 到達可能な任意のマーキングから各
トランジションが発火可能なマーキングへ遷移可能であるという性
質である．どのような状態遷移を行っても，特定の動作が実行不能
になることはないという状況に対応する．
.有界性（boundedness )・安全性（safeness ) 有界性と
は各プレースに入りうるトークン数に上限が存在する性質をいう．
上限が1の場合の有界性を特に安全性という．非有界なペトリネッ
トではプレース内のトークン数が無限に増加しうる．これは現実の
システムでは，例えばバッファのオーバフローを発生させる可能性
があることを意味する．
.保存性（conservation) 保存性は事象がどのように生
起しても変化しないような資源の存在に対応するものである．プレ
ース/トランジションネット＝,,, ，および各プレー
スへの重み:を与えたとき，任意の到達可能なマーキン
グ について∑＝∑ が成り立つとき，
は に対して保存的であるという．この性質は，有限個の資
源を複数のプロセスが交互に使用するようなシステムを設計すると
きに重要である．
発火できる回数がある非負整数値以下であるとき有界公平であると
いう．任意の二つのトランジションが有界公平であるとき，ペトリ
ネットは有界公平であるという．
g.ペトリネットの解析方法
.状態空間生成 到達可能なマーキングの集合が有限なら
ば，ペトリネットの状態遷移図を求めることにより，ペトリネット
の挙動に関するどのような問題も状態遷移図上で判定することがで
きる．しかしながら，ペトリネットのサイズに対して状態遷移図の
サイズが指数関数的に増大するという状態空間爆発（state space
explosion）の問題があり，単純な状態空間生成では現実規模のモ
デルを扱うことは困難である．このため，以下のような方法が提案
されている．
(１)ペトリネットの縮約
特定の性質を保存したままでより小さいペトリネットに変換する
手法．
(２)状態遷移図の縮約
状態遷移図の対称性，同じ動作系列の繰返しなどを利用した縮約
表現を用いる方法．
(３)半順序法（partial order method)
動作の並行性を利用して，冗長な状態遷移を除いた縮約状態空間
を生成する方法．
(４)二分決定グラフ（binary decision diagram) の利用
二分決定グラフはブール関数を効率的に表現するための方法であ
る．特定の性質を満たすマーキング集合を二分決定グラフの形で計
算することができる．
.特定のグラフ的構造をもつ場合の解析 ペトリネットの
グラフとしての構造に制限を加えたいくつかのサブクラスに対して
は，状態遷移図を計算することなく到達可能性および活性を判定す
るための必要十分条件が得られている．このようなサブクラスとし
ては，マークグラフ（marked graph），状態機械（state
machine），無競合ペトリネット（conflict -free Petri net ），自由選
択ネット（free - choice net ）などがある．
.インバリアントの計算 システムの不変量（インバリア
ント）は動作の検証において重要な手掛かりを与える．ペトリネッ
トでは以下の二つの不変量が用いられる．＝,,,を
プレース/トランジションネットとし，×の非負整数行列
＝を＝,－, （∈,∈）により定義す
る． を の接続行列（incidence matrix）という．
(１)トランジションインバリアント（transition invariant ，
T- invariant )
斉次方程式＝を満たす でない整数ベクトル∈
（ ：整数の集合）．非負のトランジションインバリアントは，一連
の動作を行って再び元の状態に戻るような発火系列の発火回数に対
応しており，可逆なネットには必ず存在する．
(２)プレースインバリアント（place invariant ，P- invariant )
斉次方程式＝を満たす でない整数ベクトル∈ ．プ
レースインバリアントにより重み付けられたトークン数の和は状態
遷移における不変量になる．
