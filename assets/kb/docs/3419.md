# 7・2・1 スーパーバイザ制御と可制御性

# 7・2・1 スーパーバイザ制御と可制御性

> **章**: 第7章 離散事象システム
> **節**: 7・2 スーパーバイザ制御

と分岐時間時相論理（branching time temporal logic）の2種類
a.離散事象システムにおける制御問題
に分類される．
.排他制御（mutual exclusion) 複数の計算機が1台の
.線形時間時相論理 次状態が一意に決定される場合に対
プリンタを共有しているとき，プリンタを利用できる計算機はたか
する論理である．論理式は原子命題（それ以上分解できない命題），
だか1台である．したがって，ある計算機が使用している間は他の
および，それらが論理演算子により結合されてできた命題からな
計算機の使用を禁止しなければならない．このように，複数のプロ
る．論理式の解釈は次状態が一意に決定されるような遷移関数をも
セスが同時に存在するとき，同時に利用できるプロセスの数が限ら
つ構造＝,,πにより与えられる．ここで， ：状態の可算
れている資源を有効に利用するための制御を排他制御という．
集合，:：次状態を与える遷移関数，π：各状態に真とな
.並行処理制御（concurrency control ) データベースで
る原子命題の集合を割当てる写像である．例えば，構造 および
状態∈に対して が真となる（,＝ ）のは次
の場合である． からの状態系列,,＝,
, …において，ある が存在して で が真であり，
かつ，任意の＜に対し で が真である．
過程が初期状態からの木として表される場合に対する論理である．
例えば，分岐時間論理の一つであるCTL（computation tree
logic）では，論理式の解釈は構造＝,,πにより与えられ
る． ，πは線形時間論理と同じであるが，⊆×は次状態を
与える関係である［,′∈は′が の次状態であることを表
す］．ある状態からの遷移可能な状態系列（パス）は一般に複数個
存在する．線形時間論理は一つのパスに対する論理であるといえ
る． をパスに対する論理式としたとき，どのようなパスで が成
立するかを表現するために，すべてのパスで成立する（∀），あ
るパスで成立する（∃）の2種類のパス限量記号が用いられる．
を与える構造と見れば，時相論理により離散事象システムの動的な
性質が記述できる．
に拡張された論理として，実時間時相論理（real -time temporal
logic）がある．時相演算子に数値の区間を付随させる［例：□
（ ◇ が真になってから3単位時間内に が真になる］，陽
にクロック値を表す定数を導入する［例：∀. □∧＝◇
∧≦＋3 ］などいくつかの方法がある．
のシステムを検証するための手法であり，三つのステップからな
る ．
(１) モデル化 設計したシステムをオートマトンやペトリネッ
の有効利用やプロセス間の干渉を抑える．このことは，オペレーテ
は複数のトランザクションを同時に実行するので，あるデータが複
数のトランザクションから読み書きされることがある．このとき，
データの内容が矛盾することなく，並行性をできる限り損なわない
ようにデータへのアクセスを制御しなければならない．このような
制御を並行処理制御という．
.分岐時間時相論理 次状態が一意ではなく，状態遷移の
.デッドロックの回避（deadlock avoidance) デッドロ
ックとは複数のプロセスが干渉し合ってお互いに処理ができなくな
ることをいう．デッドロックが発生するとあるプロセスをリセット
させることが一般的である．したがって，デッドロックを避けるよ
うに制御を行うことは重要である．デッドロックは排他制御や並行
処理制御においてもしばしば問題となる．
.ライブロックの回避（livelock avoidance) 事象が生
起するにもかかわらず，いつまでたっても目標状態に到達しないよ
うな現象をライブロックという．ライブロックとデッドロックは目
標状態には達成しない点では共通であるが，デッドロックは完全に
状態遷移が止まるのに対して，ライブロックは状態遷移が起こる点
オートマトンやペトリネットの状態遷移図をこれらの論理の解釈
が異なる．
.安定化（stabilization) 通信システムにおいては各サ
イト間で来るべきメッセージが来ないことがある．このようなとき
c .実時間時相論理 数値で与えられる時間制約を扱えるよう
に正常な状態に戻るような制御が重要である．特に，自動的に正常
な状態に戻るようなプロトコルは自己安定（self - stable）であると
呼ばれる．
.公平性 生起する事象があるプロセスに関係するものば
かりではなく，どのプロセスに関係する事象もいつかは生起できる
ことを公平（fair ）であるという．例えば，プリンタの使用では，
d.モデル検査 モデル検査（model checking）とは有限状態
使用要求を出したプロセスから順番に使用を許可するFIFO
（first - in first - out ）制御が公平な制御の典型的な例である．
.統合問題（coordination problem) 複数のプロセスが
同時進行しているとき，各プロセスが行う処理自体には全く制約を
トなどの形式的モデルに変換する．
与えず，その処理の相対的な順番のみを制御することによりCPU
ィングシステムの設計上重要な問題であり，統合問題と呼ばれる．
なる．また， の受理言語に対して制約が与えられる場合に
排他制御，並行処理制御，デッドロックの回避，公平な制御等を適
は，制御仕様が受理言語⊆で与えられ，＝
用することによって統合問題は解決される．
となるスーパーバイザ を求めることになる．制御仕様を満たす
.スーパーバイザ制御（supervisory control ）と状態フィー
言語を制御仕様と呼ぶことにする．また，一般に，⊆
ドバック制御（state feedback control ) システムが大規模化す
である．＝となるとき， はブロッキングがないス
ると，統合問題のように制御仕様もさまざまな部分制御問題が複雑
ーパーバイザであるという．
に絡み合うことになる．そこで，システマティックに制御系を構成
するために提案された手法がスーパーバイザ制御と状態フィードバ
言語⊆が
ック制御である．いずれの方法でも，システムが制御仕様を満たす
を満たすとき，［∑と に関して］可制御（controllable）と
ように次に生起できる事象を制限することで制御を行っている．ス
ーパーバイザ制御では，制御仕様を形式言語で与えて，制御対象が
いう．
生成する事象列に基づいて制御を行う．状態フィードバック制御で
を満たすとき， は -閉（ -closed）であるという．
は，制御仕様を状態上の述語で与え，システムの現在の状態に基づ
［定理1］ はブロッキングがない離散事象システムとする．こ
いて制御を行う．
のとき，
b.スーパーバイザ制御問題の定式化 制御対象となる
離散事象システムはオートマトン＝,∑,δ,,で表す．た
(１)空でない接頭語閉な言語⊆に対して，＝
だし，∑：事象の集合， ：状態の集合，δ:∑×：状態遷
を満たすスーパーバイザ が存在するための必要十分条件は，
が可制御となることである．
移関数，∈：初期状態，⊆：目標状態を表す．一般にδ
は部分関数であり，定義されているとき，δσ, !と書くことにす
満たすブロッキングがないスーパーバイザ′ が存在するための必
る（7･1･2･b項参照）．一般に⊆であるが，＝
要十分条件は， が可制御かつ -閉となることである．
となるとき， はブロッキングがない離散事象システムであ
d.可制御性判定アルゴリズム オートマトン＝,∑,
るという．
δ,,に対して＝∈;∃∈∑＝δ,，δを
各状態で，生起が許される事象の集合を制御パターンという．こ
∑×に制限した関数をδ ∑× ，＝∩
のとき，その生起が外部から禁止できる事象と禁止できない事象が
ある．前者を可制御事象，後者を不可制御事象という．例えば，タ
とおいて，オートマトン＝,∑,δ ∑× ,,
スクの開始，プリンタのような資源へのアクセスなどは可制御事象
∈，δを∑×に制限した関数をδ ∑× ，＝
である．また，システムダウン，タスクの終了等は不可制御事象の
∩とおいて，オートマトン＝,∑,
例である．可制御事象と不可制御事象の集合をそれぞれ∑ ，∑
δ ∑× ,,と定義する．また，trim＝
とおく．
と定義する．trimを求めることを，「trimを取る」という．
∑＝∑∪∑
二つのオートマトン＝,∑,δ,, （＝1,2）に対し
∑∩∑≠
て，以下の3条件が成り立つとき， は の部分オートマトン
制御パターンγは，不可制御事象を含む∑の部分集合となる．Γ
であるといい，⊆と書く．
を制御パターンの集合とおく．
･⊆
Γ＝γ;∑⊆γ⊆∑
･＝
離散事象システム に対して制御パターンの集合Γによって制御
･δ, ! ⇒δ, ! ∧δ,＝δ,
される離散事象システム は式(7･1)のようになる．
明らかに，⊆ならば⊆であるが，この逆は成立
＝,Γ×∑,δ,, (7･1)
しない．
ただし，状態遷移関数δ:Γ×∑×は次式で定義される．
制御仕様 が空でない接頭語閉な正規言語で， も有限オート
δγ,σ,＝δσ,ifδσ, ! ∧σ∈γ
undefined otherwise
マトンであるとする． を生成するオートマトンを＝,∑,ξ,
,とおく．ここで，オートマトン と の共通部分∩
事象が生起するごとに制御パターンを割当てる（スイッチする）こ
とバイアスのある同期合成 を次のように定義する．
とによって，制御が行われる．この制御パターンを割当てる制御器
はスーパーバイザと呼ばれる．スーパーバイザ は形式的には
ただし，
からΓへの関数として定義され，オートマトン＝,∑,
ξ,,と関数Ψ:Γの組 ＝,Ψによって表現でき
る．スーパーバイザ によって制御された離散事象システム，す
である．
なわち閉ループシステム は次のオートマトンで表される．
＝×,∑,δ×ξ,,,× (7･2)
ただし，
ただし，δ×ξσ,,＝δΨ,σ,,ξσ,である．
において生起した事象に従ってオートマトン の状態は遷移する．
において次に生起可能な事象は，遷移後の の状態に対し
てξによって割当てられた制御パターンの要素でなければならな
い．また，スーパーバイザによって割当てられた制御パターンの要
である．
素であっても， において生起ができない事象は，制御を行って
［補題1］ ＝,∑,δ,,は有限オートマトン，＝,
も決して生起できないことに注意してほしい．このような意味で
∑,ξ,,は空でない接頭語閉な正規言語⊆を生成する
「制御パターンによって事象の生起は許容される」という言い方を
有限オートマトンとする．
することがある．
･∩＝
c.スーパーバイザの存在条件 システムが接頭語閉な
･＝
言語⊆しか生成できないようにすることが制御仕様である
･∩⊆である．すなわち，任意の∈∩に対し
場合には，＝となるスーパーバイザ を求めることに
て，δ∩ξ,,＝δξ,,である．
∑∩⊆ (7･3)
∩＝ (7･4)
(２)空でない言語⊆に対して，′＝を
と定義する．さらに，＝∈;∃∈∑δ,
∩＝×,∑,δ∩ξ,,,×
δ∩ξσ,,＝δσ,,ξσ,ifδσ, ! ∧ξσ, !
undefined otherwise
＝×,∑,δξ,,,×
δσ,,ξσ,
δσ,,
δξσ,,＝
otherwise
ifδσ, ! ∧ξσ, !
ifδσ, !
∧ξσ,は未定義
undefined
［補題2］ ， はそれぞれ ， を生成するオートマト
＝＝,∈;δσ, ! だが
ンで は の部分オートマトンとおく．ただし，＝,∑,
ησ,は未定義となるσ∈∑が存在する．
δ,,である． が可制御であることと下記の条件は等価であ
ステップ3： から可制御な事象を削除して， ∑＝,∑,
る．
η ∑×,,を求める．
の任意の状態∈とσ∈∑に対して，
ステップ4： ∑において， に到達するパスがある状態の集
δσ, !⇒δσ, ! (7･5)
合 を求める．
補題1より＝∩，＝と選べば，つねに
＝∈－;η,∈となる∈∑が存在する．
⊆となる．補題2より， の可制御性は，以下のアルゴリズ
ステップ5： から∪とそれに関連するアークを削除して
ムによって判定できる．
得られるオートマトンは を生成する．
(可制御性判定アルゴリズム)
このアルゴリズムの計算量は であり，計算量的に最適であ
ステップ1：＝∩と＝を求める．
る．
ステップ2： の各状態 において任意のσ∈∑に対して，
