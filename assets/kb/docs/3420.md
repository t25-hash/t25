# 7・2・2 部分観測と可観測性

> **章**: 第7章 離散事象システム
> **節**: 7・2 スーパーバイザ制御

が成立するかどうかを調べる．
成立していれば可制御であり，成立していなければ可制御ではない．
上記のアルゴリズムの計算量は×となる．
ただし，は集合の要素数を表す．
e.最大可制御部分言語 制御仕様が可制御でない言語によって与えられたときは， の部分言語の中で，集合の包含関係に関して最大となる可制御言語に対してスーパーバイザを設計することが一般的である．
の部分言語の中で可制御な言語の集合を とおく．
［命題1］ は空でないとする．
さらに， は集合の和に関して閉じている．
を次のように定義する．
命題1より，任意の⊆に対して は集合の包含関係に関して最大となる可制御言語である．
を最大可制御部分言語（su-外の非可観測事象を取り除くことによって得られる事象列である．
の最大可制御部分言語の計算アルゴリズムは以下のとおりであ［ の計算アルゴリズム］成する．
であるが，δσ,は未定義となるσ∈∑が存在すを求める．
［ の計算アルゴリズム］である．
f .最大可制御部分言語の計算 と が正規言語のときはスーパーバイザによって観測される事象列を表している．
また，と定義する．
さらに，任意の言語⊆∑に対して，と定義し，任意の言語′⊆∑に対して，部分観測のもとでは，スーパーバイザ は形式的にはからΓへの関数として定義され，状態遷移関数ξが次式の条件をΓの組 ＝,Ψによって表現できる．
化せず，制御パターンが切換わらないことを意味する．
b.スーパーバイザの存在条件 言語⊆が任意の,′∈と任意のσ∈∑に対して，を満足するとき可観測（observable）であるという．
が可観測でないならば，＝′，σ∈，′σ∈－となるようイザは観測事象列より と′を区別することができず，σに対して同じ制御動作を行うことになる．
しかし， においてはσ∈よりσを許可すべきであり，′においては′σ よりσを禁止る．
a.部分観測 7･2･1項では，スーパーバイザがすべての事象の生起を観測できる完全観測を仮定した．
しかし，センサの制限などにより，一般にシステム外部からその生起が観測できない事象が存在する．
このような場合を部分観測（partial observa-tion）と呼ぶ．
部分観測のもとでは，事象の集合∑はその生起が観測できる事象の集合と，観測できない事象の集合とに分割される．
前者を可観測事象（observable event）の集合，後者を非可観測事象（unobservable event）の集合といい，可観測事象の集合を∑とおく．
部分観測を表現するための関数:∑∑∪εを次式で定義する．
関数 は以下のように:∑∑へと拡張できる．
任意の事象列∈∑に対して，∈∑は から∑の要素以る．
を ＝，＝，⊆を満たすように構とそれに関連する遷移を取り除いて，trimを取る．
これを のすべての状態に対して繰返し行い，オートマトンテップ4に移る．
そうでなければ，＋1を と置換えて，ステッで構成したとき，上のアルゴリズムの計算量は となものがある．
［定理2］ はブロッキングがない離散事象システムとする．
ステップ2：オートマトン から次の条件を満たす状態∈と定義する．
と の状態数を， とおく．
もし ＝，＝∩が接頭語閉の場合には，上のアルゴリズムよりも効率の良いな,′∈とσ∈∑が存在する．
＝′より，スーパーバは接頭語閉であると仮定する．
ステップ1：＝∩＝,∑,η,,を構成する．
たすべきである．
すなわち， と′の生起後のσの制御動作が定まらなくなる．
したがって，可観測性（observability）は各可制御事象の制御動作が部分観測のもとでも必ず定まるための条件であステップ2：集合∈を求める．
