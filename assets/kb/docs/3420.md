# 7・2・2 部分観測と可観測性

# 7・2・2 部分観測と可観測性

> **章**: 第7章 離散事象システム
> **節**: 7・2 スーパーバイザ制御

式(7･5)が成立するかどうかを調べる．成立していれば可制御であ
a.部分観測 7･2･1項では，スーパーバイザがすべ
り，成立していなければ可制御ではない．
ての事象の生起を観測できる完全観測を仮定した．しかし，センサ
上記のアルゴリズムの計算量は×となる．ただし，
の制限などにより，一般にシステム外部からその生起が観測できな
は集合の要素数を表す．
い事象が存在する．このような場合を部分観測（partial observa-
e.最大可制御部分言語 制御仕様が可制御でない言語
tion）と呼ぶ．部分観測のもとでは，事象の集合∑はその生起が
によって与えられたときは， の部分言語の中で，集合の包含
観測できる事象の集合と，観測できない事象の集合とに分割され
関係に関して最大となる可制御言語に対してスーパーバイザを設計
る．前者を可観測事象（observable event ）の集合，後者を非可観
することが一般的である． の部分言語の中で可制御な言語の集
測事象（unobservable event ）の集合といい，可観測事象の集合を
合を とおく．
∑とおく．部分観測を表現するための関数:∑∑∪εを次
＝;⊆,：可制御
式で定義する．
［命題1］ は空でないとする．このとき は空集合でな
σ＝σ
otherwise
ifσ∈∑
い．さらに， は集合の和に関して閉じている．
ε
を次のように定義する．
関数 は以下のように:∑∑へと拡張できる．
＝
･ε＝ε
命題1より，任意の⊆に対して は集合の包含関係に関
･σ＝σ,∀∈∑,σ∈∑
して最大となる可制御言語である． を最大可制御部分言語（su-
任意の事象列∈∑に対して，∈∑は から∑の要素以
外の非可観測事象を取り除くことによって得られる事象列である．
premal controllable sublanguage）と呼ぶ．
の最大可制御部分言語の計算アルゴリズムは以下のとおりであ
る ．
［ の計算アルゴリズム(１)］
,を ＝，＝，⊆を満たすように構
成する．
とそれに関連する遷移を取り除いて，trimを取る．
･δσ, !であるが，δσ,は未定義となるσ∈∑が存在す
これを のすべての状態に対して繰返し行い，オートマトン
を求める．
テップ4に移る．そうでなければ，＋1を と置換えて，ステッ
プ2へ戻る．
で構成したとき，上のアルゴリズムの計算量は とな
る ．
ものがある ．
［ の計算アルゴリズム(２)］
だし，
である．
［定理2］ はブロッキングがない離散事象システムとする．こ
f .最大可制御部分言語の計算 と が正規言語のとき
よって，システムにおいて事象列∈が生起したとき，
はスーパーバイザによって観測される事象列を表している．また，
関数:∑2∑を
＝∈∑;＝
ステップ1： ＝trim,∑,δ,, と＝trim,∑,δ,
と定義する．さらに，任意の言語⊆∑に対して，
＝∈∑∃∈;＝
と定義し，任意の言語′⊆∑に対して，
ステップ2：オートマトン から次の条件を満たす状態∈
′＝
と定義する．
部分観測のもとでは，スーパーバイザ は形式的には
る．
からΓへの関数として定義され，状態遷移関数ξが次式の条件を
満足するようなオートマトン＝,∑,ξ,,と関数Ψ:
Γの組 ＝,Ψによって表現できる．
ステップ3：ステップ2において削除された状態がなければ，ス
ξσ,＝,∀∈,σ∈∑－∑
この条件は，非可観測事象が生起してもスーパーバイザの状態は変
化せず，制御パターンが切換わらないことを意味する．
ステップ4：＝
b.スーパーバイザの存在条件 言語⊆が任
と の状態数を ， とおく．もし ＝，＝∩
意の,′∈と任意のσ∈∑に対して，
＝′∧σ∈∧′σ∈⇒′σ∈
を満足するとき可観測（observable）であるという． が可観測
が接頭語閉の場合には，上のアルゴリズムよりも効率の良い
でないならば，＝′，σ∈，′σ∈－となるよう
な,′∈とσ∈∑が存在する．＝′より，スーパーバ
イザは観測事象列より と′を区別することができず，σに対し
は接頭語閉であると仮定する．
て同じ制御動作を行うことになる．しかし， においてはσ∈
ステップ1：＝∩＝,∑,η,,を構成する．た
よりσを許可すべきであり，′においては′σ よりσを禁止
すべきである．すなわち， と′の生起後のσの制御動作が定ま
＝×
らなくなる．したがって，可観測性（observability）は各可制御
＝,
事象の制御動作が部分観測のもとでも必ず定まるための条件であ
る．
ステップ2：集合∈を求める．
のとき，
