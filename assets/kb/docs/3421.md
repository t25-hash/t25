# 7・2・3 分散スーパーバイザ制御

> **章**: 第7章 離散事象システム
> **節**: 7・2 スーパーバイザ制御

空でない接頭語閉な言語⊆に対して，部分観測のバイザがそれぞれ一部の事象の生起のみを観測・制御することにより，システム全体のふるまいを制御しようという方式である．
ローカルスーパーバイザ （∈＝1,2,…,）が禁止できる事象の集合を∑，禁止できない事象の集合を∑とおく．
ただし，とする．
ここで，∑は互いに排反である必要はない．
すなわち，一つの可制御事象を複数のローカルスーパーバイザが制御する場合を許している．
の制御パターンの集合Γはとなる．
また， は部分集合∑⊆∑の要素の生起のみが観測可能であるとする．
写像:∑∑は と同様に定義される．
形式的に は :Γと定義され，状態遷移関数ξそこで，言語のノーマル性（normality）という概念を導入する．
言語⊆がを満足するとき，ノーマル（normal）であるという．
［命題2］ 任意のノーマルな言語⊆は可観測である．
しかし，一般に命題2の逆は成立しない．
すなわち，任意の可観とき， の要素以外を禁止すべきと判断する．
分散スーパーバイザ制御においては，可制御事象の禁止に関するこの各ローカルスーパーバイザの判断を統合するルールが必要である．
各可制御事象σ∈∑に対して，σを制御できるローカルスーパーバイザのうち，少なくとも一つがその生起を禁止すべきと判断したならば，閉ループシステムで禁止されるというルールをORルールと呼ぶ．
また，σを制御できるすべてのローカルスーパーバイザがその生起を禁止すべきと判断したときに，閉ループシステムで禁止されるというルールをANDルールと呼ぶ．
ここでは，ANDルールとORルールの二つのルールを考える．
＝なる が存在するならば，＝である．
が正規言語であり， が有限オートマトンであるならば，上記の反復計算は有限ステップで収束し， を計算することができるが，その計算量は指数オーダとなる．
ーパーバイザ によってその生起後に禁止される事象の集合⊆∑は次のように定義される．
計算においても，可制御性は保存されない．
なる反復計算において，＝なる が存在するならば，＝である．
が正規言語であり， が有限オートマトンであるならば，上記の反復計算は有限ステップで収束し， を計算することができる．
命題2より， が可制御可観測でない場合，servable）であるという．
な事象がすべて可観測ならば次の性質が成り立つ．
［命題4］ ∑⊆∑とする．
このとき，任意の可制御可観測な言語⊆はノーマルである．
マル部分言語と一致し，つねに存在する．
任意の′∈に対して，と関数Ψ:Γの組 ＝,Ψによって表現できる．
可制御事象の集合∑は，ANDルールで制御される事象の集合言語⊆が次の二条件を満足するとき，共可観測（coob-直感的にいえば，共可観測性（coobservability）は，ANDルールで制御される事象に対しては許可すべきとき，ORルールで制御ローカルスーパーバイザが少なくとも一つ存在することを意味す［定理3］ はブロッキングがない離散事象システムとする．
の∑と∑への分割のもとで，分散スーパーバイザが存在するた注意されたい．
したがって， が可制御であるとき，分散スーパーバイザが存在するためには， が共可観測となるような∑のと仮定する．
は有限オートマトンであり，空でない言語⊆は正規ン＝,∑,δ,,が存在する．
まず，非決定性有限オーを定義する．
任意の＝,,,∈×××と任意のσ∈∑が成り立つならば，∈δσ,である．
そして，存在するための必要十分条件はさらに，非決定性有限オートマトンる．
任意の＝,,,,∈××××と任意任意の言語⊆∑に対して，用いて， が共可観測となるような∑の∑と∑への分割があるかどうかを判定し，もしあるならばそれを見つけることができる．
が共可観測となるような∑の∑と∑への分割がを表す．
もとで＝を満たすスーパーバイザ が存在するための必要十分条件は， が可制御かつ可観測となることである．
空でない言語⊆に対して，部分観測のもとで′＝を満たすブロッキングがないスーパーバイザ′が存在するための必要十分条件は， が可制御，可観測かつ-閉となることである．
制御仕様 は空でない正規言語であり， は有限オートマトンであるとする．
を生成するオートマトンを とし， と の状態数を， とおく．
可観測性は で判定できる．
c.可観測性とノーマル性 制御仕様が可制御かつ可観測とはならない言語 によって与えられたときは， の可制御可観測部分言語を求め，それに対してスーパーバイザを構成することになる．
しかし，最大可制御部分言語 とは異なり，一般に最大可制御可観測部分言語は存在しない．
これは，可観測性が集合の和において保存されないためである．
が次式の条件を満足するようなオートマトン＝,∑,ξ,,このような 個のローカルスーパーバイザの集合 を分散スーパーバイザと呼ぶ．
そして， で制御された閉ループシステムを表すオートマトンを と書く．
測な言語⊆はノーマルであるとは限らない．
d.最大可制御ノーマル部分言語 任意の言語⊆に対して，そのノーマル部分言語の集合を とおく．
［命題3］ 任意の言語⊆に対して， は空集合でない．
さらに， は集合の和に関して閉じている．
を次のように定義する．
命題3より， は の最大ノーマル部分言語である．
の計算のため，次の反復計算を考える．
∑とORルールで制御される事象の集合∑とに分割されるとする．
各可制御事象σ∈∑に対して，インデックス集合をσ＝∈;σ∈∑と定義する．
各事象列∈に対して，分散ス命題1と命題3より， の最大可制御ノーマル部分言語が存在し，それを とおく．
一般に，最大可制御部分言語の計算において，ノーマル性は保存されない．
また，最大ノーマル部分言語の計算するためには，不動点に収束するまで，最大可制御部分言語と最大ノーマル部分言語を繰返し計算する必要がある．
b.分散スーパーバイザの存在条件 ここでは，可制御事象の集合∑が，ANDルールで制御される事象の集合∑とORルールで制御される事象の集合∑とに分割されたもとでの，制御仕様に対する分散スーパーバイザの存在条件を述べる．
に対してスーパーバイザが設計できる．
一般に，可観測な言語がノーマルであるとは限らないが，可制御条件を満足する∈σが存在する．
任意の′∈に対して，命題4より，∑⊆∑という条件のもとでは，任意の言語⊆に対して，最大可制御可観測部分言語は最大可制御ノーて，次の条件を満足する∈σが存在する．
a.分散スーパーバイザ 分散スーパーバイザ制御（de-各ローカルスーパーバイザ は，事象列∈が生起したσ∈なる任意の∈と任意のσ∈∑に対して，次の σ∈－なる任意の∈と任意のσ∈∑に対しである．
される事象に対しては禁止すべきときに，その判断を正しく下せるる．
ここで，＝を満たす分散スーパーバイザ が存在するる．
を満たすブロッキングがない分散スーパーバイザ′が存-閉となることである．
c.可制御事象の分割 定理3より，可制御事象の集合∑めには，言語⊆が可制御かつ共可観測であることが必要である．
ここで， の可制御性は∑の分割には依存しないことに∑と∑への分割があるかどうかを判定し，もしあるならばそれを見つけることが必要である．
以下では，簡単のため＝2である言語であると仮定する．
また，任意のσ∈∑に対して，δσ,＝である．
であり，状態遷移関数δ:∑×2は以下のように定義されが成り立つならば，∈δσ,である．
また，任意のσ∈∑に対して，δσ,＝である．
上で定義した二つの非決定性有限オートマトン， を［定理4］ は有限オートマトン，⊆は空でない正規言が成り立つことである．
定理4の条件が成り立つとき，という分割のもとで， は共可観測となる．
