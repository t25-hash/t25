# 7・4・2 待ち行列理論

# 7・4・2 待ち行列理論

> **章**: 第7章 離散事象システム
> **節**: 7・4 パフォーマンス解析

∑
∈
用者によって共有されている状況を数学的に記述した確率モデル
を満たし，πは定常状態確率（steady-state probability）と呼ば
れる．もし，定常状態確率が存在するならば，それは唯一であり，
源を共有している状況では，利用したい資源が他の利用者によって
特に，状態数が有限である既約な離散時間マルコフ連鎖には必ず定
常状態確率が存在する．
c.連続時間マルコフ連鎖（continuous -time Markov chain）
.状態遷移確率 連続時間マルコフ連鎖 ;≧0に
おいて，条件付き確率Pr＋＝＝,∈ は遷移
求を客，共有資源をサーバ，客が共有資源を占有する時間をサービ
確率と呼ばれる．以下では，各状態にとどまる時間が確率1で正で
あり，かつ，遷移確率が に依存しないと仮定する．そこで
には，システム内客数分布や客の待ち時間分布などがある．
＝Pr＋＝＝,∈ とおき，遷移確率行列を
＝とする．この行列の各行は， の値が与えられ
たという条件のもとでの＋の分布を表しているため，
源を占有する時間に関する統計的情報（サービス時間過程），共有
は各要素が非負であり，かつ各行の総和が1となる正方行列であ
資源の数（サーバ数），システム内にとどまることが許される客の
る．ここで＝lim－ /によって定められる行列＝
最大数（システム容量）などによって記述される．このような待ち
行列モデルを記述する方法としてケンドールの記法（Kendall’s
は遷移率行列（transition rate matrix）あるいは無限小生成
notation）がある．これは，一般に /// の形をしており，
作用素（infinitesimal generator ）と呼ばれる．遷移率行列 の対
角要素 は負の値をとり，－は状態 から他の状態への遷移
率を与えており，状態 にとどまる時間はパラメータ－の指数
分布）， （一般分布：特定の分布を仮定しない）， （独立同一
分布（exponential distribution）に従う．一方，非対角要素
分布：特定の分布は仮定しないが，到着時間間隔あるいはサービス
は正の値をとり，状態 から状態 への遷移率を与えている．よっ
時間は互いに独立）などが用いられる．システム容量が無限大の場
て の各行の和は零である．遷移確率行列 は遷移率行列
を用いて＝expで与えられる．またπ＝Pr＝
合は単に //と書かれる．サービスの順序に関しては通常，先
着順サービスが仮定されている．
とし，行ベクトル＝πを定義すると，＝0
expが成立する．
.状態の分類と定常状態確率 連続時間マルコフ連鎖に対
しても，離散マルコフ連鎖と同様に，到達可能性や再帰性を考える
λを単位時間当たりの平均到着客数（これを平均到着率という）と
ことができる．時点0で連続時間マルコフ連鎖が状態∈ にあ
り，これ以降初めて状態∈ を訪れる時点τを状態 から状態
への初到達時間という．また，時点0で状態 にあり，これ以
降，いったん，他の状態に遷移したのち，再び状態 を最初に訪れ
る時点τは状態 の再帰時間と呼ばれる．＝Prτ＜∞とし
たとき，もし，＝1であるならば状態 は再帰的と呼ばれ，
で与えられる．一方， を時刻≧0におけるシステム内客数，
＜1ならば過渡的と呼ばれる．さらに，再帰的な状態 に対して平
を 番めに到着した客の待ち時間とすると，それらの平均
均再帰時間ν＝τを考える．もし，＝1かつν＜∞ならば
状態 は正再帰的と呼ばれ，＝1かつν＝∞ならば零再帰的と
呼ばれる．
ある≧0とある,∈ に対して＞0ならば，状態
は状態 から到達可能という．状態 と が互いに到達可能である
とき，状態 と は連結しているという．特に，状態 はそれ自身
非常に緩い仮定のもとで，リトルの公式
と連結である．離散時間マルコフ連鎖と同様に，連続時間マルコフ
連鎖においても，全状態の集合 を ＝ ∪ ∪…のように互
が成立する．
いに素な複数個の連結クラス の和集合で書くことができる．各
いはすべて過渡的かのいずれかである．連続時間マルコフ連鎖で
は，ある＞0に対して＞0ならば，すべての＞0に対して
すべての状態∈ が一つの連結クラスに含まれるような連続時
間マルコフ連鎖は既約と呼ばれる．既約な連続時間マルコフ連鎖に
を満たし，πは定常状態確率と呼ばれる．もし定常状態確率が存
待ち行列理論（queueing theory）とは，有限の資源が多くの利
（待ち行列モデル）に関する理論である．多くの利用者が有限の資
占有されている場合，他の利用者は資源が解放されるまで待つか，
あるいは，その資源の利用をあきらめなければならない．このよう
な待つという状況，あるいは競合により資源利用を拒否されるとい
う状況を抽象的に表すため，待ち行列理論では，通常，資源利用要
ス時間と呼ぶ．待ち行列理論において興味の対象となるおもな指標
a.待ち行列モデルの記法 待ち行列モデルは，共有の資源へ
の利用要求発生に関する統計的情報（到着過程），個々の要求が資
それぞれ，到着時間間隔分布/サービス時間分布/サーバ数/システ
ム容量を示している． ， に関しては （指数分布）， （一定
b.リトルの公式 定常な待ち行列モデルにおける平均システ
ム内客数 と平均システム内滞在時間 の間には，リト
ルの公式（Little’s law）と呼ばれる非常に単純な関係が成立する．
する．すなわち，0,を時間間隔0,における到着客数とし
たとき
λ＝lim0,
および は，それぞれ
＝lim1 d
＝lim1∑
で与えられる．もし と のいずれかが有限であれば，
＝λ
c .待ち行列モデルとマルコフ連鎖 任意の待ち行列モデルに
＝ 1－ρ,
＝0
おいて， を時刻≧0におけるシステム内客数，0,を
＝1,2,…
ρπ,
時間間隔0,における到着客数，0,を時間間隔0,におけ
で与えられる．
るシステムからの離脱客数とすると
