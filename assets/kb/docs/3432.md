# 7・5・2 ハイブリッドオートマトン

> **章**: 第7章 離散事象システム
> **節**: 7・5 ハイブリッドシステム

す変数＝,,…,，ならびに離散状態の変化による連続は連続状態の次元である．
離散変数間の遷移を表すために，有向多重グラフ,を用いは枝集合で，各枝は離散状態間の遷移関係を表す．
各離散状態∈に対して集合init，inv，flowがある．
init離散状態 で がとりうる値の集合を，flow⊆ × は でそれぞれ表している．
各枝∈に対して，離散状態の遷移前後での連続状態の変化用いる．
事象の集合を∑とおく．
一般に∑は有限集合である．
各枝と事に対して，event∈∑はその遷移のときに生起する事象を表b.セマンティックス ハイブリッドシステムのふるまいを記を定義する．
ただし， ：状態の集合， ：ラベルの集合， ⊆××のとき， ′ と書く．
の2種類のトランジションシステムがある．
ョンシステム＝,,,の定義は以下のとおりである．
下のように定義される．
各事象σ∈∑に対して，以下の3条件を満足する枝∈各非負実数δ∈ に対して，＝であり，かつ以下の,δ ,が定義される．
すなわち，,,δ,,∈集合flowは変数 のみで表される．
線形でないハイブリッドオートマトンは非線形ハイブリッドオートマトンと呼ばれる．
線形ハイブリッドオートマトンのサブクラスとして，以下のようなものがある．
上の区間を (＝1,2,…,）の直積＝ を 次元の矩形という．
各区間の端点は有理数か無限遠点のいずれかである．
各が有界であるとき，矩形 はそれぞれ有界であるという．
ハイブリッドオートマトン が以下の3条件を満たすとき，矩形オートマトンであるという．
の離散状態∈において，init が，∈と書ける有界な 次元矩形 が存在し，invが，∈と書ける 次元矩形 が存在する．
の離散状態∈において，flow が，∈と書ける有界な 次元矩形 が存在する．
の各枝ε∈において，すべての,′∈jumpが，∈∧∀∈′＝∧′∈となるような2個の次元矩形， と変数の集合⊆,…,が存在する．
ただし，∈,…, / ならば は有界である．
矩形オートマトンでは，フロー矩形 が離散状態に依存しない．
フロー矩形が離散状態に依存する，すなわち，各離散状態において∈となるとき，このオートマトンを多重矩形オの点からなる矩形オートマトンを単一オートマトンという．
さらに，フロー矩形が離散状態に依存する場合には多重単一オートマトンという．
1,1である単一オートマトンを時間付きオートマトンという．
時期化軌道，有限列のとき有限初期化軌道という．
における無限システムという．
のすべての有限初期化軌道が の軌道に拡張いう．
また初期化軌道 に対して，ラベルの列 …を,のる．
る無限軌道 …に対して＝または,∑となる集合をそれぞれ， とおく．
が に対して機械閉であるイブトランジションシステム,と,のトレースはそ,…,と整数値定数,,…,に対して，＋＋…等式という．
以下の2条件を満たすハイブリッドオートマトン結合で表される．
の（ から出発する）軌道という．
もし が初期状態ならば，り，実時間システムのモデルとして時間付きオートマトンがしばしe .ハイブリッドオートマトンにおける双模倣性 まず，双模ステム＝,,,を考える．
を状態集合 上の同値において各遷移に対して継続期間を以下のように定義する．
を満たす任意の,∈に対して 事象σ∈∑に対して，遷移σ ′ の継続時間は零である．
－′ならば， ′ で′′となる′∈が存在する．
の無限軌道 …において， の継続期間をδとおと がともに -模倣であるとき， は -双模倣であると模倣的であるといい， ～ と書く．
2項関係～は同値関係となり，それ自身が -双模倣となる．
-双模倣的な同値関係～によるンシステム～＝～,,～,～ が定義できる．
ただし，と定義される．
明らかに， の各同値類は～の同値類の和集合で表現できる．
また，～が有限集合となるとき，同値関係～が有Φ⊆を命題の集合とおく．
Φから誘導される同値関係≡を,∈に対して≡となるのは，任意のπ∈Φ対して,∩πが空集合もしくは,となるときのみである．
上の有限個の命題の集合をΦとおく．
命題πが に関するブ結合によって記述されるとき，πは線形であると呼ぶ．
線形命題πで書かれるとき，πは矩形であるという．
ただし， は の以下の定理が知られている．
テム と 上の有限個の矩形命題の集合Φに対して，有限な≡-［定理2］ 二次元矩形オートマトン の時間抽象トランジショ限な≡ -双模倣関係が存在する．
係が存在しないことがわかっている．
maton） は次の要素からなる．
連続状態を表す変数＝,,…,，連続状態の微分値を表状態の変化を表す変数′＝′,′,…,′を用いる．
ただし，集合 は節点集合で，各節点が離散状態を表す．
象の関係を表す関数をevent:∑とおく．
すなわち，各枝∈す．
述するためのグラフ構造としてラベル付きトランジションシステム：状態遷移関係，⊆：初期状態の集合を表す．
′∈ハイブリッドオートマトン のセマンティックスとして，以下.時間付きトランジションシステム 時間付きトランジシ集合， は の状態空間，初期値集合と呼ばれ，以の部分集合を -領域または単に領域という．
ただし， は非負の実数の集合を表す．
が存在するとき，,σ ′,′が定義される．
すなわち，1.枝 は節点 から′ への枝である．
2条件を満足する微分可能な関数:0,δ が存在するとき，である．
flow がいずれも成り立つ．
時間抽象（time- abstract）トランジションシステム時間抽象トランジションシステム＝,,,の定義は以下のとおりである．
と の定義は時間付きトランジションシステム と同じ．
ただし，τ ∑は時間の経過を表す事象である．
各事象σ∈∑に対して，σの定義は時間付きトランジションシステム と同じ．
時間付きトランジションシステム において,δ,を満たす非負実数δ∈ が存在するとき，,τ ,と定義する．
連続状態の時間的変化を規定するのが集合flowである．
各 に対して，,∈flow となる が一つしか存在しないとき，節点 において連続状態のダイナミクスは微分方程式で表されることになる．
一方， が複数個あるならば，微分包含で記述されることになる．
c .非ゼノン性 実際のシステム設計において，ハイブリッドオートマトン がライブであるためには，生起する事象の列が無限列となることに加えて，事象の生起時刻が発散することが必要である．
発散しない事象列が存在することをゼノン的であるという．
ここでは，非ゼノン性（non- Zenoness）の定義を述べる．
非ゼノン性を定義する前に，いくつかの補助的な用語を定義しておく．
ラベル付きトランジションシステム＝,, ,において，，＝を満たす有限または無限列＝ …をを の初期化軌道という．
さらに，この が無限列のとき無限初初期化軌道の集合を とおく．
対,をライブトランジションできるとき， は に対する機械閉（machine-closed）であるとトレースという．
∑δが発散するとき，この軌道は発散するという．
におけような の発散軌道 …が存在するとき，軌道とき，ハイブリッドオートマトン は非ゼノンであるという．
ラれぞれ の時間付きトレース，時間抽象トレースという．
＋を線形項といい，線形項 と に対して，≦を線形不を線形ハイブリッドオートマトンという．
システム に対する双模倣性について述べる．
状態集合⊆×間付きオートマトンでは，連続変数は時間の経過を表すことになば用いられている．
倣性（bisimulation）について説明する．
トランジションシ関係とおく．
2項関係 が以下の条件を満たすとき，トランジションシステム 上の -模倣（simulation）であるという．
実数δ∈ に対して，遷移 δ ′の継続時間はδであを満たす -模倣 が存在するとき， は を -模倣するという．
2項関係 の逆 を以下のように定義する．
となる -双模倣 が存在するとき， と は -双…は発散するという．
および の発散初期化軌道のの商集合（同値類の集合）～を状態集合とする商トランジショ,,∈ ～∃∈,∈:,,∈d.ハイブリッドオートマトンのサブクラス 実数値変数,限（finitary）であるという．
以下のように定義する．
の集合init，inv，flow，jumpが線形不等式のブール次に，ハイブリッドオートマトン の時間抽象トランジションーリアン値変数と連続状態 に関する線形不等式とのブーリアンの連続状態 に関する線形不等式条件がすべて≦または≧番めの要素で， は有理数定数である．
［定理1］ 単一オートマトン の時間抽象トランジションシス双模倣関係が存在する．
