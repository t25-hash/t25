# 7・5・3 ハイブリッドペトリネット

# 7・5・3 ハイブリッドペトリネット

> **章**: 第7章 離散事象システム
> **節**: 7・5 ハイブリッドシステム

a.時間ペトリネット トランジションの発火は瞬時に起こ
り，マーキングは から に瞬時に遷移すると考える確定的な
時間を導入したペトリネットを時間ペトリネット（timed Petri
net ）という．時間の導入方法の違いにより，発火遅延時間モデル，
発火継続時間モデル，プレース時間モデルの3種類に大別され
る ．ここでは，発火遅延時間モデルについて説明する．ネット
構造を表す有向2部グラフ＝,,Pre,Postと初期マーキン
グ で定義されたペトリネットを考える．ただし， はプレース
の集合， はトランジションの集合，Pre,はプレース から
トランジション への枝の重み，Post,はトランジション か
らプレース への枝の重みを表す．
発火遅延時間モデルでは，各トランジション に時間 を割当
てる． はトランジション が発火可能になってから実際に発火
するまでの遅延時間を表す．発火可能となったトランジションは，
発火するまでの間発火により取り去るトークンを各入力プレースに
おいて確保する必要がある．このために，トークンは，予約
（reserved）と非予約（nonreserved）の2種類の状態に分類され
る．時刻∈ においてプレース にある予約トークン数を
，非予約トークン数を と書く．ただし， にあ
るすべてのトークン数 は＝＋
である．初期マーキングはすべて非予約であるとする．すなわち，
各∈に対して0＝0＝である．時刻
においてトランジション が発火可能となるのは，式(7･13)を満
たすときである．
≧Pre,,∀∈● (7･13)
が発火可能となると， の各入力プレース∈● において
Pre,個のトークンが予約となる．つまり，
＝－Pre, (7･14)
＝＋Pre, (7･15)
となる．ただし， は の直前を表す．そして，時刻＋のと
きにトランジション は発火し， の各入力プレース の予約ト
ークンがPre,個取り去られ，各出力プレース に非予約トー
クンがPost,個投入される．
b.連続ペトリネット 連続ペトリネット（continuous Petri
net ）とは，トークンおよび枝重みとして実数値をとることを許し
たペトリネットである ．連続ペトリネットが提案された動機の
一つに，トークン数がかなり多い場合に可到達マーキング数の爆発
により事実上解析が不可能になることが挙げられる．例えば，生産
システムでは，各バッファに蓄えられるパーツの量を各プレースで
のトークン数としたとき，バッファの容量がかなり大きい場合に
は，可達集合の要素数が爆発的に増大する．そこで，パーツの個数
を整数ではなく実数で表現し，パーツの流れをフローとして近似し
て解析をするほうが実用上有効となる．時間を導入した場合には，
図7･22連続ペトリネットの例
図7･23 時間ペトリネットの挙動とその近似としての連続ペトリネット
の挙動（実線：時間ペトリネット，点線：連続ペトリネットの
挙動を表す)
本来トランジションが離散的に発火することにより離散的にマーキ
ングが変化するものを，フローとみなしたことによりマーキングが
連続的に変化する，つまり，トランジションの発火が継続的になる
という状況が必然的に生じてしまい，本来のペトリネットのふるま
いをうまく近似するためにどのような発火規則を採用すればよいか
という点が重要な問題点となる．ここでは，簡単のために，発火遅
延時間モデルで表された時間ペトリネットのふるまいの近似として
得られる連続時間ペトリネットの最もシンプルな発火規則を説明す
る ．
図7･22に示す時間ペトリネットを考える．初期マーキングを
5,0とすると，プレース と のマーキングの時間変化は図7･
23の実線のようになる．ここで，トランジション は0.1時間ご
とに発火するので，単位時間当たり10個のトークンが から
に流入し，同じく単位時間当たり10個のトークンが に流入して
いると考えられる．一般に，遅延時間が のトランジション に
おいて，各入力プレース からは速度Pre,でトークンが流
入し，各出力プレース′へは速度Post′,でトークンが流出
すると考えればよい．つまり，時刻 において は発火速度
＝1で連続的に発火していると考える．ところで，この例では定
常状態においてはマーキングが0,5と1,4を周期的に繰返すこ
とになる．しかし連続ペトリネットで近似した場合には，0,4に
なった瞬間にトランジション は発火が止まるが，同時に には
トークンが連続的に流入してくるので，その瞬間 は発火を再開
することになる．つまり，瞬時に発火の停止と再開が発生すること
になり，この発火規則では整合性がとれなくなる．さらに， へ
流入するトークンの速度のほうが から流出するトークンの速度
より小さいという点も考慮しなければならない．したがって，マー
キングが零になっても発火が継続するような規則を導入する必要が
ある．そこで，すべての入力プレースのトークン数が非零のときの
発火を強発火と呼び，そうでないときの発火を弱発火と呼ぶ ．
弱発火では， へ流入するトークンの速度を流出するトークンの速
度とする．換言すれば，流入するトークンの速度がPre,よ
りも大きければ強発火となり，その結果 にトークンが蓄えられ
ることになり，小さければトークン数は零のまま弱発火し続けるこ
とになる．したがって，この例では，トークンの変化は以下のよう
になる．
＝max5－5,0
＝min5,5
この軌道は図7･23の点線のようになる．さらに，トランジション
の発火速度 は以下のようになる．
＝10,
if＜5
otherwise
5,
＝5
一定値になるような発火規則を採用したペトリネットを定速度連続
ペトリネット（constant speed continous Petri net ）という．この
ほかにも，発火速度が入力プレースのトークン数に依存する可変速
度連続ペトリネットや漸近的連続ペトリネットなどが提案されてい
る ．
においてパーツの流れなどを連続量で近似することは有効ではある
が，機械の立上げ，停止などを表現するには離散的なトークンを利
用したほうがよい．そこで，連続値をとるトークンと離散値しかと
れないトークンを共存させたペトリネットをハイブリッドペトリネ
ット（hybrid Petri net ）という．
ス，連続トークンをもつ連続プレース，瞬時に発火する離散トラン
ジション，連続発火する連続トランジションからなる．これらを節
点とする有向グラフによってハイブリッドペトリネットは定義され
る．これらを区別するために図7･24に示す記号が用いられる．ネ
ット構造は＝,,Pre,Postで表し，枝の重みPre:×
ションが連続，離散のいずれであるかを示す関数:
,を導入する．ここで， は離散を， は連続を表す．例え
ば＝ならば は離散プレースを意味する． はハイブリッ
ド関数と呼ばれる．マーキング: は非負実数値をとる
が，離散プレース に対して は整数値に制限し，任意のトラ
ンジション に対してPre,とPost,も整数値に制限する．
離散プレースと連続トランジションの間に枝があってもよいが，以
下の条件を満たしていると仮定する．
ンジション は時刻 で発火可能であるという．
離散トランジションの発火規則は発火遅延時間モデルと同じ規則に
従う．連続トランジションについては，弱発火と強発火の2種類の
発火がある．弱発火となるのは，非予約トークンが零である入力プ
レースがあっても，ある連続トランジションからのトークンの流入
がその入力プレースにあるときである．このようにトークンの流入
があるとき，そのプレースは供給されている（supplied）と呼び，
連続トランジション が以下の条件を満たすとき，時刻 で発火
ション
トランジションが強発火または弱発火しているときの発火速度が
c .ハイブリッドペトリネットの定義 生産システム
ハイブリッドペトリネットは，離散トークンをもつ離散プレー
とPost:× は実数値をとる．各プレースとトランジ
＝∧＝Pre,＝Post, (7･16)
トランジションの発火可能性を定義する．次式を満たす離散トラ
≧Pre,,∀∈●
(ａ)離散プレース (ｂ)連続プレース
(ｃ) 離散トランジ
(ｄ)連続トランジ
ション
図7･24ハイブリッドペトリネットの節点 図7･25連続プレースと連続トランジションからなる競合の例
可能であるという．
(１)各離散プレース∈● に対して
≧Pre,
(２)各連続プレース∈● に対して，＞0，もしく
は は供給されている．
すべての入力プレースに非予約トークンが存在するとき， は強
発火可能であると呼び，非予約トークンがないが発火可能であると
き弱発火可能であるという．
遅延時間 をもつトランジション は，離散トランジションの
場合には発火可能となってから， 時間後に発火する．連続トラン
ジションの場合には，最大発火速度＝1で連続発火する．つ
まり，強発火の場合には発火速度は である．弱発火のときには，
入力プレースへ流入するトークンの速度に依存した 以下の速度
で発火する．
時刻 におけるマーキング は式(7･17)で記述される．
＝＋ σ＋ τdτ (7･17)
ただし， は接続行列と呼ばれ，その,成分はPost,
－Pre,となる．σは発火回数ベクトルでこの 番めの要素
は，時刻 までにトランジション が発火した回数であり，もし
＝のときはつねに零である．τは時刻τにおける発火速
度ベクトルで，この 番めの要素はトランジション の時刻τに
おける発火速度であり，連続発火していないときは発火速度は零と
なり，また＝ならば恒等的に零となる．
最近では，ハイブリッドペトリネットを拡張したバッチペトリネ
ット ，高水準ハイブリッドペトリネット ，微分ペトリネッ
ト などが提案されている．例えば，微分ペトリネットでは，連
続プレースと連続トランジションに関連したトークンや枝の重みに
負の実数を許すように拡張されている．
d.競合の解消 一般にペトリネットには競合が存在する．競
合とは複数個のトランジションが発火可能となるが，そのうちいく
つかが発火すると，発火可能であった他のトランジションが発火不
可能となることをいう．競合を解消するための規則を導入しておか
ないと，ペトリネットの挙動をシミュレーションすることができな
い．例えば一つのプレース から二つのトランジション ， へ枝
があるとき（図7･25）には，以下の6種類の競合がある ．
(１) ＝， ＝＝
本質的に離散ペトリネットでの競合と同じで，トランジションに
優先度を付けることにより競合を解消すればよい．
(２) ＝， ＝＝
(１)の場合と同様で，トランジションに優先度を付けて競合を解
消する．
(３) ＝， ＝， ＝
離散トランジションと連続トランジションの発火は本質的に異な
るので，優先度をトランジション個別に与えるという問題として扱
うよりも，離散トランジションと連続トランジションのいずれに優
先度を与えるのかという問題と見るほうがよい．離散トランジショ
ンを優先させる場合が実際には多い．
(４)＝， ＝， ＝
競合の有無が の値に依存する．もし＜Pre,な
らば， は発火可能とはならず， が連続発火することになる．
＞Pre,ならば，Pre,個のトークンが の発火のた
めに予約され，残り－Pre,が の連続発火のために使
われる．＝Pre,のときに競合が発生する．この場合に
は(３)の場合と同様，離散トランジションを優先的に発火させる場
合が多い．
(５)＝， ＝＝
のトークンをいずれのトランジションの発火のために使うかを
指定する必要がある．しかし，トークンの利用時間を指定しない
と，片方のみが連続発火し続けることになる．したがって，二つの
トランジションを交互に連続発火させるための時分割規則を導入す
る必要がある．
(６)＝， ＝＝
トランジションに優先権を付けて一方のトランジションのみを発
火させることもできるし，連続値トークンを二つのトランジション
でシェアし，両方が並行的に連続発火させることもできる．
