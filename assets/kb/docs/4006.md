# 2・1・4 論理回路

# 2・1・4 論理回路

> **章**: 第2章 基礎技術
> **節**: 2・1 ディジタル論理の基礎

複数のディジタル信号の入力を論理変数とし，それらの間の論理
れる．
演算を実行する回路を論理回路という．
a.組合せ回路と順序回路 すべての論理回路は，AND，
OR，NOTの論理回路（デバイス論理ゲートという）を組合わせ
て作ることができる．また一般に，論理回路は組合せ回路（com-
binational circuit ）と順序回路（sequential circuit ）に分類され，
前者はすべての入力の除去と同時に出力も消滅する回路，後者はす
べての入力を除去しても出力が残存する，あるいは複数入力を与え
る順序により出力の状態が変わる回路である．順序回路は組合せ回
路にフィードバックループを付加して作られる．その例はフリップ
禁止されている．
フロップ（FF:flip-flop）である．ゲートもFFもIC（integrated
circuit ）の形で市販されている．
b.ICゲート 最も多く用いられているICゲートはTTL
(transistor transistor logic)，CMOS(complementary metal
oxide semiconductor ，シーモス）ゲートである．TTLは高速動
作が可能であるが，スレッショルド電圧（その値を超える電圧が入
力すると入力1が入ったことになる）が約1.4Vで雑音に弱い
（小さい雑音で誤って動作する）．CMOSは消費電力が小さく（腕
時計などによく使われる)，またスレッショルド電圧が約2.5V
（電源電圧5Vの半分）で雑音には強いが，低速動作（周波数特性
が劣り1MHz以上では使用困難）であり，さらに出力インピーダ
ンスが高い（プリント基板のパターンやコネクタ配線が長いと雑音
を拾いやすい）．TTLゲートとCMOSゲートとをそのまま接続し
たのでは正常動作が得られない．
6(ａ)は6個のNOTゲート（別名インバータ）が組込まれたもの
（例：SN7404)，図2･6(ｂ)は2入力NANDゲート（例：
SN7400）である．ほかに排他的論理和（exclusive OR）ゲート
（入力 ， の一方が1のときだけ出力が1，例：SN7486，
CD4030)，2組の数の大小を比較する回路（例：SN7485，
MC14585)，BCDコードを10進数に変換する回路（例：
SN7442)，2組の数の加・減算を行う回路（例：SN7483，
CD4008）などが市販されている．
(ａ)タイムチャート
c .フリップフロップ FFは2個の出力端子をもち，一方の
出力が1のとき他方の出力は0となるので，出力を ， と書く．
SR-FFは，図2･7（横軸は時間）のように入力 が1になると出
力 は1，入力 が1になると出力 は0となる． をセット入
力， をリセット入力という． ， がともに1になることは禁
止されている（誤動作を生じる）．図2･7はタイムチャートと呼ば
SRT- FFは入力 ， のほかにクロック入力 を与える．ディ
ジタルシステムでは一定の周期のクロックパルスの発振器［例えば
8ビットマイクロコンピュータ用Z80CPU（central processing
unit ，中央処理装置）では2.5MHz］を備え，クロックがくるご
とに論理演算が順次行われるようになっている．SRT-FFは図2･
7(ｂ)のような動作を行う．すなわち が1であり が1のとき
は1となり， が1で が1のとき は0となる（その他で
は の変化がない）．この場合も ， がともに1となることは
JK-FFは二つの入力が同時に1となってもよいFFである．そ
のタイムチャートは図2･8(ａ)である．入力 ， がともに1のと
きクロックパルス が1となれば出力 の状態(1，0)が反転す
る．JK- FFのブロック図を図2･8(ｂ)に示す．ここで， ， を
入力として使えばSR- FFの動作を行い， ， ， に入力を与
えればSRT- FFの動作を行う（例：SN7473，7476)．
そのほか，入力をクロックに同期して出力するD- FF（例：
SN7474，7475)，2個のパルスを入力し1個の方形波を出力する
T-FFがある．
d.カウンタとレジスタ
.カウンタ（counter ) 入力パルスの個数を 進数の形
で計数できるのがカウンタである．FFを 個接続すれば＝2
であるが，任意の のものも設計できる．入力パルスの計数を10
進数で表すICの例としてSN7490がある．
図2･6はICゲートのブロック図（ピン接続）を示す．図2･
(ａ) SR-FF
(ｂ)SRT-FF
図2･7 SR，SRTフリップフロップのタイムチャート
(ａ)インバータ
(ｂ)2入力NAND
図2･ 6 ICゲートのブロック図
(ｂ)ブロック図
図2･8 JKフリップフロップ
.レジスタとシフトレジスタ パンチテープの孔から読み
されるとき，多くの場合D-A変換器のモデルとして
取られたデータ（孔のありは1，なしは0）を蓄えるには，そのビ
ット数だけの個数のFFを使えばよい．入力を与えておいて書込み
パルスを与えると，ANDゲートを通って各FFに 入力が入り，
入力 の1，0に応じて出力 が1，0となる．これがレジスタ
系の性能に関して不利にはたらく．
（register ）である．読出しパルスを与えると出力が外部に取出さ
(１)サンプル時刻以外は開ループ制御である．
れる．
(２)サンプル時刻間における制御入力は定値である．
乗除算を実行する場合には，レジスタの各ビットの内容を順番に
右または左に移していく（シフトパルスを与えて行う）動作が必要
である．これを行うものをシフトレジスタ（shift register ）と呼ぶ
（例：SN9495)．またシフトレジスタを使うと，パンチ孔から与え
られたコードをシフトパルスを与えるごとにディジタル波形に変え
， ，
て出力することができる．
＝sinω
＝sinω＋ω
