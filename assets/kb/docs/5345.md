# 7・2・4 PKI （public key infrastructure)

# 7・2・4 PKI （public key infrastructure)

> **章**: 第7章 情報圧縮・セキュリティ技術
> **節**: 7・2 情報セキュリティ技術

る．もし，電子署名が正当であれば，電子署名を作れるのはAさ
んだけである（MACのときと異なり，受信者であるBさんは電子
署名が作れない）ため，否認も防止できる．
ここで，署名生成・検証方式を簡単に説明する．メッセージを
，そのハッシュ値を＝hashとする．RSA暗号において
は，電子署名は，ハッシュ値 を秘密鍵 で暗号化することによ
り生成される．一方，電子署名を検証するには，この値を公開鍵
で復号化し，元のハッシュ値 と比較することにより行われる
（同一であれば正当であると判定できる）．だ円曲線暗号において
は， の位数を ，＝＝,，＝mod，＝
＋modとすると，電子署名は,となる．一方，電子
署名,を検証するには，＝1mod，＝mod，＝
mod，＝,＝＋と計算し，modが と
一致するか検証することにより行う．
